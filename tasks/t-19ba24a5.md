---
id: t-19ba24a5
title: Audit P56: cron/kanban/fleet residue r6 — iOS stale next_run_at, Review column, fleet repeat/model, dispatch scope
status: done
added: 2026-09-12
priority: high
---

## Description

Round-6 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round6.md, cron/kanban section). Needs round-6 product decisions 6–9. Relates t-3bcd1d7f.

- HIGH · PRE · iOS `CronEditorView.buildJob` forwards `nextRunAt: existing?.nextRunAt` across a schedule change (`Scarf iOS/Cron/CronListView.swift:571`; written by `IOSCronViewModel.upsert` → `saveJobs` `:424-431`). `_evaluate_due_job` fires on the stored `next_run_at` (`cron/jobs.py:2925` @ v2026.9.7) and only recomputes when absent; a moved one-shot fires at the OLD time, or is retired by `_retire_expired_oneshot` (`:2853-2865`) without running; a re-timed interval fires once early. Only `kind == cron` is repaired (`_reanchor_stale_cron` `:2801-2819`). `clearingNextRunAt()` exists (`HermesCronJob.swift:605`) and is applied only on the `setEnabled` fallback (`IOSCronViewModel.swift:218-219`). Fix: `nextRunAt: scheduleMoved ? nil : existing?.nextRunAt`, reusing the `scheduleMoved` flag at `:547-552`. Mac has no twin (shells `cron edit`).
- MED · PRE · Kanban Review column is a dead end: `KanbanService.plan(for:)` (`:589-635`) has no `.review` source arm; every drag out throws "No CLI path exists for this transition." — false: `kb.complete_task` accepts `review -> done` (`kanban_db.py:2524-2534`) and `kanban reopen-review` exists (`kanban_parser.py:323`, `kanban.py:990-1008`). No context-menu/inspector door either (`KanbanCardView.swift:172-198`). Decision 6. Both need `hasKanbanV015` gating.
- MED · PRE · Fleet copy drops `repeat` (`FleetApplyPlan.swift:370-372,:422` hard-codes `repeatCount: nil`; doc claims it is not modeled — false since P38: `HermesCronJob.repeatSpec` `:800-812`). `cron create --repeat` accepted (`subcommands/cron.py:38`). Decision 7: forward `job.repeatSpec.times`.
- MED · PRE · Fleet copy drops `--model/--provider/--reasoning-effort` with no downgrade note at either seam (`FleetApplyPlan.swift:378-380`; `FleetApplyViewModel.caveats:145-190`; `FleetApplyExecutor:272-274,:430-442`). Decision 8: the P50 two-seam note shape (`hasModelPin`, caveat, `modelPinDowngrades` counter + catalogue row).
- MED · PRE · Dragging one card to Running runs `hermes kanban dispatch --json` board-wide (`KanbanBoardViewModel.swift:566-582`; no per-task flag, only `--dry-run/--max/--failure-limit/--json`, `kanban_parser.py:346-353`). Decision 9: `--max 1` + copy, or a confirm/notice.
- LOW · PRE · `"Edit cron job"` (`CronListView.swift:169`) has no catalogue row (siblings do) → t-3bcd1d7f.
- LOW · PRE · `KanbanCreateRequest.argv()` emits `--branch` ungated (`:101-103`); first at v2026.5.28 (0.15.0), absent v2026.5.16; no production caller sets it today. Gate or doc line.

## Plan



## Artifacts

**Done.** Six commits on `fix/whole-surface-audit-r6`: `d3db068c`, `e4e25ccb`, `9c882c65`, `e3eeaca7`, `058ea28c` (fresh-eyes remediation), `070688ee`.

Per finding:

- **HIGH · iOS `buildJob` stale `nextRunAt` — FIXED** (`d3db068c`). `nextRunAt: scheduleMoved ? nil : existing?.nextRunAt`. Citations re-opened at `v2026.9.7`: `_evaluate_due_job` (`cron/jobs.py:2910`, `:2925`), `_retire_expired_oneshot` (`:2853-2865`), `_reanchor_stale_cron` (`:2801-2819`), `_recover_missing_next_run` (`:2690-2710`). Mac has no twin (shells `cron edit`).
- **Lesson 14 (same commit)** — all eight `update_job` gates enumerated from `cron_edit` (`hermes_cli/cron.py:594-632`); five already in the form, three missing and all cheap, so fixed here as `HermesCronJob.scheduleFormRefusal` + `CronScheduleFormRefusal`: blank/malformed cron `expr`, `interval` with no `minutes`, unreadable one-shot `run_at`. The minutes FIELD (a feature, not a validation) is `t-b74c65a4`.
- **MED · Review column — FIXED** (`e4e25ccb`, corrected by `058ea28c`). **Decision 6's `hasKanbanV015` was wrong**: `complete_task` takes `'review'` only from `v2026.8.13` (0.20.1), not at `v2026.8.3` (0.20.0) nor at the v0.15 floor `v2026.5.28`; `reopen-review` lands at the same tag. New `hasKanbanReviewExits = isV0201OrLater` covers both. `.review` source arms, `reopenReviewArgv` with `--` (one list-valued parser, unlike `archive`), `KanbanTransitionStep.reopenReview`. **No C5 output verdict needed** — both verbs exit 1 on refusal via `_bulk_apply` (`kanban_output.py:61-69`). **No iOS twin** — `ScarfGoKanbanView` is read-only, no drag and no menu.
- **MED · fleet `--repeat` — FIXED** (`e3eeaca7`). `repeatCount: job.repeatSpec.times`. **No gate**: present at `subcommands/cron.py:38` @ `v2026.9.7` and `main.py:3936` @ `v2026.3.30` (0.6.0 floor), first at `v2026.3.17`. An older target renders it identically.
- **MED · fleet model pin — FIXED** (`e3eeaca7`), the P50 shape exactly: `hasModelPin`/`modelPinFields`, `FleetApplyViewModel.caveats`, `modelPinDowngrades` on the executor's success arm, catalogue row in six locales. `FleetApplyExecutorCatalogueP50bTests` (the file-scoped forward gate) caught the row's absence before the commit.
- **MED · drag-to-Running confirm — FIXED** (`9c882c65`). `pendingDispatch` / `confirmPendingDispatch()` / `cancelPendingDispatch()` + a `confirmationDialog`; `attemptMove` returns before the optimistic override too. Keyed on the destination, so the inspector Claim button and the `blocked`/`scheduled` sources ask as well.
- **LOW · `"Edit cron job"` catalogue row — FIXED** (`d3db068c`), six locales + `iosOnlyKeys`.
- **LOW · `--branch` — DELETED, not gated** (`e4e25ccb`). Floor confirmed (`v2026.5.28` has it, `v2026.5.16` does not) but no production caller ever set it, so deleting beat a flag + four-test group guarding a field nothing fills. Alarm kept.

**Also fixed, pre-existing and proven so**: `HermesP38SourceSweepTests.noSubscriptFollowsACountExpectation` was RED at `475dec73` (reproduced on a worktree at that commit) on `strings[key] == nil`. Matcher tightened with a calibration case and a planted needle (`9c882c65`).

**Tests.** ScarfCore `swift test` **3213 in 269 suites** (a `475dec73` worktree: **3173 in 262**) → +40 tests, +7 suites; the only failures were `M4ACPIOSTests`' two ACP `initialize` timeouts, which reproduced on that worktree and pass in isolation (46/46). Mac serial over 9 suites (`KanbanDispatchConfirmP56Tests`, `FleetModelPinNoteP56Tests`, `HermesP38SourceSweepTests`, `SubscriptAfterCountMatcherP48bTests`, `LocalizationCatalogTests`, `FleetCronNoteP50Tests`, `FleetApplyExecutorCatalogueP50bTests`, `CatalogueCoverageP47Tests`, `MainActorSpawnDisciplineP22Tests`): **57 tests, 0 failures**. `scarf` Debug and `scarf mobile` both build clean.

**Memory.** `decisions/hermes-v0-21-1-compatibility-decisions` gains `## Whole-surface remediation — P56`; `architecture/kanban-board-architecture-v2-7-5`, `architecture/hermes-cron-recovery-is-three-doors-not-one-and-scarf-must` and `decisions/phase-1-milestone-3-fleet-and-portfolio-dimension-implementation-decisions` edited in place (the last retires its own "cron-fidelity-gap"/"deferred (4)" `--repeat` entries).

**Tasks filed.** `t-b74c65a4` (iOS interval-minutes field), `t-b290817d` (reason sheet on `review -> upNext`, result sheet on `review -> done`).

