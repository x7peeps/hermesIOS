---
id: t-f1593849
title: Voice: M1ACPTests flake under the full ScarfCore run (branch-only)
status: done
added: 2026-09-18
priority: high
---

## Description

On feat/voice (after merging the audit fixes F1/F2a/F3/F5), the full ScarfCore `swift test` fails 2 of 3 runs. The failing tests are M1ACPTests ones (e.g. loadSessionSucceedsOnDictResult, clientRoutesSessionUpdateNotificationToEventStream), all hitting the 2 s `waitFor` timeout at M1ACPTests.swift:846. They pass in isolation. `main` passes 3/3 on the same machine. The branch changed ACPClient.swift (context notes) and added many tests (WKWebView JS tests, engine timing tests). Find the real cause: does a new test hog executors or the main actor, sleep on the cooperative pool, or leak tasks? Or did ACPClient get slower? Fix the cause rather than raising timeouts, unless the cause is a genuinely fair load issue.

## Plan



## Artifacts

Fix commit 7612d8ad on branch feat/voice-flake (worktree scratchpad/voice-flake), test-only, not pushed.

Causes:
1. HermesP55GoalMirrorTests.mirrorStateIsGoneFromEveryTarget was a sync @MainActor repo sweep that held the MAIN thread for 26 s alone and 30-97 s in the full run. M1ACPTests are @MainActor, so a test mid-waitFor could not get back onto the main actor. Main passes only because its M1ACP bodies happen to queue behind the sweep. Repro: `swift test --filter 'M1ACPTests|HermesP55GoalMirrorTests'` fails 2/2 before the fix and passes 3/3 after.
2. ShellTestRunner (new on the branch) blocked on a DispatchSemaphore inside sync tests. Each python/sh child parked a cooperative-pool thread; the two Hermes ACP contract tests held 2 of 10 threads for 11 s+.
3. aDisconnectIsLostOnlyAfterTheGrace compared Swift sleeps against a 150 ms JS timer.
ACPClient.swift diff: no added latency (a pure promptBlocks refactor plus an overload).

Fixes: the sweep is off @MainActor and uses one compiled regex (2 s), with a matcher calibration test and a source pin. ShellTestRunner is async (terminationHandler plus a dispatch timer, handles closed after spawn), callers are async, the trait is async, and ShellTestRunnerPoolTests is a rendezvous test. The WebView grace test is timed in the page. The M1/M4 waitFor ceiling goes from 2 s to 30 s, a legitimate-load ceiling paid only on failure.

Evidence: unfixed at load 21-28: 2/4 full runs red. Fixed: 8/8 consecutive full runs green (logs scratchpad/flake-logs/v4-*.log, ~29 s each vs 45-52 s unfixed). Mutants: @MainActor restored or the matcher's \b dropped both fail the P55 tests; a blocking runner fails ShellTestRunnerPoolTests; both page mutants fail the grace test. ScarfIOS swift test: 92 tests green.
Memory: conventions/a-scarfcore-test-that-blocks-the-main-thread-or-a-pool.

