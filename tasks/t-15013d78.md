---
id: t-15013d78
title: Update the ScarfDesign SlashMenu mockup to the real ACP roster
status: todo
added: 2026-09-10
priority: low
---

## Description

`scarf/Packages/ScarfDesign/Sources/ScarfDesign/ScarfChatView.swift:853-861` — the design-gallery `SlashMenu` preview hardcodes `compress, clear, model, project, memory, cost`. P34 reconciled the live menu (`RichChatViewModel.alwaysAvailableCommands`) with the ACP adapter's roster: `/cost` has never existed in Hermes at any tag (the CLI verb is `usage`), and `/clear`, `/project`, `/memory` are not ACP slash commands either. The mockup is a static showcase, not the live menu, so it is cosmetic — but it is now the only place in the repo that shows users a `/cost` row. Replace with the real roster: `compress, reset, model, tools, context, version`.

Found during P34 (`t-069033ec`), deliberately left out of scope.

## Plan



## Artifacts



