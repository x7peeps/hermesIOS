---
id: t-1eaf1579
title: Model GATEWAY_MULTIPLEX_PROFILES and SLACK_REQUIRE_MENTION .env overrides in Settings
status: todo
added: 2026-09-10
priority: low
---

## Description

Deferred by round-3 product decision 7 (2026-09-10). Hermes reads `GATEWAY_MULTIPLEX_PROFILES` (`gateway/config.py:76-95,711-713` @ v2026.9.7) and `SLACK_REQUIRE_MENTION` (`plugins/platforms/slack/adapter.py:5923`) from the environment and lets them override config.yaml; Scarf's Settings (`ProfileRoutesYAML.parseMultiplex`, `HermesConfig+YAML.swift:606`) reads only the YAML, so on a provisioned host the rendered value can differ from the value in effect. Doing it means adding an `.env` read to the Settings load (P22 choreographed that load; keep it off-main) and a third "in effect from .env" spelling with the row read-only when the env wins. Not worth it this cycle; revisit when Settings next grows an `.env`-aware row.

## Plan



## Artifacts



