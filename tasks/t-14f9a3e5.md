---
id: t-14f9a3e5
title: Check SkillDetailView markdown render for link-scheme bait
status: todo
added: 2026-09-10
priority: low
---

## Description

Out-of-phase finding from P25. `scarf/Scarf iOS/Skills/Installed/SkillDetailView.swift:301` renders a skill's own SKILL.md text through `AttributedString(markdown: raw, options: opts)` with no link-scheme allowlist — the same vector P25 closed for worker-authored kanban bodies (`scarf/Scarf iOS/Kanban/ScarfGoKanbanDetailSheet.swift:82`, now plain `Text`). A hub skill is third-party content, so `[Approve](javascript:…)`-shaped bait is possible. Decide whether this should be plain `Text`, routed through the allowlisted `MarkdownContentView`/`MarkdownRenderer` path, or left as-is because the options already strip links — read `opts` at that site first. Also worth a look: `scarf/Scarf iOS/Projects/Widgets/TextWidgetView.swift:40` (user-authored, likely fine).

## Plan



## Artifacts



