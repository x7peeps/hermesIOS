---
id: t-521b5646
title: Voice F6: fresh-eyes fixes on main after the voice merge (Mac + ScarfGo + ScarfIOS)
status: done
added: 2026-09-19
priority: high
---

## Description

From the 2026-09-19 fresh-eyes review of `git diff ad0ae467..main`. Sibling of t-30cee401 (ScarfCore). Each fix gets a test that fails without it.
1. HIGH ScarfGo: nothing observes ChatController.state, so a dead/failed ACP connection leaves the Live Voice session streaming to OpenAI (billing) and every spoken turn throws .chatNotReady. Mac has endForLostConnection(); add a .hermesConnectionLost teardown trigger in VoiceLiveSessionModel + onChange(of: controller.state) in ChatView, with a sheet notice.
2. MEDIUM C9 MessageSpeechService ~170: logs String(describing: error) at privacy .public; that error carries verbatim host stdout/stderr from the TTS script (could contain a provider key). Log the case name publicly, the tail privately.
3. MEDIUM Mac VoiceLiveController/ChatViewModel.acceptVoiceLiveConsent: consent is checked before registry.claim, so Continue while another window started a session silently does nothing. Re-check isBlockedByAnotherWindow and surface the "running in another Scarf window" hint.
4. MEDIUM iOS VoiceLiveAVAudioSession.deactivate never restores the pre-session category; and the deferred deactivate can fire while dictation (re-enabled the moment isActive flips) holds the session. Restore the prior category/mode/options; guard the deferred deactivate with a generation/owner token.
5. MEDIUM CitadelServerTransport.streamScript ~356-386: the stdin write runs inside the drain task before draining starts, and writeAndFlush ignores cancellation, so a write parked on a full window blocks past the timeout. Put the write in its own group task so the drain runs concurrently.
6. LOW PushToTalkController.showNotice dedupes by value so two identical notices share one timer; use a generation counter like VoiceLiveSessionModel.showComposerNotice.
7. LOW MessageSpeechService.playAudioFiles appends to pendingTempFiles before AVAudioFile(forReading:) can throw; on throw the WAVs leak until the next stop().
8. LOW Mac ChatTranscriptPane.onDisappear calls endImmediately() but not dismiss(), so a stale "session ended" strip and a dead WKWebView survive leaving and re-entering Chat.
Not here: t-2140ec98 (ScarfGo cancels typed turns) runs after this lands.

## Plan



## Artifacts



