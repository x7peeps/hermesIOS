---
id: t-93517390
title: Project tool/skill scoping (blocked on hermes-agent#45958)
status: todo
added: 2026-06-21
---

## Description

Apply per-project scoped toolsets/skills at ACP session start. `ScarfProject` already carries `scopedToolsets`/`scopedSkills` (currently always empty). BLOCKED UPSTREAM: Hermes's `acp_adapter/session.py:_make_agent` hardcodes `enabled_toolsets=["hermes-acp"]` (+ configured MCP servers) and ignores the `hermes acp -t/--toolsets` flag — there is no per-session toolset seam. Filed NousResearch/hermes-agent#45955 (issue) + #45958 (PR adding `SessionManager(default_toolsets)` + a per-session `toolsets` param on `session/new`). Buildable ONLY once that merges; then thread the project's scoped sets into the ACP `new_session` call and surface a "Scope" panel in the cockpit. From the Phase-1 "projects amazing" plan — Decision #2 (locked 2026-06-14), the one upstream-blocked item.

## Plan

UPSTREAM PR RESUBMITTED 2026-06-28. NousResearch/hermes-agent#45958 ("feat(acp): honor --toolsets and add per-session tool scoping") was OPEN but mergeStateStatus=DIRTY (conflicted) — created 2026-06-14, then v0.17 refactored the files it touches; that conflict was the "not live". Rebased the 2 commits (author+committer = Alan Wizemann <alan@wizemann.com>) onto latest origin/main (0c2e6c0, 2026-06-28): the FEATURE commit applied cleanly (its base already had `_expand_acp_enabled_toolsets`); only scripts/release.py's AUTHOR_MAP needed a trivial conflict resolve (other contributors added since June). Tests: tests/acp_adapter/test_acp_toolsets.py 4/4 pass against the rebased code (resolution chain: per-session override > `--toolsets` process default > historical ["hermes-acp"]). Force-pushed to awizemann:feat/acp-toolset-scoping (lease-guarded) → PR head now bdbad4058, mergeStateStatus DIRTY→BLOCKED (awaiting review/CI, no longer conflicted). PR is live again.

VERIFIED latest main STILL lacks the seam (create_session(cwd:)/_make_agent hardcode toolsets), so the feature is still needed.

SCARF-SIDE WORK STILL BLOCKED ON MERGE: once #45958 merges into a released Hermes, the Scarf side is small — thread ScarfProject.scopedToolsets/scopedSkills into the ACP new_session call (the PR adds a top-level/`_meta` `toolsets` param on session/new + accepts a JSON array or comma/space string) + surface a "Scope" panel in the cockpit. Re-check #45958 merge status each Hermes bump; build the Scarf side when it lands in the installed version.

## Artifacts



