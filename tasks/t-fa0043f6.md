---
id: t-fa0043f6
title: Audit P62: capability floors r7 — hasImageGenModel 0.11, hasXAIVoiceCloning, slash notice by rule, /new name, nine untested flags
status: todo
added: 2026-09-13
priority: high
---

## Description

Round-7 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round7.md, capabilities section). All PRE. Relates t-54ec6eb3 (29 walked this round, ~21 remain — list in the report). Needs round-7 product decision 3.

- MED · `hasImageGenModel` (`HermesCapabilities.swift:396`) floored 0.13 on "advertised but ignored pre-0.13" — false: `tools/image_generation_tool.py:495-508` @ v2026.4.23 (0.11.0) reads and applies `image_gen.model`; needle absent @ v2026.4.16. Real floor 0.11.0; consumer `AuxiliaryTab.swift:185` hides the row on 0.11/0.12 hosts that honour it. Re-floor + four-test group.
- MED · `hasXAIVoiceCloning` (`:404`) floored 0.13 on a Hermes COMMENT: `tools/tts_tool.py::_generate_xai_tts` byte-identical @ v2026.4.30 and v2026.5.7 (`voice_id` read at `:875` both); only `hermes_cli/config.py:887` @ v2026.5.7 gained a trailing comment. Consumer `VoiceTab.swift:93` badge. Re-floor to the tag `tts.xai.voice_id` arrived, or retire if ≤ 0.6.0. Lesson: a floor set by a Hermes comment is a release-note floor; open the implementation at the tag before.
- MED · the unknown-slash notice is a two-name allowlist (`RichChatViewModel.acpUnhandledSlashNames = ["goal","subgoal"]` `:1145`); every other non-roster slash name (`/clear`, `/cost`, `/yolo`, `/sessions`, `/exit`, `/reload-skills`, `/codex-runtime`, typos) still reaches the model silently on Mac (`ChatViewModel.swift:1361-1382`) and iOS (`ChatView.swift:1715-1730`); `acp_adapter/commands.py:94-95` @ v2026.9.7 returns None. Derive the notice: leading-slash name in neither `availableCommands` nor the client-side/project/global sets → notice.
- MED · `hasNewWithSessionName` (floor 0.13 correct: `commands.py:61-62` @ v2026.4.30 vs `:67` @ v2026.5.7) arms `argumentHint: "[<name>]"` on `/new` (`RichChatViewModel:788`) but both consumers drop the name (`ChatViewModel.swift:1228-1233`, `ChatView.swift:1606-1616`, `_ = name`). Decision 3: withdraw the hint now and file the feature (reviewer's read) or implement `startNewSession(name:)`.
- MED · `ChatViewModel.deleteSession` `:2591` calls `sessionDeleteRunner` synchronously on the main actor (production default `ctx.runHermes(...)`, a spawn/SSH exec) from `ChatSessionListPane.swift:120`; its twin `SessionsViewModel.confirmDelete:602-608` hops off. Use `OffPool.run`, keep the injected seam. (C10; the sweep's domain is `Task.detached` bodies so it cannot see a bare sync call — see P63.)
- MED · two flags in no test anywhere: `hasSkillsUninstallYes` (`:2209`, declared inside "Convenience predicates" not the v0.20.5 group; floor correct: `subcommands/skills.py:185` @ v2026.8.19 vs parser ends `:184` @ v2026.8.18) and `hasEssentialHermesAgentSkill` (`:1804`; `skills_config.py:55-64` @ v2026.8.27 vs absent @ v2026.8.19). Seven more absent from `HermesCapabilitiesTests`: `hasBotMode`, `hasCronPastOneShotResumeRefusal`, `hasKanbanProviderOverride`, `hasManagedMarkerContents`, `hasPluginEnableToolOverrideFlag`, `hasPluginsListJSON`, `hasReasoningDisableAliases`; predicates `isV0181OrLater`, `isV0202OrLater` untested. Fix: move/enumerate, and add the mechanical guard: a source-scan test parsing every `public var has…` from `HermesCapabilities.swift` and asserting each is named in `HermesCapabilitiesTests.swift`.
- LOW · `hasCronWorkdir` doc `:170` names `--context-from`, an argv at no tag (jobs.json field / web form only). Drop the clause, cite the two tags (`--workdir` at `main.py:8506,:8548` @ v2026.4.30; absent @ v2026.4.23).
- LOW · `hasVercelTerminal` (0.12) is a window not a floor: symbol absent tree-wide @ v2026.5.28, partial 0.16–0.19.0, back @ v2026.7.30. Consumer-free; doc the hole or model as a window when a consumer lands.
- Not reached on t-54ec6eb3 (~21): the v0.14 cluster — `hasSubgoal`, `hasSessionsSlashCommand`, `hasCodexRuntimeSlashCommand`, `hasGrokOAuthProvider`, `hasNovitaProvider`, `hasBraveFreeSearchBackend`, `hasDDGSearchBackend`, `hasMCPParallelToolCalls`, `hasDockerExtraArgs`, `hasDisplayTimestamps`, `hasCronDeliverAll`, `hasDiscordHistoryBackfill`, `hasOpenRouterParetoCoder`, `hasCustomProviderAPIMode`, `hasPluginToolOverride`, `hasHermesProxy`, `hasACPSetupBrowser`, `hasFileMutationVerifier`, `hasYOLOWarning`, `hasQwenCloudDisplayName`, `hasCrossSessionClaudeCache` (prioritise the last two).
- zsh gotcha for walkers: `git show $t:hermes_cli/x.py` is broken by the `:h` history modifier; write `"${t}:hermes_cli/x.py"`.

## Plan



## Artifacts



