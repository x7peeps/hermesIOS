---
id: t-d2000dc5
title: Audit P63: C10 residue r7 — async-func blocking seams (backup/restore/logs), sweep domain, launchTerminal env
status: todo
added: 2026-09-13
priority: high
---

## Description

Round-7 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round7.md, C10 section). All PRE. Relates t-406d56d6, t-02f830f4. Needs round-7 product decisions 4–5.

- HIGH · the OffPool sweep's domain is `Task.detached` bodies only; a blocking seam in a plain `async func` or an actor method is invisible by construction. Eleven sites: `RemoteBackupService.swift:117,:131,:176` (`preflight()`), `:163` `fileExists` and `:481` `estimateBytes` per project in a loop, `:236` in `run()` — 30–60 s SSH round trips each on the Backup sheet's first paint; `RemoteRestoreService.swift:206,:213,:259,:790,:793`; `HermesLogService.readLastLines()` `:139` — a synchronous actor method doing a 30 s `tail -n` exec (Logs pane on a remote server); `ServerContext.swift:343` `UserHomeCache.probe` (actor, 10 s). Fix: `try transport.runProcess(…)` → `try await transport.asyncRunProcess(…)` (the seam exists); enforce with a second matcher whose region is every `async` function body and non-`nonisolated` actor method, using the existing `isAwaitedCall`/`offPoolLineRanges` helpers. Decision 5: do backup/restore now (reviewer's read: yes, shipped surface).
- MED · `ChatViewModel.launchTerminal` main-actor `enrichedEnvironment()` (P22 allowance, `MainActorSpawnDisciplineP22Tests.swift:716-724`): 5 s + 3 s zsh probe on a cold "open remote terminal" click. Decision 4: split it now in the `SpotifyAuthFlow` shape or leave another round.
- MED · `FleetApplyExecutor` blocks cooperative-pool threads: `withTaskGroup` bounded at 4 (`:142-172`), each body calls sync `ctx.runHermes` (`:341`, `:417`) per job per host. Invisible to both sweeps. Convert to `asyncRunProcess`/`OffPool.run`.
- MED · guarded-store spellings are a needle the sweep lacks: `GuardedJSONStore(`/`store.inspect(`/`GuardedTextFile(` inside `Task.detached` at `IOSCronViewModel.swift:559-565` (the iOS cron save), `KanbanToolsetEnabler.swift`, `IOSMemoryViewModel.swift`, `SettingsViewModel.swift`, `SkillsViewModel.swift`. Add needles, convert or baseline with reasons.
- PRE noted: `RichChatViewModel.loadQuickCommands:2131-2134`, `loadGlobalScopedCommands:2181-2188` wrap a blocking `readText` in `Task.detached` (on t-406d56d6).
- Test health fact for the report: Mac PARALLEL `xcodebuild test` is red (84 issues / 8 suites on the branch, 124 / 12 on `main` at d53d3cbe — shared-global-state suites: `ChatViewModelStartLifecycleTests`, `SessionDeletedSignalTests`, `ScarfMiniAppBridgeTests`, `AllowEditsForSessionActionTests`, `MainActorBlockingWritesP11Tests`, `HermesCLIExitCodeTruthTests`, `ProjectTemplateBoundsP43Tests`); serial is the usable signal and the reports must say so. Consider `.serialized` on those suites.

## Plan



## Artifacts

## Release blockers (2026-09-13) — branch `fix/release-blockers`

**HIGH async-func blocking seams (backup / restore / logs) — DONE.**
- `cd43b9d2` fix(release): backup, restore and logs stop blocking on SSH

Ten `transport.runProcess(…)` → `try await transport.asyncRunProcess(…)`, same
timeouts:
- `RemoteBackupService` `:117` `:131` `:176` (preflight), `:236` (run's WAL
  checkpoint), `:481` (`estimateBytes`, which becomes `async`; both callers
  `await` it, including the per-project loop).
- `RemoteRestoreService` `:206` `:213` (inspect probes), `:259` (run's mkdir).
- `HermesLogService.readLastLines()` `:139` — the method becomes `async`. It was
  a synchronous ACTOR method holding a 30 s `tail -n`, so it froze the actor and
  every other caller. Both call sites (`LogsViewModel:135,:147`) already awaited
  it across the actor hop, so nothing changed at the call sites.
- `ServerContext.swift:343` `UserHomeCache.probe` — CONVERTED. It was already
  `async` with a single `async` caller (`resolve`), so it did not fight the
  actor's callers at all.

**Left alone, deliberately:** `RemoteBackupService:163` `fileExists` and
`RemoteRestoreService:790,:793` `readFile`/`stat`. Those are not `runProcess` and
`ServerTransport` has no `async` twin for them — `asyncRunProcess` is the only
async seam on the protocol. Converting them means adding protocol members, which
is outside this scope.

Test: `AsyncFunctionBlockingSeamTests` (`scarf/scarfTests`), a brace-matched
matcher whose REGION is every `async` function body (multi-line signatures
included) and whose needle is a bare `runProcess(`, reusing P52's
`containsNeedle` / `isAwaitedCall` / `offPoolLineRanges` / `stripComment` rather
than re-implementing them. Calibrated with a planted needle plus four
near-misses (the cure spelling, an already-awaited call, an `OffPool.run` body,
a synchronous function). Per-file premise check that the file still uses the
async seam.

Measured honestly: against the pre-fix tree it reports 8 of the 10 sites. The two
it misses — `readLastLines` and `estimateBytes` — were SYNCHRONOUS functions
then, so outside the region by construction; the fix made each `async` AND
awaited its call in one edit. The test's doc comment records this.

Every existing backup/restore/log test stays green (full ScarfCore run below).

Every other item on this task (launchTerminal, FleetApplyExecutor, guarded-store
needles, the `.serialized` suggestion) is untouched and still open.

