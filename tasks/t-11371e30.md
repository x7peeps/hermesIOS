---
id: t-11371e30
title: Audit P61: CLI verdict residue r7 — seven two-way .unconfirmed arms, dashboard gate, curator --, memory-reset seal
status: todo
added: 2026-09-13
priority: high
---

## Description

Round-7 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round7.md, CLI-verdicts section). All PRE. Needs round-7 product decisions 1–2.

- MED · seven `.unconfirmed` arms reach a two-way `if` (P54b invariant: unconfirmed gated on confidence alone; the `HermesMemoryResetVerdict.failureSummary` three-branch shape): `MCPServersViewModel.swift:157-167` (`HermesMCPRemoveVerdict` → "Remove failed: <tail>"), `ToolsViewModel.swift:81-92` (`HermesToolsToggle`), `ScarfCore/ViewModels/SkillsViewModel.swift:953-959, :966-974` (install/uninstall; `:972` also quotes the exit code the verdict declared meaningless), `ProjectSkillsViewModel.swift:85-94` (`skills trust|untrust`), `GatewayViewModel.swift:581-583, :607-615` (`HermesPairingVerdict`, `fallbackDetail: false` → bare "Approve failed"/"Revoke failed"). Decision 2: pairing's neutral wording.
- MED · Web Dashboard row release-gated and ungated (C1+C5): `HealthView.swift:413-416` shows on `!context.isRemote` alone; `HealthViewModel.swift:1268` spawns `dashboard --no-open --port` with stdout/stderr to /dev/null and no timeout; `hermes_cli/main.py` @ v2026.3.30 has no `dashboard` parser; `hasDashboardCommand` (`HermesCapabilities.swift:888`, 0.9) has zero production consumers. Decision 1: gate the row (reviewer's read) or keep it visible with an honest failure. Also bound the spawn.
- LOW · `CuratorService.swift:251` `curator rollback <id> -y` and `:275` `curator adopt <name> --yes` lack `--`; `curator.py:658-661` (`entry_id nargs="?"`), `:632` (`skill nargs="*"`) @ v2026.9.7, no list-valued option → safe.
- LOW · memory-reset alert is a two-state seal over three-state text: `MemoryView.swift:137-141` (`ResetAlert` has `.failure`/`.note` only), iOS `MemoryListView.swift:151` (`resetError`); "printed no result" renders under "Couldn't reset memory". Add the neutral arm.
- LOW · `HealthViewModel.migrateXAISummary` `:1069` else-arm is an unconditional "Migrated to …" with no marker matched; bounded by `hasXAIModelRetirement` but the exit-0⇒success shape. Judge on the success marker (`migrate.py` @ v2026.9.7).
- Site count at HEAD: 134 hermes-argv invocation sites (68 app / 61 ScarfCore / 5 iOS) with the widened vocabulary (round 6: 110).

## Plan



## Artifacts

## Release blockers (2026-09-13) — branch `fix/release-blockers`

**MED item 2 — Web Dashboard row release-gated and ungated — DONE.**
- `baac0639` fix(release): gate the Web Dashboard row, and bound its daemon
- `bd838c39` fix(release): the dashboard's escalation is a thread, not a Task

Tag walk re-opened, both tags: the string `dashboard` occurs ZERO times in
`hermes_cli/main.py` at `v2026.3.30` (0.6.0, the supported minimum) and zero
times at `v2026.4.8` (0.8.0, the tag before the floor). `def cmd_dashboard(args)`
is `main.py:4458` at `v2026.4.13` (`pyproject.toml` = 0.9.0), registered at
`:4180` and `:5978`; still present at `v2026.9.7` (`:2502`). So
`hasDashboardCommand`'s v0.9.0 floor is correct and the row now consults it —
its first production consumer.

C1 for the added gate: 0.9.0+ renders byte-identically to the previous release;
an undetected host hides the row until detection lands, which is the shape the
four other gated Health rows already use (`HealthView`'s
`.onChange(of: capabilitiesStore?.capabilities.detected)` re-renders).

C10: the LAUNCH is already bounded (nothing waits on the daemon; liveness is the
HTTP probe, itself capped at ~6 s). What had no ceiling was the way OUT —
`stopDashboard`'s bare `terminate()`, no escalation, exactly the defect round-5
P48 fixed in `HermesProxyService.stop()`. Now SIGTERM first, then
`waitUntilExit(timeout: dashboardStopCeiling)` (3 s, matched to
`HermesProxyService.stopCeiling`) on `Thread.detachNewThread`, OUTSIDE the Task —
`ProcessAsyncWaitP43cTests` caught the first arrangement, which had nested it in
a `Task.detached`, i.e. back on the cooperative pool.

Tests: `WebDashboardGateTests` (app, 4). The flag at 0.6/0.8/0.9/0.9.1/0.21.1 and
undetected; the view's guard window; the stop path's escalation. 3 issues against
the pre-fix tree, 0 now.

**MED item 1 — seven `.unconfirmed` two-way arms — DONE.**
- `74567c5b` fix(release): seven `.unconfirmed` arms stop asserting a refusal

Each consumer gains a static three-branch summary in the
`HermesMemoryResetVerdict.failureSummary` shape (unconfirmed gated on the
confidence ALONE):
- `MCPServersViewModel.removeFailureSummary` (`mcp remove`)
- `ToolsViewModel.toggleFailureSummary` (`tools enable|disable`)
- `SkillsViewModel.installFailureSummary` / `.uninstallFailureSummary`
- `ProjectSkillsViewModel.trustFailureSummary` (`skills trust|untrust`)
- `MessagingGatewayViewModel.pairingFailureSummary` (`pairing approve|revoke`)

The uninstall arm also stops quoting `(exit 0)` — the verdict reaches
`.unconfirmed` only at exit 0, so it was a number the verdict had declared
meaningless printed beside a claim of failure.

Pairing is the sharpest case: both verbs run `fallbackDetail: false`, so `detail`
is nil by design and the two-way `if` asserted a bare "Approve failed" /
"Revoke failed" on a run that printed nothing either way. Neutral wording matches
the sibling verbs: "hermes pairing approve printed no result. Check the host."

No new catalogue rows: all seven reuse the existing six-locale interpolated key
`%@ printed no result. Check the host.` that `WebhooksViewModel` already drives
with a verb. None of these five view models has an `OutcomeMessage` channel
(they carry plain `String?` banners), so there was no `.unconfirmed` seal to add.
The non-neutral arms keep the exact strings they had (they were never localized;
localizing them was out of scope).

Tests: `UnconfirmedVerdictArmTests` (app, 6 executed) and
`UnconfirmedSkillsVerdictArmTests` (ScarfCore, 3 executed) — split so each suite
sits where its code lives. Every case drives a NON-EMPTY unconfirmed output; an
empty one passes against the old two-way code and proves nothing. Both go red
when the unconfirmed arms are neutralized (17 issues ScarfCore, 9+ app).

The LOW items (curator `--`, memory-reset alert seal, `migrateXAISummary`, the
134-site count) are untouched and still open.

