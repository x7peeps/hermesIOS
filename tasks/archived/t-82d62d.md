---
id: t-82d62d
title: gh#102 streaming-CPU: `LocalSQLiteBackend.refresh()` no longer close+reopens the SQLite handle on every Dashboard `.onChange(fileWatcher.lastChangeDate)` tick — read-only sees Hermes' WAL writes transparently, so the close+reopen on a 285 MB state.db + 114 MB uncheckpointed WAL was pure cost. `forceFresh: true` remains the schema-migration escape hatch; added `deinit` so the fd still releases on ViewModel teardown. `DashboardViewModel.loadImpl()`'s explicit `dataService.close()` dropped in lockstep. Reporter's ScarfMon dump confirmed `mac.fileWatcher.localFire` storm + Dashboard-only manifestation + empty-DB bisect; the fix targets the consumer side (cheap reload) rather than throttling the watcher input.
status: archived
source: gh#102
---

## Description



## Plan



## Artifacts



