---
id: t-a397264c
title: Audit P50: cron/kanban residue — pre-v0.21 error hint, fleet pre_run_script note, dead KanbanWatchFilter
status: archived
added: 2026-09-11
---

## Description

Round-5 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round5.md, cron/kanban section). Needs round-5 product decisions 11–13. Relates t-848d3adc, t-3bcd1d7f.

- MED · PRE · `errorNeedsNewerHermesHint` (`CronRecoveryOffer.swift:74-79`, emitted `HermesCronJob.swift:752-755`, rendered `CronView.swift:717-722`, `BotRoutinesView.swift:186`, iOS `resumeRefusalMessage`) says "edit the schedule to re-arm it" on hosts in [0.20.6, 0.21.0) = v2026.8.27, where `cron edit --schedule` on a recurring `error` job writes `next_run_at` (`cron/jobs.py:2310,2322,2345`) and the terminal guard raises (`:2367-2375`, `:638-640`). The Duplicate button is already rendered for this arm. Decision 11.
- MED · PRE · Fleet-copied agent job silently loses `pre_run_script` (`FleetApplyPlan.swift:412-427`, partition `:283-298`); Hermes injects the script's stdout into the prompt every run (`subcommands/cron.py:41-46`). Decision 12: surface as a downgrade note at the `copyableCronJobs` seam (P42 shape) or skip.
- LOW · PRE · `KanbanInspectorPane.swift:257` completion-contract `.help` key — FIXED in P46 (verify); the other 18 unlocalized `.help(...)` literals across AdvancedTab/DisplayTab/AgentTab etc. (P45 report) — file under t-3bcd1d7f or fix here.
- LOW · PRE · `KanbanWatchFilter` (`KanbanFilters.swift:78-79,:99-115`) is dead and its doc asserts `kanban watch --json` which does not exist (`kanban_parser.py:359-365`). Delete or correct.
- LOW · PRE · Cron detail pane's primary "Run now" (`CronView.swift:667-672`) is not `.disabled(refusesTerminalJobLocally)` like `:489-494` and `BotRoutinesView.swift:201-203`.
- LOW · PRE · iOS `CronEditorView.isValid` blocks Save on any `once` record whose time is spent, edits included. Decision 13.

## Plan



## Artifacts

Commits `55e464f8` (decisions 11 + 13 + the LOWs) and `39b888d1` (decision 12) on `fix/whole-surface-audit-r5`.

**Per finding**

- MED · `errorNeedsNewerHermesHint` — FIXED (`55e464f8`). Now "duplicate it to schedule a new one", six locales. Walked at `v2026.8.27`: `update_job` writes `next_run_at` for a non-paused record (`cron/jobs.py:2310`, `:2322`, `:2345`), the guard below raises on the bare `is_terminal_job` (`:638-640`, `:2367-2375`); `create_job` (`:1915`) has no terminal guard. Duplicate confirmed rendered on all four surfaces that can show this hint.
- MED · fleet `pre_run_script` — FIXED (`39b888d1`) as a downgrade note at both the `copyableCronJobs` preview seam and the executor's success arm. `cron create` HAS `--script` (there is no `--pre-run-script` spelling) and validates nothing at create time (`hermes_cli/subcommands/cron.py:41-46`, `hermes_cli/cron.py:540`, `:453-465` @ `v2026.9.7`) — which is why forwarding is wrong, not right: the path names the source host's filesystem. Script-file replication stays `t-848d3adc`.
- LOW · `KanbanInspectorPane.swift:257` — VERIFIED already fixed in P46 (key present in all six locales). The other 18 (a fresh scan found 28) unlocalized `.help(…)` literals — FILED under `t-3bcd1d7f` with file:line; not a mechanical pass (20 files, ~28 keys, a third with interpolation/inflection).
- LOW · `KanbanWatchFilter` — DELETED (`55e464f8`). No non-test reader; `kanban watch` @ `v2026.9.7` takes `--assignee/--tenant/--kinds/--interval` only (`hermes_cli/kanban_parser.py:359-365`), no `--json`.
- LOW · cron detail-pane "Run now" — FIXED (`55e464f8`), `.disabled(refusesTerminalJobLocally)` like the row menu and `BotRoutinesView`.
- LOW · iOS `CronEditorView.isValid` — FIXED (`55e464f8`) per decision 13: refuses a spent one-shot unless the time is the record's own unedited stored value. Hermes accepts a prompt-only `cron edit` of a one-shot that RAN (`_complete_job_record` leaves it `enabled=False`, so `_reject_terminal_activation` and `_fill_missing_next_run` both pass); the never-run ghost shape would still be refused, and the P42b gate is what stops a new one.

**Tests.** ScarfCore 3057/3057; Mac serial 46/46 over nine suites incl. `HermesP38SourceSweepTests`; `scarf mobile` builds. Nine new tests in four suites, each watched failing pre-fix.

**Memory.** `scarf/decisions/hermes-v0-21-1-compatibility-decisions` — new "P50" section. `scarf/architecture/hermes-cron-recovery-is-three-doors-not-one-and-scarf-must` — corrected for the new hint copy.

