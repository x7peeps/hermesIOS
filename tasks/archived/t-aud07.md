---
id: t-aud07
title: **[t-aud07]** Loading overlays — ADDED. `.loadingOverlay(isLoading, label:, isEmpty:)` on InsightsView (VM already had `isLoading`), SessionsView, and LogsView; added `isLoading` to `SessionsViewModel` (via `defer` in `load()`) and `LogsViewModel` (set in `load()`/`switchLogFile`, deliberately NOT in the 2s tail poll). Verified: macOS BUILD SUCCEEDED, no new warnings.
status: archived
---

## Description



## Plan



## Artifacts



