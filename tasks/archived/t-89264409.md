---
id: t-89264409
title: Audit P49: capability floors — hasACPSetSessionModel, hasYOLOSlashCommand, hasContextCompressionCount
status: archived
added: 2026-09-11
---

## Description

Round-5 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round5.md, capabilities section). Needs round-5 product decisions 9–10. Relates t-1febd6fa.

- MED · PRE · `hasACPSetSessionModel` (`HermesCapabilities.swift:347-353`) floored at v0.13 with doc "Pre-v0.13 hosts ignore the call"; `set_session_model` is present and functional at every adapter tag from v2026.3.17 (`server.py:466-479`), incl. the v0.6.0 floor (`:482` @ v2026.3.30; `:929-953` @ v2026.9.7). Consumers hide the model chip and project model binding on 0.6.0–0.12 hosts (`SessionInfoBar.swift:252`, `ProjectChatSettingsSheet.swift:64,209,265`, `SidebarView.swift:75`, `SidebarProjectsWell.swift:514`, `ChatViewModel.swift:1603`, iOS `ProjectDetailView.swift:79`). Decision 9: retire (unfloored, like reset/context/version) or pin at the first adapter tag.
- LOW · PRE · `hasYOLOSlashCommand` (`:398`) floored v0.14; `CommandDef("yolo")` first at v2026.4.3 (0.7.0), absent v2026.3.30. No consumer. Decision 10.
- LOW · PRE · `hasContextCompressionCount` (`:305-307`) gates a wire field absent under `acp_adapter/` at every tag (`server.py:917-925` @ v2026.9.7); `ACPClient.swift:534-542` TODO(WS-8-Q1); `SessionInfoBar.swift:380`. Fold into t-1febd6fa; correct the doc.
- Ten older flags re-walked at random in round 5: eight correct (hasACPImagePrompts, hasMCPSSETransport, hasNewWithSessionName, hasSubgoal, hasSessionsOptimize, hasConfigUnset, hasAuxiliaryReasoningEffort, hasACPCompressSpelling). Consider a full re-walk of the remaining MARK groups as a follow-on.

## Plan



## Artifacts

Commit `c718237d` on `fix/whole-surface-audit-r5` (14 files).

**Decision 9 — `hasACPSetSessionModel` retired.** Tag walk (every blob opened): `set_session_model` is `acp_adapter/server.py:466` @ **v2026.3.17** (0.3.0, earliest tag with an `acp_adapter/`), `:482` @ **v2026.3.30** (= **0.6.0**, Scarf's supported floor, with a working body) and `:929` @ v2026.9.7. It IS present at the v0.6.0 floor, so the flag was retired outright rather than pinned at the first tag. Consumers ungated across all three targets: `ChatViewModel.applyProjectModelPreset`, `SessionInfoBar` (chip + 2 doc comments), `ChatModelBadge` doc, `ProjectChatSettingsSheet` (section, `load()`, `save()`, type doc), `SidebarView.sections` (`.models` unconditional), `SidebarProjectsWell` (Chat Settings menu item — also dropped its `hasSessionEditAutoApproval` half, since the sheet always has a working section now; the v0.15 gate survives inside the sheet), iOS `ProjectDetailView` badge, `ACPClient.setSessionModel` doc. The v0.13 degradation assertions in `HermesCapabilitiesTests` are gone; a source sweep across all six roots asserts no code reads the flag.

**Decision 10 — `hasYOLOSlashCommand` deleted.** Zero consumers confirmed by grep across the Mac app, ScarfCore and iOS. Floor recorded in the retirement comment: `CommandDef("yolo")` absent at v2026.3.30, `hermes_cli/commands.py:96` @ v2026.4.3 (0.7.0), `:181` @ v2026.9.7; `yolo` absent from `acp_adapter/server.py` at every tag. Three test assertions removed; `M9SlashCommandTests` now pins `hasSessionsSlashCommand` instead; two sibling docs no longer link the deleted symbol.

**`hasContextCompressionCount` — walked, kept, re-documented.** ACP `Usage` is five keys at every tag: `acp_adapter/server.py:325-336` @ v2026.3.30, `:1050-1059` @ v2026.5.7 (the claimed floor), `:917-924` @ v2026.9.7 — no compression count anywhere. Plumbing NOT removed (`SessionInfoBar.swift:380` still reads it, `RichChatViewModel.acpCompressionCount` still feeds it); `TODO(WS-8-Q1)` in `ACPClient.swift` replaced by the cited fact, and the flag's + view model's doc comments corrected. Folded into `t-1febd6fa`.

**Follow-on light re-walk — 20 flags, 13 MARK groups, 18 correct.** Two wrong, both consumer-free, both v0.16: `hasInsightsCommand` deleted (`hermes insights` is `hermes_cli/main.py:4634` at v2026.3.30 = 0.6.0, below the supported minimum) and `hasDashboardCommand` re-floored 0.16 → 0.9 (`main.py:4458` @ v2026.4.13 = 0.9.0; absent at v2026.4.8 = 0.8.0). Full confirmation list in the decisions note's P49 section. Remaining ~85 flags filed as **`t-54ec6eb3`**.

**Tests.** 17 new tests in four suites across two `HermesP49Tests.swift` files (ScarfCore 11, `scarfTests` 6). ScarfCore `swift test`: **3049 in 236 suites, 0 failures** (3046/235 before). Mac serial `-only-testing:scarfTests`, three batches with non-zero counts: 23/23, 32/32, 105/105. `scarf mobile` builds clean. No UI tests run, app never launched.

**Memory.** P49 section appended to `decisions/hermes-v0-21-1-compatibility-decisions`; `architecture/hermes-capability-gating-pattern` and `architecture/the-acp-adapter-s-slash-roster-is-nine-names-and-has-been` corrected for both retired flags.

