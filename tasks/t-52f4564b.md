---
id: t-52f4564b
title: iOS: route confirmModelPreflight through LocalModelConfigPlan (live stale-key bug, same class as Mac)
status: todo
added: 2026-07-14
---

## Description

Drift-audit MED-HIGH finding (2026-07-14) — a LIVE iOS bug of the exact class this session fixed on Mac. iOS ChatView.confirmModelPreflight (scarf/Scarf iOS/Chat/ChatView.swift:1346-1419, writes at :1363/:1373) writes model.provider + model.default RAW, bypassing LocalModelConfigPlan's clear-on-switch scrub of model.base_url/api_key/api_mode/context_length. So switching away from a local provider on iOS leaves stale base_url → chat silently routes to the wrong/dead endpoint (GH #27132 class). Mac routes model saves through LocalModelConfigPlan (SettingsViewModel:127-146, ChatViewModel:596/628) and is safe; iOS never got the fix. FIX: route the iOS preflight write through LocalModelConfigPlan.operations(...) exactly as Mac does. Zero iOS test coverage on this path today — add one. Ships via TestFlight (separate track from the Mac release), so not a Mac-release blocker, but should ride the next ScarfGo build. Risk: LOW-MED.

## Plan



## Artifacts



