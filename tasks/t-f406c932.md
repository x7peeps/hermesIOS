---
id: t-f406c932
title: Audit P51: settings/forms residue — Unicode-space trims, Discord/Mattermost keys, remote pairing, override dedupe
status: done
added: 2026-09-11
---

## Description

Round-5 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round5.md, settings/YAML section). Needs round-5 product decisions 14–17. Relates t-d02dd23e, t-f3d7bdd2, t-8f55df7d.

- MED · PRE · Scarf's YAML reader trims with Foundation `.whitespaces` (Unicode Zs), PyYAML keeps U+00A0 etc. (`HermesYAML.swift:159,:207,:250,:258-259`, `normalizedScalar:415`); `yaml.safe_dump` emits such values bare, so Hermes-written values render short and the next write persists the trimmed form. Decision 14: a space+tab `yamlWhitespace` set for parser trims.
- MED · PRE · Auxiliary tab does an SSH file read on the main actor in `onAppear` (`AuxiliaryTab.swift:221,:225` → `NousSubscriptionService.loadState()` `:84-88`); `ModelPickerSheet.swift:193-195` has the detached shape. C10.
- MED · PRE · `discord.allow_any_attachment` is dead at the tag (`config_defaults.py:1448` only; docs say no-op) and ungated (`DiscordSetupViewModel.swift:95`, view `:40`, reader `HermesConfig+YAML.swift:450`). Retire or gate.
- MED · PRE · `DiscordSetupViewModel.save()` writes `discord.history_backfill` unconditionally (`:42,:94`) while the view gates the row on `hasDiscordHistoryBackfill` (v0.14); VM carries no capabilities. Use Telegram's `load(capabilities:)` shape (`TelegramSetupViewModel.swift:112-118`).
- MED · PRE · Mattermost require-mention reads config.yaml and writes `.env` (`MattermostSetupViewModel.swift:52,:64`); adapter prefers config (`mattermost/adapter.py:491-494,:504,:704,:713-729`). Pick one side.
- MED · PRE · Signal/WhatsApp pairing spawn locally regardless of context (`SignalSetupViewModel.swift:121,:139`; `WhatsAppSetupViewModel.swift:96-99` execs the REMOTE `hermesBinary` path locally; `EmbeddedSetupTerminal.swift:53-83`). Disable on `isRemote` with a sentence naming the host (`runBackup` posture, `SettingsViewModel.swift:1408-1412`). Decision 15.
- MED · PRE · Reasoning-override editor's case-insensitive dedupe (`AgentTab.swift:383-392`) deletes a live override; Hermes's lookup is case-sensitive (`hermes_constants.py:892-940`). Decision 16.
- LOW · PRE · No platform-setup form applies the `YAMLScalar` control-char refusals (`WhatsAppSetupViewModel.swift:82`, `DiscordSetupViewModel.swift:91`, `NtfySetupViewModel.swift:83-85`).
- LOW · PRE · `NtfySetupViewModel.load` drops the proven `.env` half when config is nil (`:51-52`).
- LOW · PRE · Two main-actor `enrichedEnvironment()` calls can block up to 8 s (`EmbeddedSetupTerminal.swift:73`, `NousAuthFlow.swift:94`).
- LOW · PRE · iOS shows a reasoning-effort value with no "host ignores it" affordance (`Scarf iOS/Settings/SettingsView.swift:237-241`); `unsupportedLevelNotice` is in ScarfCore. Decision 17.

## Plan



## Artifacts

Commits on `fix/whole-surface-audit-r5`:
- `76206b4d` — decision 14, the reader's trim (ScarfCore).
- `c838c562` — decisions 15, 16, 17 (remote pairing, exact override dedupe, the iOS notice).
- `c93c2287` — the Discord/Mattermost/Ntfy config keys, the setup-form control-character refusal, the C10 residue.

Per-finding disposition (all eleven fixed; none deferred):
- MED Unicode-`Zs` trims → `HermesYAML.yamlWhitespace` (space+tab) over all seventeen reader trims. `normalizedScalar`/`normalizedLevel` deliberately KEEP the wide trim — they mirror Python `.strip()`, which does remove U+00A0. PyYAML 6.0.3 oracle pinned in the suite.
- MED Auxiliary tab SSH read on the main actor → `.task` + detached hop, shared with the sign-in sheet's idle twin; the same twin in `ModelPickerSheet`'s sheet completion fixed too.
- MED `discord.allow_any_attachment` → GATED, not retired. It is a WINDOW [v0.15.0, v0.18.0): the adapter stopped CALLING the getter at v2026.7.1 while the getter lingered to v2026.8.31. Retiring would have regressed v0.15–v0.17 hosts (C1). New `hasDiscordAllowAnyAttachment`.
- MED Discord `history_backfill` written below its floor → Telegram's `load(capabilities:)` shape; both windowed keys gated in `save`.
- MED Mattermost require-mention split → config.yaml both directions (the adapter prefers `config.extra`); `.env` kept as the read fallback for an ABSENT key via a new `MattermostSettings.requireMentionIsSet`. Registered in `AllConfigWritersParityTests`, which caught it on the first run.
- MED Signal/WhatsApp pairing on a remote context → disabled with a sentence naming the host (`runBackup` posture), guarded in the view models too; six locales.
- MED case-insensitive override dedupe → exact. Hermes's lookup is `variant in overrides`, case-sensitive. The optional case-collision warning was NOT taken (a new string in six locales, not one line).
- LOW `YAMLScalar` control-char refusals → placed at `commitSave`, covering all fifteen forms rather than the three named.
- LOW Ntfy drops the `.env` half → guard moved below the `.env` assignments.
- LOW two main-actor `enrichedEnvironment()` calls → both detached, with held tasks so a Stop/cancel stops a start that has not spawned.
- LOW iOS effort affordance → `unsupportedLevelNotice` beside the read-only value, same ScarfCore function and capability question as the Mac.

Hermes citations re-opened at `v2026.9.7` unless noted: `plugins/platforms/discord/adapter.py` (also at v2026.4.13/4.30/5.7/5.16/5.28/5.29/6.5/6.19/7.1/7.7/7.20/7.30/8.3/8.13/8.16/8.18/8.19/8.27/8.31 for the call-site walk), `hermes_cli/config_defaults.py:1448`, `website/docs/user-guide/messaging/discord.md:703`, `hermes_constants.py:875-941` (`parse_reasoning_effort:884`, `_canonical_model_variants:892-926`, `resolve_per_model_reasoning_effort:929-941`), `gateway/config.py:25-32`, `tools/approval_context.py:203-210`, `plugins/platforms/mattermost/adapter.py:491-494,:504,:700-729`.

PyYAML 6.0.3 oracle: U+00A0/U+3000/U+2009/U+1680/U+202F/U+205F/U+2003 all KEPT (leading and trailing, in values, keys, list items, flow list and flow map); a plain trailing space IS dropped; a tab around the value indicator is a `ScannerError` in both positions; NEL and U+2028 are treated as line breaks and stripped (neither is in `.whitespaces`, so no regression).

Tests: ScarfCore 3091 in 246 suites, 0 failures, twice, no flakes. Mac serial 122 in 17 suites, 0 failures (includes `HermesP38SourceSweepTests`, `LocalizationCatalogTests`, `MainActorSpawnDisciplineP22Tests`, `SpawnDisciplineP43Tests`, `AllConfigWritersParityTests` and every suite of a touched file). `scarf` and `scarf mobile` both build. 45 new tests in 10 suites across two `HermesP51Tests.swift` files; every behavioural fix watched failing against its reverted form. `Localizable.xcstrings` +82 lines, 0 deletions.

Fresh-eyes findings on P51's own diff: `NousAuthFlow.handleTermination:219` had a THIRD instance of the main-actor `loadState()` shape (fixed); the control-character helper was order-dependent over a `Dictionary` (sort moved into the helper); the oracle test's own assertion compared against the literal character while `json.dumps` escapes non-ASCII (corrected); commit 2 initially shipped without the helper it calls (amended).

Tasks filed: `t-295ef4d2` (GatewayConfigWriter + ProfileRoutesYAML still trim Unicode `Zs`), `t-f30054a8` (five more main-actor env/spawn sites; `SpotifyAuthFlow:133` is the literal twin of the `NousAuthFlow.start()` fix).

Memory: `scarf/decisions/hermes-v0-21-1-compatibility-decisions` (P51 section appended), `scarf/architecture/a-yaml-reader-is-opted-in-per-key-and-only-for-what-scarf` (the trim rule + the parser-vs-`.strip()` invariant), `scarf/decisions/setup-forms-write-the-resolved-default-settings-treats` (the row bound, the ceiling, the adapter-preferred side).

