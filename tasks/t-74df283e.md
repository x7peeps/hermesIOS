---
id: t-74df283e
title: Gate the project-skills trust bar on the `skills trust` floor
status: todo
added: 2026-09-11
---

## Description

Found during P39 (round-4 remediation), out of that phase's scope.

`hermes skills trust|untrust` ARRIVES at **v2026.8.16.2** — `hermes_cli/main.py:12051-12052` dispatches `_cmd_skills_trust` (`:12059`) from that tag; at v2026.8.16 and earlier the action falls through to `skills_command(args)`, where `trust`/`untrust` are unknown. Scarf's trust bar is UNGATED: `ProjectCockpitView.swift:697,701` offer "Trust Repo"/"Untrust" on any host, and `ProjectSkillsViewModel.setTrusted` (`scarf/Packages/ScarfCore/Sources/ScarfCore/ViewModels/ProjectSkillsViewModel.swift:47`) shells `ProjectSkillsScanner.trustArgs` unconditionally (charter C5).

P39 made this safe rather than silent — the new `HermesSkillsTrust.judge` reports a failure on a host that prints none of the four success lines — but the right shape is a `HermesCapabilities` flag (`hasProjectSkillsTrust`, floor v2026.8.16.2 / v0.20.x) that hides or inerts the buttons, mirroring how the "Host default" approvals row is gated on `hasConfigUnset`.

Also in the same helper: `trustArgs` (`ProjectSkillsScanner.swift:141-143`) emits `["skills", trusted ? "trust" : "untrust", root]` with no `--` before the positional path. Verify against `hermes_cli/subcommands/skills.py` at the tag and add the separator if the parser accepts it, the way P39 did for `config set`/`config unset`.

## Plan



## Artifacts



