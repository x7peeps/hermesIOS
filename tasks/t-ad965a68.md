---
id: t-ad965a68
title: Audit P65: cron/kanban residue r7 — iOS model snapshots, duplicate strips, Review entrance, unconfirmed dispatch twins, cron split
status: todo
added: 2026-09-13
priority: high
---

## Description

Round-7 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round7.md, cron/kanban section). All PRE (F7 confirm-before-plan and F8 unreachable catalogue row were NEW and fixed in P60). Relates t-3bcd1d7f, t-b74c65a4. Needs round-7 product decisions 9–10.

- MED · iOS cron form edits `model` but forwards `extra` with stale `provider_snapshot`/`model_snapshot` (`CronListView.swift:398,589`); `update_job` recomputes both on an inference-axis change (`cron/jobs.py:1951-1963` @ v2026.9.7); the drift guard (`cron/scheduler.py:1569-1591`, `hermes_cli/config.py:3051-3056`) then skips the job with a warning no Scarf surface shows, or never engages. Lesson 14 again: which machine fields do the edited fields compute. Fix: drop both snapshots whenever the pin changes (fail-open, Hermes's documented no-snapshot back-compat).
- MED · `duplicatedAsNewJob` (`HermesCronJob.swift:551-556`) strips seven keys; `create_job` (`:1760-1797`) also stamps `created_at`, `failure_streak`, both snapshots, and no claim. A duplicate of a mid-run `once` job inherits `run_claim` → `_evaluate_due_job` false (`:2919-2923`), `_retire_expired_oneshot` (`:2862`) neither fires nor retires → permanently stuck card. iOS-only. Add `run_claim`, `fire_claim`, `created_at`, `failure_streak`, `provider_snapshot`, `model_snapshot` to the strip list.
- MED · Review ENTRANCE refusal is false: `KanbanService.swift:639-644` says "Review is managed by the dispatcher", but `kanban request-review` (`kanban_parser.py:312-320`, `request_review` `kanban_db.py:2992-3060`, running/ready → review, `--force` past a live claim) exists from v2026.8.13 — `hasKanbanReviewExits` already covers it. Decision 9: add running/ready → review behind the flag (confirm for `--force`) or reword the refusal.
- MED · board-wide dispatch runs unconfirmed from `reassignTask` (`KanbanBoardViewModel.swift:464`) and `createTask` (`:500`) — idle twins of decision 9's confirm. Decision 10: confirm there too, or `--max`/no dispatch on those gestures.
- LOW · `cronExpressionHasParseableShape` splits on space/tab/newline; Python `str.split()` splits on every `isspace()` char (`\xa0`, `\r`, `\x0b`, `\x0c`) — the form refuses a schedule the host accepts (P51's Unicode-space class).
- LOW · `SessionsViewModel.beginExport` comment `:784-788` says md/qmd/html hand the path to the CLI; `availableExportFormats:652-660` hides them on remote. C6 honoured; fix the comment.
- LOW · `FleetModelPinNoteP56Tests`: two of three tests are exact-spelling source scans (`source.contains("if job.hasModelPin { modelPinDowngrades += 1 }")`); replace with behavioural assertions on the counter.
- LOW · `KanbanDispatchConfirmP56Tests.confirmingProceedsWithTheMove` leaves a real CLI task in flight against a scratch home.

## Plan



## Artifacts



