---
id: t-3f074bf4
title: Upstream P2: rebuild Hermes PR #45958 (per-session ACP toolsets) on current main
status: done
added: 2026-09-21
priority: high
---

## Description

Our open Hermes PR NousResearch/hermes-agent#45958 (issue #45955) is conflicting and has two review defects. Re-implement it on top of current upstream main in the local clone /Users/awizemann/Developer/hermes-agent-fork (origin = awizemann fork, upstream = NousResearch). NO PUSH — Alan approves pushes.

Strategy decided by the orchestrator (see plan): ours is the PER-SESSION layer and is complementary to the config-based PRs (#106834, #111575, #37842, #46832, #82546, #70326), which set the process/profile-wide base. Keep our diff to one narrow seam so it composes with whichever of those lands.

## Plan

1. New local branch from upstream/main (keep the name feat/acp-toolset-scoping; old head is preserved at origin/feat/acp-toolset-scoping). Author as Alan Wizemann <319078+awizemann@users.noreply.github.com> so the AUTHOR_MAP commit is not needed. No Co-Authored-By trailers. Conventional commit `feat(acp): ...`.
2. Per-session override on session/new via the ACP-spec extension map `_meta.toolsets` (verify how the acp python library hands `_meta` to new_session; support a top-level field only if the library dictates it).
3. Build on main's existing `_make_agent(enabled_toolsets=, disabled_toolsets=)` plumbing. The /model rebuild already carries the live agent's toolsets, so prove persistence with a regression test rather than new state.
4. A scoped session must NEVER silently widen: cover every sibling path — model switch, restore from DB after process restart (load/resume), fork. Prefer persisting the selection in the existing model_config JSON meta (no schema change); justify the choice.
5. `--toolsets` process default: add to hermes_cli/subcommands/acp.py parser AND acp_adapter/entry.py, forward in cmd_acp. Precedence: per-session > process flag > existing base.
6. Validate toolset names the same way `hermes chat --toolsets` does; unknown names must give a truthful error, not a silent empty toolset.
7. Tests: precedence, _meta extraction, model-switch persistence, restore persistence, fork inheritance, CLI parsing end to end, unknown-name handling. Run the project's test runner for the ACP + CLI slices and ruff.
8. Update website/docs/user-guide/features/acp.md.
9. Self-audit with fresh eyes, then report.

## Artifacts



