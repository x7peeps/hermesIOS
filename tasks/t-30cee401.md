---
id: t-30cee401
title: Voice F6: fresh-eyes fixes on main after the voice merge (ScarfCore)
status: done
added: 2026-09-19
priority: high
---

## Description

From the 2026-09-19 fresh-eyes review of `git diff ad0ae467..main` (all four test gates were green first: ScarfCore 3507, ScarfIOS 92, Mac scarfTests 1477 serial, iOS build).
Fix now, each with a test that fails without it:
1. HIGH HermesTTSCache.evictOverflow (HermesTTSCache.swift ~172): the manifest branch overwrites chunk entries already accumulated under the same stem when a chunk file is enumerated before its .json, so those chunks are never counted or deleted and the 256 MB cap does not hold. Merge instead of replace. No HermesTTSCache test file exists; add one.
2. MEDIUM GPTLiveEngine ~362: applyAnswer failures put the raw WKJavaScriptExceptionMessage into .audioConnectFailed(detail:), which is logged at privacy .public; WebKit SDP parse errors can quote the SDP line (ice-pwd, fingerprint). Use a fixed detail on that path and extend VoiceLiveHostExchange.redact to strip a=ice-ufrag/ice-pwd/fingerprint/crypto lines.
3. LOW VoiceLiveHostExchange.createSession ~134: a cancelled script comes back as .connectFailure("Script cancelled") → TransportError, so `catch is CancellationError` never matches and the caller sees a spurious transport error. Check Task.isCancelled before mapping.
4. LOW SSHScriptRunner.ScriptFeeder.pump ~78-97: a non-EPIPE write error breaks to finish(), which closes the pipe and lets the shell run a truncated script. Record the errno and report it as a connect failure.
5. MEDIUM ProjectConfigKeychain.setIfAbsent ~213: the real-Keychain branch is a plain set (SecItemUpdate first), not compare-and-set. Make it add-only (SecItemAdd, errSecDuplicateItem = lost the race, then re-get).
Deferred to their own tasks: late-answer billing leak during .connecting (engine), consent enforcement inside GPTLiveEngine.start, XCTest-seam failsafe, ShellTestRunner timeout cleanup.

## Plan



## Artifacts



