---
id: t-30667749
title: Voice: fix Mac convention-test failures on feat/voice
status: done
added: 2026-09-18
priority: high
---

## Description

Found by P5a (t-8c80d256) running the full macOS app test suite (`scarf` scheme tests) on the voice branch. The failures come from P1/P2/P4 code:
- The config-writer convention checks flag `setSetting(VoiceChatMode.configKey, …)` as a non-literal key, and `VoiceLiveReadiness.swift` isn't registered as a config writer.
- The source sweep flags array indexing after a count check in `VoiceLiveTurnNoteContractTests.swift` (25, 39, 74) and `PushToTalkControllerTests.swift:228`, and a 30 s sleep in `HermesSpeechServiceTests.swift:508`.
- `SpawnDisciplineP43` failed a leak count, possibly environmental: compare against main.
- The whole run stalled for about 25 min.
Fix them the way the conventions intend (don't weaken the checks). Run the full Mac suite plus the ScarfCore and ScarfIOS package tests, and prove it's green, or show that each remaining failure also exists on main.

## Plan



## Artifacts

Branch `feat/voice-fix` (worktree scratchpad/voice-fix), 2 commits on top of c81f54df, not pushed:
- 97a8aa10 fix(voice): write voice.voice_chat_mode with a literal key. Both writers now spell the key literally (Mac `setSetting`, iOS `saveValue`). Removed the unused `VoiceChatMode.configKey` and `configSetArgv()`: no production code called them, and they made VoiceLiveReadiness.swift look like an unregistered writer. The C5 notes moved onto `VoiceChatMode`. The gate stays as strict as before (SettingsViewModel is back to 9 declared non-literal sites).
- faea0c43 test(voice): `try #require` on the count in VoiceLiveTurnNoteContractTests / PushToTalkControllerTests. HermesSpeechServiceTests' 30 s sleep became `parkUntilCancelled()`, an AsyncStream suspended until the task is cancelled, with `.timeLimit(.minutes(1))`. No allowlist entries were added.

Results (branch):
- Mac scarfTests, serial (`-only-testing:scarfTests -parallel-testing-enabled NO`): 1461/1461 passed (1457 Swift Testing + 4 XCTest), 491 s test time. The config-writer, source-sweep and SpawnDisciplineP43 checks all passed.
- ScarfCore `swift test`: 3466 tests / 295 suites passed, plus 30 XCTest.
- ScarfIOS `swift test`: 88 tests / 14 suites passed.
- Builds: macOS `scarf` Debug and iOS `scarf mobile` (generic iOS Simulator) both BUILD SUCCEEDED.

Remaining failures only in the default parallel / Full-plan mode, which also fails on main:
- Parallel scarfTests (19 keychain-touching suites skipped on both sides): main failed 53/57/58/50 over 4 runs, the branch 57/61/55 over 3. `SpawnDisciplineP43Tests.onlyReadEndsLeak` failed on main in 2 of 4 runs, so it is a parallel-load flake, not ours. Failures seen only on the branch: VoiceLiveMacTests ×6 (new tests; same 5 s acpStatus==ready wait as ChatViewModelStartLifecycleTests, which fails on main in every parallel run), GwF4 successAutoClears ×1, and AuditP21 pluginsUpdateSuccessLines ×1 (sibling of HermesCLIExitCodeTruthTests pluginsEnableSuccessLines, which failed on main).
- scarfUITests with CODE_SIGNING_ALLOWED=NO: the runner never connects on main or the branch.
- The ~25 min stall: a login-keychain SecurityAgent prompt. Tests read the real `com.scarf.miniapp-grants` key through MiniAppGrantSigner, and a freshly built unsigned host prompts. Reproduced on main by sampling the stuck host. Follow-up chip filed. Memory: scarf/operations/mac-scarftests-run-green-only-serially-and-an-unsigned-test.

