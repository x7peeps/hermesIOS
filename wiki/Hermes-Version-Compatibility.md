---
title: Hermes-Version-Compatibility
type: note
permalink: scarf-wiki/hermes-version-compatibility
updated: 2026-09-13
created: 2026-05-29
---

# Hermes Version Compatibility

Scarf reads Hermes's SQLite database directly and parses CLI output from `hermes status`, `hermes doctor`, `hermes tools`, `hermes sessions`, `hermes gateway`, and `hermes pairing`. It also speaks ACP (`hermes acp`) for chat. Compatibility comes from automatic schema detection plus tolerant parsing.

## Currently targeted

**Scarf targets Hermes v0.21.2 (v2026.9.11)** — target bumped 2026-09-14 on `main`. v0.21.2 is Hermes's "state.db patch release" (986 commits over v0.21.1). Verified at the tag and against a live 0.21.2 host: SCHEMA_VERSION stays 30 with no table removed and every column Scarf probes present (`scripts/check-hermes-tables.py --tag v2026.9.11` exits 0, `lanes=5/5`); hosted rooms moved to `shared-state.db`, which Scarf never reads; the ACP adapter changed four lines of `/model` provider detection; no verb or flag Scarf issues was removed and every output marker Scarf judges on is byte-identical; the Smoke and Live UI plans pass against the installed 0.21.2. One behaviour change answered: `hermes backup` now defaults to `--keep 3` and prunes older `~/hermes-backup-*.zip` files, so Scarf's Backup Now passes `--keep 0` behind `hasBackupKeep` (v0.21.2+). New verbs (`vault`, `auth upgrade`, `plugins browse|validate`) have no Scarf surface yet. Not adopted this cycle: the CLI's new `repeat` re-derivation when a cron edit flips one-shot to recurring, and creation-time model snapshots for unpinned jobs — both on the iOS `jobs.json` form's list (task t-ad965a68).

**Voice feature floors, ahead of the general target.** Two voice surfaces gate on their own Hermes version, independent of the line above. [Hermes Voice playback](Chat#hermes-voice-playback-mac) (Settings → Voice → Playback Engine → Hermes Voice) needs **v0.20.1+**, already covered by the v0.21.2 target. [Live Voice](Chat#live-voice-mac-and-scarfgo) (GPT-Live) needs **v0.21.3** — one patch *ahead* of Scarf's current v0.21.2 target, the first Scarf surface gated on a host newer than what Scarf otherwise verifies against. Hosts on v0.21.2 or older simply don't show the Live Voice button; nothing else changes for them. Source: `tools/voice_live.py`, first present at tag `v2026.9.14` (`pyproject.toml` version `0.21.3`), confirmed absent at `v2026.9.11` (`v0.21.2`). Full design notes: `documents/plans/2026-09-18-live-voice-spike.md`.

Previously: **Scarf targeted Hermes v0.21.1 (v2026.9.7)** — parity landed 2026-09-08 on branch `feat/hermes-v0211-parity` (Phases 0–7); the Scarf version that ships it is **TBD** and is set at release prep. Verified via a source-audited v0.21.0→v0.21.1 delta (~5,995 commits, mostly a `hermes_cli` modularization, so most file diffstats are moves and every finding was verified on semantics at both tags). Verdict: the `messages` DDL Scarf reads is byte-identical (SCHEMA_VERSION 26→30, all FTS-only; `sessions` gained additive `tool_names` / `compression_recovery_deadline`), the ACP wire is byte-clean, and none of the 88 argv Scarf issues changed. Full findings: `documents/hermes-v0.21.1-audit-report.md` and the v0.21.1 row below.

Previously: **Scarf v2.23.0 targeted Hermes v0.21.0 "Pantheon" (v2026.8.31)** — verified via a source-audited v0.20.5→v0.21.0 delta (~2,287 commits, including the intermediate v0.20.6 tag). Eight-surface audit verdict: SCHEMA_VERSION unchanged at 26 but DDL changed via Hermes's ADD-COLUMN auto-migrator (so all detection stays PRAGMA/sqlite_master-based), and the ACP wire byte-clean. The full findings and per-flag floors live in the repo's `documents/hermes-v0.21.0-audit-report.md` and the v0.21 rows below. History of prior targets follows.

Previously: **Scarf v2.20.0 targeted Hermes v0.20.4 "Herald" (v2026.8.18) — verified via a source-audited v0.20.0→v0.20.4 delta (~3,000 commits over four patch tags).** The eight-surface audit found `state.db` effectively additive (SCHEMA_VERSION 25→26; `messages`/FTS/`session_model_usage` byte-identical, `sessions` gained `hidden`/`last_read_at`/`title_source`/`git_metadata_generation` — the first two schema-detected and adopted) and the ACP wire clean. Three would-be breakages were fixed ahead of the host upgrade: the 14 built-in personalities moved out of `config.yaml` into Hermes code (Scarf now carries them in-code too, unioned with user entries, and fixed two pre-existing parse bugs that had the Personalities list broken since inception); v0.20.4's cron firing gate refuses jobs with pause markers, so ScarfGo's enable toggle now drives `hermes cron pause|resume` with full resume semantics; and `skills update` silently skips locally-edited skills, which Scarf now reports (with per-skill, explicitly-confirmed `--force`). Adopted v0.20.4 surfaces, all patch-gated (`isV0204OrLater`, 8 flags) or column-probed: unread session indicators (Hermes `session_unread` watermark semantics incl. the messages-max term), hidden-session filtering, reset-child session listing, curator ledger / purge / per-entry rollback, repo-local project skills + trust, the 20-server optional-MCP catalog picker, MCP `identity_header`/`strict_redirect_headers`/stdio-`cwd` (validation matching Hermes's), the new Actual Computer provider + `openai-codex` label rename, top-level-precedent `multiplex_profile_allowlist` parsing (fails closed like upstream), and ~10 new config keys (wake-word capture, STT trimming/idle-unload, `auxiliary.background_review.enabled` kill switch, concurrency + timeout knobs). Pre-0.20.4 hosts render byte-identically — proven by degradation tests.

**Scarf v2.18.0–v2.19.2 targeted Hermes v0.20.0 "Herald" (v2026.8.3) — verified via a source-audited v0.18.2→v0.20.0 delta (~5,700 commits spanning v0.19 "Quicksilver" and v0.20 "Herald").** The audit found `state.db` SCHEMA_VERSION moved 19→25 but every column Scarf queries was untouched — all additions are additive (`messages.effect_disposition`/`api_content`/`display_kind`/`display_metadata`, `sessions.pinned`/`last_activity_*`/`profile_name`, new tables `system_prompts`/`session_model_usage`/`async_delegations`/`delivery_obligations`); the ACP method roster and `session_update` discriminators are byte-identical across the delta. Forced changes: **`/compact` renamed `/compress`** (Scarf follows suit on 0.20 hosts, keeps `/compact` on older ones — never both); the `curator status` header text changed generations (Scarf's parser now accepts both); the provider catalog picked up **Vercel AI Gateway** (back, with aggregator handling), **Fireworks AI** and **Google Vertex AI** (overlay-only), a Solar→Upstage alias, and retired-DeepSeek (`deepseek-chat`/`deepseek-reasoner`) → `deepseek-v4-flash` resolution; **Buzz** (Block's Nostr-based messenger) joined the gateway platform roster (user-gated via `allowed_users`, no allowlist-kind mapping); `agent.max_turns`'s display default follows the connected host (60 pre-0.20, 500 at 0.20+, never baked into the parser). New v0.20-gated surfaces: pinned sessions + last-activity descriptions in the chat sidebar, per-model cost breakdown on the Dashboard, richer session export (Markdown/HTML/Quarto/trace + secret redaction, schema-gated to stdout-only formats on remote hosts), `approvals suggest` allowlist proposals in Settings → Security, and cron run history. Scarf v2.19.0 closed the remaining Hermes 0.20 settings backlog (title generation, per-task reasoning effort, smart approval policy, secrets sources, voice STT/TTS tuning, telemetry + SQLite journal knobs, `gateway.profile_routes` routing editor) and fixed a long-standing bug where the browser backend picker wrote `browser.backend` — a key that never existed in any Hermes version; the real key is `browser.cloud_provider`. The same two-cycle audit caught five other pre-existing bugs, all fixed: `skills uninstall`/`update` passed a nonexistent `--yes` flag (Skills UI uninstall/update had never worked); the "runtime metadata footer" toggle wrote `agent.runtime_metadata_footer` instead of the real `display.runtime_footer.enabled`; Google Chat and Microsoft Teams used the wrong platform ids (`google-chat`/`microsoft-teams` vs Hermes's `google_chat`/`teams`); per-platform `busy_ack_enabled` was a silent no-op (Hermes only reads the global `display.busy_ack_enabled`); and a "slash command notice TTL" setting wrote a key that doesn't exist in any Hermes version. Every new surface is capability-gated through `HermesCapabilities`; pre-0.20 hosts render exactly as before.

**Scarf 2.16.x–2.17.x targeted the Hermes v0.18 line — verified through v0.18.2 (v2026.7.7.2).** The v0.18.1/v0.18.2 patches were re-audited surface-by-surface (see the Verified table); the only integration-facing change was the cron `run_claim` field, absorbed by the lossless jobs.json round-trip that also fixed two never-matched keys (`pre_run_script`→`script`, `expression`→`expr`/`minutes`). v0.18 itself is a light-to-moderate cycle for Scarf: the ACP wire, CLI verbs (all 42+ Scarf invocations), config keys Scarf writes, and gateway platform roster were verified stable, but `state.db` gained its first schema change since v0.16 — a `messages.compacted` column marking rows soft-archived by in-place compaction. Scarf schema-detects it (`hasCompactedColumn`) and widens **search** to `(active = 1 OR compacted = 1)` to match `hermes` search on the same DB; transcript and activity queries stay active-only, mirroring Hermes. Provider tables: the **MoA (Mixture of Agents)** overlay was added (auth type `virtual` — no credentials, preset-name models) and **google-gemini-cli** was removed (replaced upstream by the models.dev-backed **Google Vertex AI** `vertex` provider). The new per-job cron **`attach_to_session`** field round-trips safely (no editor UI yet). The same audit fixed two pre-existing Scarf bugs: the Web Tools tab wrote and read dead `web_tools.*` config keys (Hermes reads `web.backend` / `web.search_backend` / `web.extract_backend` — the tab was a silent no-op since at least v0.9), and the cron enable-toggle / editor copy paths dropped `workdir` / `context_from` / `no_agent` from jobs.json on save. New flags (`hasCronAttachToSession`, `hasMCPReauth`) + `isV018OrLater`; pre-v0.18 hosts render byte-identical.

**Scarf 2.12.0 targets Hermes v0.17.0 (v2026.6.19).** v0.17 is the largest Hermes release yet, but the upstream surfaces Scarf depends on — the `state.db` schema, the ACP wire protocol, the `hermes` CLI verbs, the config keys, and the model catalog — were verified **entirely stable**, so it required zero mandatory compatibility changes. Scarf 2.12.0 adds the Scarf-relevant slice: **WhatsApp Business Cloud API** and **SimpleX** gateway setup forms, **Telegram** rich messages (Bot API 10.1) + an online/offline status toggle, an opt-in **curator consolidation** toggle (v0.17 turned the LLM skill-merge pass off by default), and a **max-concurrent-sessions** cap. The same audit fixed a batch of pre-existing Scarf bugs — four broken Health/Settings CLI actions (`hermes audit`→`security audit`, `migrate xai`→`--apply`, browser-setup `--yes`, and the no-op WhatsApp recipient allowlist) and the Curator "Prune" mislabel (rebuilt as the correctly-reversible **"Archive idle skills"**). iMessage via Photon is intentionally deferred — the protocol is still changing upstream. Every v0.17 surface is capability-gated through `isV017OrLater`; pre-v0.17 hosts render the v2.11.0 surface byte-identical.

The **v0.16.0** surface still applies: the first `state.db` schema change since v0.11 (the `messages.active` soft-delete filter applied on `/undo`, schema-gated so older DBs don't error), live session titles via the ACP `session_info_update` notification, the `sessions.rewind_count` "rewound ×N" badge, Spotify's move from a skill to a built-in tool/plugin (sign-in relocated to Plugins), two gateway allowlist/config-shape fixes, and the `hermes sessions optimize` + Kanban goal-pill quick wins. New v0.16 flags (`hasSessionsRename`, `hasSessionsOptimize`, `hasKanbanGoalMode`, `hasInsightsCommand`, `hasDashboardCommand`) + `isV016OrLater` gate them.

The **v0.15.0** baseline still applies — the recommended minimum for every v2.10.0+ feature: **OpenAI lands as a first-class provider** with wire ID `openai-api` (distinct from the existing OpenAI Codex runtime — bare `openai` stays a Hermes alias to OpenRouter so it's intentionally NOT registered in the picker), **Krea image-generation models** (`krea-2-medium` / `krea-2-large`) join the image-gen list, the **xAI May-15 model retirement** maps retired Grok IDs (`grok-4-0709`, `grok-4-fast-*`, `grok-4-1-fast-*`, `grok-code-fast-1`, `grok-3` → `grok-4.3`; `grok-imagine-image-pro` → `grok-imagine-image-quality`) forward so a stored retired model still resolves, while **Vercel AI Gateway (provider) and Vercel Sandbox (terminal backend) are removed** entirely — deleted upstream and dropped from Scarf's picker + terminal-backend list, **xAI Web Search** is a new `web_tools.search.backend: xai` option reusing your Grok OAuth / `XAI_API_KEY` credential, **ntfy** lands as the 23rd gateway platform (push notifications via a topic URL, no account; config under `platforms.ntfy.extra.{topic,server,publish_topic,token,markdown}`) alongside new per-platform flags (Telegram `disable_topic_auto_rename` + `ignore_root_dm`, Discord `allow_any_attachment`, Signal group-only `require_mention`), the **xAI TTS `auto_speech_tags`** opt-in toggle (default off) inserts light `[pause]` tags for more natural voice replies, the **Kanban v0.15 maturation wave** brings server-side `--sort`, Promote / Schedule (park) / Delete-permanently (`archive --rm`) card actions, new Scheduled + Review columns (collapse when empty; **both exits added round-6 P56 behind `hasKanbanReviewExits` at a v0.20.1 floor, NOT `hasKanbanV015`**), read-only `model_override` in the inspector (**per-task worktree `--branch` on create was withdrawn 2026-09-13, `e4e25ccb` — it was emitted ungated and argparse rejects it below v0.15**), a precise chat-scoped board keyed by the originating ACP `session_id` (replacing the old tenant + time-window approximation, with a "This chat ⇄ All tasks" scope toggle) and `--board` multi-board plumbing in the service layer (board switcher UI + `swarm` create-sheet are follow-ups), **Bitwarden Secrets Manager** gets a new Settings → Secrets tab for the `secrets.bitwarden.*` block (enabled, `access_token_env` default `BWS_ACCESS_TOKEN`, project_id, override_existing, server_url US/EU/self-hosted, cache_ttl_seconds, auto_install — the bootstrap token itself lives in `~/.hermes/.env`), **MCP mTLS client certificates** (`client_cert` / `client_key` / `ssl_verify`) surface in the server editor for HTTP + SSE servers alongside a read-only `hermes mcp catalog` browse sheet, **skill bundles** get a read-only Bundles tab listing `~/.hermes/skill-bundles/*.yaml`, **per-session edit-approval modes** add a chat-header chip toggling a live session between Default / Accept Edits / Don't Ask via ACP `session/set_mode` (distinct from the global `approvals.mode` / YOLO surface — sensitive paths always still prompt), and the Health view gains a **"Run supply-chain audit"** button (`hermes audit`, OSV.dev) plus an **xAI retired-model warning** offering one-click `hermes migrate xai`. Every one of these surfaces is **capability-gated** through `HermesCapabilities` — new v0.15 flags (`hasKanbanSessionFilter`, `hasKanbanV015`, `hasNtfyPlatform`, `hasXAIWebSearchBackend`, `hasXAITTSAutoSpeechTags`, `hasBitwarden`, `hasHermesAudit`, `hasXAIModelRetirement`, `hasMCPClientCerts`, `hasMCPCatalog`, `hasSkillBundles`, `hasSessionEditAutoApproval`, `hasAzureEntraAuth`) plus the `isV015OrLater` convenience predicate. On a v0.14 host, Scarf 2.10.0 renders the v2.9.0 surface byte-identical, and the v0.15 affordances are hidden. (The bulk of v0.15 — the `run_agent.py` refactor, cold-start performance, promptware defense, `session_search` rebuild, the Ink TUI orchestrator, the web dashboard, Docker s6 supervision, and the API-server session-control REST — is server-side or other-frontend and benefits Scarf transparently with no Scarf change.)

The v0.14.0 baseline still applies — with one **withdrawal, 2026-09-13 (round-6 decision 3, `a275f59a`): Scarf no longer surfaces `/goal` or `/subgoal` at all**, on any host. Neither has ever been on the ACP adapter's `_COMMANDS` table at any tag (`acp_adapter/commands.py:44-66` @ `v2026.9.7`), and Scarf's chat speaks ACP, so both pills were optimistic mirrors that did nothing; they are gone and the text now falls through with the "sent as an ordinary prompt" notice. The v0.13 Persistent Goals row and the v0.14 `/subgoal` row below are HISTORY of what Hermes shipped, not a description of Scarf's surface. The rest of the v0.14 baseline stands: **`/yolo` + `/sessions` + `/codex-runtime`** are new always-available slash commands that toggle YOLO mode / browse sessions / flip the Codex app-server runtime mid-chat, **xAI Grok OAuth (SuperGrok)** is a new subscription-gated overlay provider with wire ID `xai-oauth`, **NovitaAI** is a new API-key overlay provider with wire ID `novita`, the **Alibaba → Qwen Cloud display rename** flips the picker label while keeping the wire ID stable, **LINE Messaging API** lands as the 21st gateway platform + **SimpleX Chat** as the 22nd (talking to a local `simplex-chat` daemon over WebSocket), **Brave Search (free tier)** + **DuckDuckGo (DDGS)** are new web-search backends — `brave-free` honors `BRAVE_SEARCH_API_KEY` for higher quotas, `ddgs` works anonymously via a lazy-installed Python package, **MCP `supports_parallel_tool_calls`** is a new optional field on MCP server entries surfaced as a tri-state picker, **Hermes Proxy** wraps `hermes proxy start` to give third-party tools an OpenAI-compatible local endpoint at `http://127.0.0.1:8645/v1` that attaches your authenticated upstream credentials, **ACP `--setup-browser`** is a new one-shot setup verb that installs chromium + provisions playwright, the **YOLO mode warning chip** renders when `approvals.mode = yolo`, **`terminal.docker_extra_args`** forwards extra flags to `docker run`, **`display.timestamps`** toggles per-message timestamps in the TUI, **Cron `deliver=all`** fans cron job delivery out to every connected channel, **Discord channel-history backfill** is on by default and now toggleable, and **plugin `tool_override`** manifest flags render as a visible badge on PluginsView. Every one of these surfaces is **capability-gated** through `HermesCapabilities` — 24 new flags on v0.14 plus the `isV014OrLater` convenience predicate. On a v0.13 host, Scarf 2.9.x renders the v2.8.0 surface byte-identical, and the v0.14 affordances are hidden. (Note: v0.14's `/handoff` slash command is messaging-platform handoff (`cli_only` in Hermes's command catalog), not model handoff — Scarf does NOT surface it in the ACP slash menu. Mid-chat model switching stays on the existing `session/set_model` RPC under `hasACPSetSessionModel` from v0.13.)

The v0.13.0 baseline still applies: Persistent Goals (`/goal`), ACP `/queue`, Kanban v0.13 diagnostics + recovery UX, Curator archive/prune + synchronous `run`, Google Chat (20th platform), cross-platform allowlists + per-platform behavior toggles, `hermes gateway list`, MCP SSE transport, Cron `--no-agent`, Web Tools per-capability backends, Profiles `--no-skills`, 5 new models, `image_gen.model`, OpenRouter response caching, context compression count chip, `/new <name>`, `display.language` i18n, xAI Custom Voices. The v0.12.0 baseline still applies too: autonomous Curator, multimodal ACP image content blocks, 5 v0.12-era providers (GMI Cloud / Azure AI Foundry / LM Studio first-class / MiniMax OAuth / Tencent TokenHub), Microsoft Teams + Yuanbao gateways, Kanban CLI, Skills URL install / reload / OFF pill, cron `--workdir`, `auxiliary.curator`, prompt-cache TTL, redaction toggle, runtime metadata footer, Piper TTS, Vercel terminal. The v0.11 / v0.10 baselines still apply too — `/steer`, `messages.reasoning_content` / `sessions.api_call_count` columns, SKILL.md frontmatter chips, `hermes memory reset`, Tool Gateway / Nous Portal subscription routing all continue to work.

## Verified versions

| Hermes | Date | Status |
|---|---|---|
| v0.6.0 | 2026-03-30 | Verified |
| v0.7.0 | 2026-04-03 | Verified |
| v0.8.0 | 2026-04-08 | Verified |
| v0.9.0 | 2026-04-13 | Verified |
| v0.10.0 | 2026-04-16 | Verified (Tool Gateway introduced) |
| v0.11.0 | 2026-04-23 | Verified |
| v0.12.0 | 2026-04-30 | Verified (Curator, multimodal ACP, 5 new providers, Teams + Yuanbao, Kanban — read-only list in v2.6, full read/write board with per-project tenants in v2.7.5, Skills URL install / disable / curator pin, cron --workdir, auxiliary.curator, prompt_caching.cache_ttl, redaction toggle, runtime metadata footer, Piper TTS, Vercel terminal) |
| v0.13.0 | 2026-05-07 | Verified (Persistent Goals + `/goal` slash command, ACP `/queue`, Kanban diagnostics engine (`kanban diagnostics --json`; NOTE 2026-09 audit: the "hallucination gate", `auto_blocked_reason` and `darwin_zombie_detected` were release-note claims Hermes never shipped on the wire — see decisions/hermes-v0-21-1-compatibility-decisions § P14) + per-task max_retries + multiline title, Curator archive + prune + list-archived + synchronous `run`, Google Chat as 20th platform, cross-platform allowlists + busy_ack_enabled + gateway_restart_notification + slash-command auto-delete TTL, `hermes gateway list` cross-profile digest, MCP SSE transport, Cron --no-agent watchdog mode, Web Tools per-capability backends, Profiles --no-skills, 5 new models, image_gen.model, OpenRouter response caching, context compression count, /new \<name\>, display.language i18n, xAI Custom Voices) |
| v0.14.0 | 2026-05-16 | Verified (`/subgoal` + `/yolo` + `/sessions` + `/codex-runtime` ACP slash commands, xAI Grok OAuth (`xai-oauth`) + NovitaAI (`novita`) overlay providers, Alibaba → Qwen Cloud display rename, LINE + SimpleX Chat as 21st + 22nd gateway platforms, Brave Search (free tier) + DuckDuckGo (DDGS) web-search backends, MCP `supports_parallel_tool_calls`, Hermes Proxy local server (`hermes proxy start`), ACP `--setup-browser`, YOLO mode warning chip, `terminal.docker_extra_args`, `display.timestamps`, Cron `deliver=all`, Discord channel-history backfill toggle, plugin `tool_override` badge. Cross-session 1h Claude prefix cache, native Windows beta, PyPI install, and the cold-start performance wave are server-side gains Scarf inherits automatically.) |
| v0.15.0 | 2026-05-28 | Verified (OpenAI first-class provider (`openai-api`, distinct from OpenAI Codex; bare `openai` stays an OpenRouter alias), Krea image-gen (`krea-2-medium` / `krea-2-large`), xAI May-15 model-retirement aliases (`grok-4.3` / `grok-imagine-image-quality`), Vercel AI Gateway + Vercel Sandbox removed, xAI Web Search backend (`web_tools.search.backend: xai`), ntfy as 23rd gateway platform + per-platform flags (Telegram `disable_topic_auto_rename` / `ignore_root_dm`, Discord `allow_any_attachment`, Signal group-only `require_mention`), xAI TTS `auto_speech_tags` toggle, Kanban v0.15 wave (server-side `--sort`, Promote / Schedule / Delete-permanently (`archive --rm`) actions, Scheduled + Review columns, worktree `--branch` + read-only `model_override`, session-scoped board via ACP `session_id`, `--board` plumbing), Bitwarden Secrets Manager (`secrets.bitwarden.*`), MCP mTLS client certs (`client_cert` / `client_key` / `ssl_verify`) + `hermes mcp catalog` browse, skill Bundles tab (`~/.hermes/skill-bundles/*.yaml`), per-session edit-approval modes (`session/set_mode`), Health supply-chain audit (`hermes audit`, OSV.dev) + xAI `hermes migrate xai`. The `run_agent.py` refactor, cold-start wave, promptware defense, `session_search` rebuild, Ink TUI, web dashboard, Docker s6 supervision, and API-server session-control REST are server-side / other-frontend gains Scarf inherits automatically.) |
| v0.16.0 | 2026-06-05 | Verified (first `state.db` schema change since v0.11 — `messages.active` soft-delete filter + `sessions.rewind_count` badge, both schema-gated; live session titles via ACP `session_info_update`; Spotify → built-in tool/plugin (sign-in moved to Plugins); two gateway allowlist/config-shape fixes; `hermes sessions optimize` + Kanban goal-pill quick wins; new flags + `isV016OrLater`) |
| v0.17.0 | 2026-06-19 | Verified (zero mandatory compat changes — `state.db` schema, ACP wire, CLI verbs, config keys, model catalog all stable; WhatsApp Business Cloud API + SimpleX setup forms; Telegram rich-messages (Bot API 10.1) + online/offline status toggles; opt-in curator consolidation; max-concurrent-sessions cap; plus pre-existing-bug fixes — four broken Health/Settings CLI actions and the Curator "Prune" → reversible "Archive idle skills" rebuild. iMessage/Photon deferred (moving protocol). New flags + `isV017OrLater`) |
| v0.18.0 | 2026-07-01 | Verified (`messages.compacted` schema change — search widened to include compacted rows, schema-gated; MoA overlay provider (auth `virtual`); google-gemini-cli → Vertex AI (`vertex`, models.dev-backed); cron `attach_to_session` round-trip; `hermes serve` / `hermes journey` / `hermes mcp reauth` noted for roadmap; plus pre-existing-bug fixes — dead `web_tools.*` config keys → real `web.*` keys, and cron copy paths dropping workdir/context_from/no_agent. New flags + `isV018OrLater`) |
| v0.18.2 | 2026-07-07 | Verified (v0.18.1 is an uncurated ~712-commit rollup, v0.18.2 a WhatsApp Baileys dependency fix; eight-surface re-audit vs `v2026.7.7.2` found the ACP wire, CLI argparse for all Scarf invocations, provider tables, gateway platforms, MCP/skills/curator, and every state.db column Scarf queries unchanged — the upstream schema v17→19 additions (`sessions.display_name` / `origin_json` / `expiry_finalized`, internal `gateway_routing` table) are unqueried. Only integration-facing addition: the cron scheduler's transient `run_claim` field, covered by the new lossless jobs.json round-trip (unknown keys preserved generically; the same fix corrected `pre_run_script`→`script` and `expression`→`expr` + interval `minutes` — keys that had never matched Hermes). No new capability flags — nothing user-facing gates on the patches. `sessions.display_name` and `hermes curator usage` noted for roadmap) |
| v0.19.x "Quicksilver" | 2026-late-7 | Verified (audited as part of the combined v0.18.2→v0.20.0 delta, not separately released against — see v0.20.0 row for the full findings. Introduced `gateway.profile_routes`, per-task `reasoning_effort` overrides, and `hermes config unset` (`hasConfigUnset`), all adopted by Scarf 2.19.0) |
| v0.21.1 | 2026-09-07 | **Verified — current target** (source-audited v0.21.0→v0.21.1 delta, ~5,995 commits — mostly the `hermes_cli` modularization. `messages` DDL byte-identical, `sessions` +`tool_names`/`compression_recovery_deadline`, new `conversation_generations` table (SCHEMA_VERSION 26→30, FTS-only migrations; all PRAGMA/sqlite_master-probed); ACP wire byte-clean; zero CLI removals/renames/arity changes. Forced: Tavily is BACK (removal remodelled as a one-release window, 0.21.0 only), the new `perplexity` web backend + the never-added `keenable` (floor v0.20.5), `agent.service_tier` bounded modes auto/cold + `fast_auto_seconds`, `telemetry.shared_metrics.send`/`.endpoint` (the "no remote sink" copy is now false), `gateway status`'s third PID-less "served by the default-profile multiplexer" verdict, the `messages_fts` 8 KB tool-content prefix (bounded LIKE top-up + "index rebuilding" note, both state_meta-key-gated), the `quarantine_zeroed_state_db` inode swap (state.db handle now reopens on (st_dev, st_ino) change), and the `_ALIAS_GROUPS` dict-comprehension that crashed `check-hermes-tables.py`. Adopted behind `isV0211OrLater` + per-feature flags: `plugins compat --json`, cron `--paused` / `--failure-deliver` / dispatch diagnostics + the new doctor "delivery unverified" issue, kanban `--completion-contract` + `last_failure_error`, `auth priority` / cooldown clearing, MCP device-code OAuth (code surfaced from STDERR), the Computer Use permission card, and eight new config keys. Pre-existing bugs fixed: `skills search` results were always discarded (no `#` column → `--json`), 8 missing hub sources, `debug share` could never succeed (non-TTY needs `-y`), the gateway platform roster (+9 rows, `imessage`→`bluebubbles`, Discord `allowed_channels` gap closed), a dead `slash_command_notice_ttl_seconds` field and `gateway_restart_notification` default drift, a stale image-gen list, and four unreachable plugin providers. Several surfaces the report called v0.21.1 are older (v0.15–v0.18) — the modularization moved their argparse blocks; each is gated at its true floor. Pre-target hosts render byte-identically) |
| v0.21.2 | 2026-09-11 | Verified |
| v0.21.0 "Pantheon" | 2026-08-31 | **Verified** (source-audited v0.20.5→v0.21.0 delta, ~2,287 commits incl. intermediate v0.20.6/v2026.8.27: SCHEMA_VERSION still 26 but DDL changed via auto-migrator — `messages._compressed_summary` detected, `gateway_heartbeats` recorded, lazy `hosted_room_*` Bot Mode tables noted (all sqlite_master/PRAGMA-probed, never version-probed); ACP wire byte-clean. Fixed-before-upgrade: dead `auxiliary.web_extract.*` writes, Tavily backend removal, turn-lease default 1800→5s, curator pin/unpin exit codes, offline `--version` silence, MCP catalog 20→65 + dead Atlassian URL, carrier-aware session previews (byte-identical eligibility SQL port), liberal cron jobs.json shapes, two new providers. Adopted: Peers section (`hermes peer` dm/run/status/stop), cron incidents/doctor/Run Now/Bot Chat delivery, dotted-key `\.` escaping. New `isV0206OrLater` + `isV021OrLater` groups with per-flag floors verified against every intervening tag — several release-note "v0.21" features actually shipped at v0.20.6. Pre-target hosts render byte-identically; shipped in Scarf v2.23.0) |
| v0.20.6 | 2026-08-27 | Verified (intermediate patch tag audited within the v0.21 cycle; carrier of several features the v0.21 notes advertise: cron incidents / resume --run-now / bot-chat delivery, `browser close-profile`, essential `hermes-agent` skill, curator pin/unpin diagnostics, `auxiliary.web_extract` removal — each gated at this floor in Scarf) |
| v0.20.5 | 2026-08-19 | Verified (source-audited in the v2.21.0 cycle, ~804 commits: schema/ACP unchanged; `--version` full-output probe, OpenCode Free keyless provider, unlimited max turns, unseeded `stt.provider`, profile display-name rendering; flags `isV0205OrLater` + `hasVersionFlagFullOutput` + `hasCronReasoningEffort`) |
| v0.20.4 "Herald" | 2026-08-18 | Verified (source-audited v0.20.0→v0.20.4 delta, ~3,000 commits over patch tags v2026.8.13/8.16/8.16.2/8.18: state.db additive (`sessions.hidden`/`last_read_at` adopted via column probes + JSON1 check), ACP wire clean. Fixed-before-upgrade: personalities moved to code (plus two pre-existing Scarf parse bugs), cron pause-marker firing gate (iOS now drives `cron pause|resume`), `skills update` silent local-edit skips now reported. Adopted: unread indicators, hidden filter, reset-child listing, curator ledger/purge/entry-rollback, project skills + trust, MCP catalog picker + `identity_header`/`cwd`, Actual Computer provider, multiplex allowlist parsing, ~10 new settings keys. New `isV0204OrLater` patch-level flag group; also fixed in the same cycle: inverted wake-reconnect analytics, curator purge sentinel case, YAML flow-list parsing. Pre-0.20.4 hosts render byte-identically) |
| v0.20.0 "Herald" | 2026-08-03 | Verified (source-audited v0.18.2→v0.20.0 delta, ~5,700 commits: `state.db` SCHEMA_VERSION 19→25, all additions additive and unqueried by Scarf; ACP method roster + `session_update` discriminators byte-identical. `/compact`→`/compress` rename, `curator status` header generation change (Scarf parses both), provider catalog refresh (Vercel AI Gateway back, Fireworks AI + Vertex AI overlay-only, Solar→Upstage alias, DeepSeek retirement aliases), Buzz (Nostr) as new gateway platform, `agent.max_turns` display default 60→500. Scarf 2.18.0 shipped pinned sessions + last-activity sidebar, per-model cost breakdown, richer session export (MD/HTML/Quarto/trace + redaction), `approvals suggest`, cron run history, plus six pre-existing-bug fixes (skill uninstall/update `--yes`, runtime-metadata-footer key, Google Chat/Teams platform ids, busy_ack_enabled no-op, nonexistent slash-notice-TTL key). Scarf 2.19.0 closed the settings backlog (title generation, reasoning effort, smart approval policy, secrets sources, voice tuning, telemetry + SQLite journal knobs, `gateway.profile_routes` editor) and fixed the browser picker writing the nonexistent `browser.backend` instead of the real `browser.cloud_provider`. New v0.20 capability-flag group + per-key 0.18/0.19/0.20 floors gate every surface; pre-0.20 hosts render exactly as before) |

## How compatibility is maintained

**SQLite schema detection.** [`HermesDataService`](Core-Services) probes for v0.7+ columns (`reasoning_tokens`, `actual_cost_usd`, `cost_status`, `billing_provider`) and v0.11+ columns (`sessions.api_call_count`, `messages.reasoning_content`) at open time. The v0.11 flag (`hasV011Schema`) only flips true when both new columns are present — partial-migration DBs (sessions migrated, messages not yet) stay on the v0.7 path to avoid runtime "no such column" errors. When absent, model fields stay nil/0 and the views render placeholders. Older databases keep working; newer columns light up automatically once the database has them.

**Log line format.** Hermes log lines may carry an optional `[session_id]` tag between the level and the logger name. [`HermesLogService.parseLine`](Core-Services) treats the tag as an optional capture group, so older untagged lines still parse.

**ACP capability gates.** Rich Chat checks for `session/list` and `session/load` support; if Hermes doesn't advertise them, the session picker / reconnect UI degrades gracefully (you can still chat, just with reduced session navigation). Note that Scarf no longer sends `session/resume` at all — reconnects are load-only, because Hermes's resume silently creates an orphan server-side session when given an unknown id while a failed load is detectable and falls back to `session/new`.

**CLI output parsing.** Where Scarf parses `hermes` CLI output (Health, Tools, Pairing), the parsers tolerate field reordering and unknown lines.

**HERMES_OVERLAYS mirror.** [`ModelCatalogService.overlayOnlyProviders`](Core-Services) mirrors the overlay-only entries from `hermes-agent/hermes_cli/providers.py`. The set has grown across releases — the original v0.10 group (Nous Portal, OpenAI Codex, Qwen OAuth, Google Gemini CLI, GitHub Copilot ACP, Arcee), the v0.12 additions (GMI Cloud, Azure AI Foundry, LM Studio, MiniMax OAuth, Tencent TokenHub), the v0.14 additions (xAI OAuth, NovitaAI), and the v0.15 addition (OpenAI `openai-api`). When Hermes adds a new overlay-only provider, Scarf needs a release to surface it in the picker — but existing overlays silently pick up base-URL and auth-type refinements without a Scarf release, because only the identity is mirrored, not the full overlay struct.

**Device-code OAuth flow (v0.10.0+).** Nous Portal sign-in goes through `hermes auth add nous --no-browser` as a subprocess (see [`NousAuthFlow`](Core-Services)). Scarf regex-extracts the device-code block from stdout and auto-opens the verification URL via `NSWorkspace`. On success, it re-reads `~/.hermes/auth.json` to confirm `providers.nous.access_token` landed. Any change to Hermes's `To continue: / 1. Open: / 2. If prompted, enter code:` output shape would break the parser — low-risk because it's been stable across v0.9 and v0.10, but worth a smoke test on future Hermes releases.

**iOS exec channel — non-interactive PATH (v2.5+).** `CitadelServerTransport` runs commands over Citadel's raw exec channel which doesn't source the user's shell rc files, so non-interactive sessions land with a stripped PATH (typically `/usr/bin:/bin`). pipx installs `hermes` at `~/.local/bin/hermes`, and many sub-tools (git, curl, python) live in homebrew prefixes that the remote sshd would otherwise add via login-shell init. Mac's OpenSSH sshd handles this transparently; Citadel does not. v2.5 inline-prepends `PATH="$HOME/.local/bin:/opt/homebrew/bin:/usr/local/bin:$PATH"` on every iOS-routed `runProcess` so bare `hermes` resolves AND any subprocess it spawns can still find its tools. If you install `hermes` at an unusual path on a remote host (custom virtualenv, system-managed install dir), set the **Hermes binary hint** field when adding the server to point at the absolute path.

**iOS exec output capture (v2.5+).** Citadel's `executeCommand` API throws `CommandFailed` on any non-zero exit and *discards* the captured stdout buffer in the throw path. `hermes skills browse` happens to print its full table and *then* exit non-zero on some hosts — pre-2.5 iOS got nothing while Mac (Foundation `Process`) got the full table with `exitCode=1`. v2.5 drives `executeCommandStream` directly, drains stdout + stderr regardless of outcome, and recovers the exit code from the `CommandFailed` catch — feature parity with Mac for any Hermes command that emits useful output before exiting non-zero.

## v0.11 CLIs not yet adopted

Hermes v0.11 added a handful of new top-level CLIs Scarf doesn't yet drive directly: `hermes plugins`, `hermes webhook`, `hermes insights`, `hermes logs`, `hermes dashboard`, `hermes completion`. Scarf still reads the underlying files (`~/.hermes/plugins/`, `~/.hermes/webhooks/`, etc.) directly today, which keeps working. Switching to the canonical CLIs is forward-compatible work for v2.6+.

**`hermes memory reset`** is surfaced as the **Reset memory…** toolbar action on Memory views (Mac + iOS) with a destructive-confirmation dialog.

**`hermes profile`** is half-adopted as of v2.5.1. Scarf doesn't yet drive `hermes profile create / use / delete` from in-app UI (still file-based, still local for now), BUT Scarf v2.5.1+ **respects the active profile** by reading `~/.hermes/active_profile` at path-resolution time via [`HermesProfileResolver`](Core-Services). So `hermes profile use coder` on the host followed by a Scarf relaunch correctly reads `~/.hermes/profiles/coder/state.db`, `config.yaml`, sessions, memory, cron, etc. See [Projects & Profiles](Projects-and-Profiles) for the full profile model. In-app profile switching (writing `active_profile` from a UI control) is a separate v2.6+ feature — handling running ACP processes through a profile switch is more involved than just flipping a file.

## What breaks across major Hermes versions

If Hermes ships a major release that changes:

- **Database schema** — columns dropped, table renames — Scarf may need a release. Expected to be infrequent and called out in Hermes release notes.
- **ACP method names or message shapes** — chat would break. Scarf would ship an update with the new methods supported alongside the old.
- **CLI output format** — Health / Tools / Pairing would degrade. Often Scarf can ship a parser update independently.

When in doubt, check the [Health](Gateway-Cron-Health-Logs) view for compatibility warnings (Scarf surfaces the Hermes version it sees and flags mismatches with what it was tested against).

## Reporting a compatibility issue

If a new Hermes release breaks something in Scarf, please file an issue including:

- Output of `hermes --version`.
- Scarf version (Settings → General → About).
- The specific view that's broken and what's wrong.
- The relevant log snippet from `~/.hermes/logs/errors.log` (filter sensitive content first).

---
_Last updated: 2026-09-18 — Added the voice feature floors: Hermes Voice playback (v0.20.1+, already within target) and Live Voice / GPT-Live (v0.21.3, ahead of the current v0.21.2 target). Previously 2026-08-20: Scarf v2.20.0 + Hermes v0.20.4 "Herald" (v2026.8.18), the current target at that time. Intro and Verified table updated for the v0.18.2→v0.20.0 audit (v0.19 "Quicksilver" + v0.20 "Herald", ~5,700 commits) and the Scarf 2.18.0/2.19.0 releases that closed out the v0.20 compatibility + settings work. Previously 2026-07-14: ACP capability-gates section updated for load-only reconnect; v0.18 and earlier baselines still fully apply)_