# Whole-surface audit round 3 — after the P18–P29 remediation branch

Date: 2026-09-10. Branch `fix/whole-surface-audit-r2` (34 commits, P18–P29), merged to `main` as `merge(whole-surface-audit-r2)`. Round 2 is `documents/hermes-v0.21.1-whole-surface-audit-round2.md`; the brief is `documents/hermes-v0.21.1-parity-agent-brief.md`; the per-phase lessons are the "Whole-surface remediation — P18" … "P29" sections of `.memory/decisions/hermes-v0-21-1-compatibility-decisions.md`.

## Branch contents

| Phase | Commits | Subject |
|---|---|---|
| P18 | `74cd8321` | cron emptied-field gestures (`--repeat 0`, `--prompt ""`), cold-launch doctor re-parse, `oneShotIsUnresumable` on terminal, fleet `cronFieldStatus`, last literal `== "true"` readers, ten never-emitted kanban wire keys |
| P19 | `c7a50524` | one `YAMLScalar` quoting routine for keys and values, duplicate env/header keys refused in the editor, comment preservation, `'` un-doubling, implicit-type quoting, BOM + per-line endings |
| P20 | `37497c16`, `f82ccb60`, `a6dc9322` | ~20 config defaults corrected in both layers; sentinel + resolver for `approvals.mode`, `display.show_reasoning`, `approvals.timeout`, `gateway_notify_interval`; Slack shared-key precedence; `multiplex_profiles` written to the key in effect; full TTS/STT rosters (decisions 4, 5) |
| P21 | `4995e7c0` | `mcp login` drained to EOF before judging; `pairing list` hint filter; `plugins install/update` consent discard; `skills update` attempts vs installs; anchored success markers; `security audit` advisories label |
| P22 | `6ce74848` | `PlatformSetupForm` choreography for all 15 setup VMs off-main with blank-form guards; dashboard `lsof` bounded; kanban throttle stamps the attempt; gateway runner seam + named timeouts; `SkillsViewModel` `@MainActor` |
| P23 | `7644b37d` | ten re-floors (0.18.1–0.20.3), `hasSessionsRename` removed, `parseLine` fails closed (decision 7), Web Tools widen-for-current, eight roster rows gated with `isVisible(on:isConfigured:)` (decision 6) |
| P24 | `e4bf9653` | OAuth token path sanitiser ported over unicode scalars, Clear Token removes the whole state set, dead `sse_read_timeout` field removed, `_parse_boolish` type gate, exact-case transport, owner-scoped reap, CRLF-tolerant device prompt |
| P25 | `eac1efa3`, `5dbc2f0e`, `2b6e2960`, `21cb7668` | iOS `updateFaults`, "ago ago", iOS plain-`Text` card body (decision 1), "Re-scan skills" (decision 2), trace redaction honoured (decision 3), Export All withholds `trace`, cron skills block on an empty roster, four dead `KanbanService` verbs |
| P26 | `ced2866c` | ~20 citations re-anchored at v2026.9.7; `computeConfiguredPlatforms` separator; `overlayMetadata` alias fallback |
| P27 | `8b1df76d`, `479886cf` | `check-hermes-tables.py` fails closed (lane 5 `sys.exit`, skips → exit 2 unless `--allow-skip`), reads `--tag v2026.9.7` by default, 13 unit tests |
| P28 | `6a401176` … `4c8623f6` | cross-phase review remediation: reachable widen-for-current hatch + Tools tab through one seam, iOS approvals sheet from `HermesApprovalMode` with sentinel, per-run `OutputInbox`, trace redaction default ON, last literal boolish readers, ten stale consumer comments |
| — | `bff03f0f`, `30aa324b` | two test-host fixes: load-proof waits, and a `guard` where `rapidTogglesAreSerialised` trapped and took the whole `scarfTests` host down (three crash reports) |
| P29 | `7a835680` … `28799050` | round-3 pre-merge fixes of regressions this branch introduced (below) |

## Verdict

**P18–P28 hold, with four regressions of their own caught by round 3 and fixed in P29 before merge.** Every round-2 finding re-derived by the five reviewers is genuinely fixed, the cross-phase review's 14 items are remediated, and the "Verified sound" sections below are again larger than the previous round's. Round 3 found **9 HIGH, ~12 MED, ~30 LOW**: 4 HIGH were NEW (introduced by P21/P23/P25/P28 — all fixed in P29), 5 HIGH are PRE-existing and newly visible (filed as P30–P34).

Defect classes, round 3:

1. **The wrong Hermes surface was read.** P23 fixed `/compress` against `hermes_cli/commands.py` (the CLI/TUI table); Scarf's composer talks to `acp_adapter`, whose slash set spells it `compact` through 0.19.0 and `compress` from 0.19.1 with no alias. The CLI-table argument was correct and irrelevant. Fixed in P29 (`hasACPCompressSpelling` @ 0.19.1). Same class: seven other slash-menu rows the ACP adapter has never dispatched (P34).
2. **A walk claim in a doc comment is not a walk.** `hasSessionsExportNoRedact` said "a walk of all 32 tags puts the first registration at v2026.9.7"; it is `hermes_cli/main.py:13567` @ v2026.7.7 (0.18.1). The tests encoded the claim. Fixed in P29.
3. **Sentinel read without sentinel write.** P28 gave iOS the "Host default" row and the untouched-save guard but not the Mac's no-op-on-sentinel rule, so selecting the row wrote `approvals.mode: ''` — which Hermes normalises to `manual` while Scarf renders "Host default (smart)". Fixed in P29.
4. **Verdicts fixed in one twin.** P21 output-judged `skills update` (all) but not `skills update <name> --force` — the one that discards local edits. Fixed in P29. Still open: `pairing approve`/`revoke` are `-> None` handlers judged by exit code (P31).
5. **Quoting routines Scarf still has four of.** P19 unified two; `ProfileRoutesWriter.quoted` and `HermesBotProfileYAML.quoted` remain and both make PyYAML raise on a leading `}`/`]`/backtick (P32).
6. **Proving one half of a read.** P22's form choreography proves the `.env` read and not the config.yaml read, so a blipped config.yaml read + Save wipes WhatsApp Cloud credentials (P33).
7. **Recovery semantics Hermes splits and Scarf does not.** Recurring jobs in `error` are resumable on the host and blocked in Scarf; "Resume & Run Now" is one-shot-only on the host and offered for every paused job in Scarf (P30).

## Test-infrastructure finding

The full parallel `scarfTests` run was not a usable signal until this branch's last two commits: `MainActorBlockingWritesP11Tests.rapidTogglesAreSerialised` indexed `spans[1]` after its count expectation had already failed under load, trapping with `Index out of range` and taking the whole test host down (three `.ips` crash reports today, all that line), which cascaded into ~100 timing failures in unrelated suites and was mis-read by two phase agents as "pre-existing flakes". After `30aa324b` a full parallel run shows only the four known load-sensitive suites (`ACPClientStartIdempotenceTests`, `ChatViewModelStartLifecycleTests`, `ScarfMiniAppBridgeTests`, the ACP `session/cancel` suites — t-f3820038), each green in isolation. ScarfCore 2603/2603; Mac 1015/1015 serial; iOS builds; `scripts/tests` 13/13; `check-hermes-tables.py --tag v2026.9.7` → `OK … lanes=5/5`.

## Product decisions for Alan (calls, not bugs)

1. **Wedged recurring cron jobs** (P30). Mirror Hermes's split (plain Resume for recoverable-error recurring jobs; "Resume & Run Now" only for `once`)? And for a recurring job that went terminal via `completed`: (a) plain Resume + "no future occurrences" hint, (b) one-click "re-arm as one-shot" that rewrites the schedule, or (c) leave recovery to a manual edit?
2. **What a partly-refused `skills update` quotes** (P31): suppress `is already installed at` from the failure set on the update path only, quote the LAST refusal, or one reason per attempted skill?
3. **Refused `pairing approve`/`revoke` presentation** (P31): sticky error or quiet no-op; and whether to surface the lockout's "clears in ~N minute(s)" text verbatim.
4. **ACP slash-command roster** (P34): reconcile the whole fallback list with the adapter's nine names (drop `/clear`, `/cost`, `/reload-skills`, `/exit`, `/yolo`, `/sessions`, `/codex-runtime`; add `/reset`, `/context`, `/version`), or only delete the never-existed `/cost` this cycle?
5. **`max`/`ultra` reasoning-effort floors** (P35): re-floor to 0.18.1/0.19.0 (adds a picker row on 0.18.1–0.19.1 hosts — a C1 rendering change in the permissive direction) or keep the v0.20 gate and mark it deliberately conservative?
6. **Refuse vs reshape a control character in a user-typed scalar** (P32): route `ProfileRoutesWriter`/`HermesBotProfileYAML` through `YAMLScalar.quoteIfNeeded` (silently double-quotes a tab/newline) or make it a visible editor validation error like `duplicateKey`?
7. **Settings reading `.env` overrides** (P32): model `GATEWAY_MULTIPLEX_PROFILES` and `SLACK_REQUIRE_MENTION` (truthful on provisioned hosts; adds an `.env` read to the Settings load and a third "in effect" spelling)?
8. **Post-load selection reconciliation** (P35): when the roster narrows after the detached read, snap `selected`/`selectedPlatform` back, leave the stale form read-only, or accept the short window?
9. **Forms vs Settings posture on a resolved default** (P33/P35): platform-setup forms prime and write the resolved default on Save; Settings treats absence as a sentinel and writes nothing. One rule, or state why forms differ?
10. **iOS sentinel row: unset or refuse** (P35): the Mac's "Host default" row is inert; getting out of an explicit mode needs `hermes config unset` behind `hasConfigUnset`. Wire the sentinel row to `config unset` on both platforms (a new gated write path), or accept an inert row?

## Proposed remediation phases (P30+)

Filed as Memophant tasks: **P30** `t-f1f8fe74` cron recovery semantics (HIGH ×2, PRE); **P31** `t-4d0b9fe4` pairing/plugins/skills verdicts + missing `--` (HIGH ×2, PRE); **P32** `t-700f9255` unify the last two YAML quoting routines (HIGH ×2, PRE); **P33** `t-eff9696b` prove the config.yaml read in setup forms + `saveDirectYAML` on `writeChain` (HIGH, PRE); **P34** `t-069033ec` ACP slash-command roster (HIGH, PRE, decision 4); **P35** `t-602f6b7b` remaining floors/selection/residue (MED); **P36** `t-628227d4` citation sweep + README target lines (LOW). Also still open from earlier rounds and relevant: `t-bd119897` (OAuthFlowController drain), `t-6fa3fc84` (Slack writer shadowed by a top-level block), `t-ff609789` (cron `--`), `t-f3820038` (ACP idempotence flake).

---

# Per-surface reviewer reports (verbatim)

The five read-only reviewers' full output follows. Items marked NEW that P29 fixed: ACP compress spelling, `hasSessionsExportNoRedact` floor, iOS sentinel write, `--force` verdict, `yamlScalar` tab guard, `HermesApprovalMode` boolish spellings (`0`/`1` are ints to PyYAML and stay `manual` — P29 corrected that half of the finding), `pluginsUpdateFailure` bare `Error:`, `ProcessTimeout` overrun, `detectSignalCLI` on main, the three untested flags and four inline roster floors, `sessionsRenameIsUngated`, the `url`-before-`transport` order, and every NEW stale citation.

## Cron, Kanban, Sessions, Fleet apply

### HIGH

- **HIGH · PRE · "Resume & Run Now" can never succeed on a recurring job, and Scarf offers it for every paused one.** `rearm_oneshot` parses the injected `now` into a `kind == "once"` schedule (passes `cron/jobs.py:2053`) and then, inside `apply`, rejects the **job's own** schedule: `if job.get("schedule", {}).get("kind") != "once": raise ValueError(_REARM_RECURRING_ERROR)` — "Cannot re-arm recurring jobs: re-arm is one-shot-only; use plain resume or cron run." (`cron/jobs.py:2040-2042`, `:2065-2066` @ `v2026.9.7`); `cron_resume` catches it and returns 1 (`hermes_cli/cron.py:691-695`). Scarf shows the button whenever `hasCronResumeRunNow, !job.enabled || job.isTerminal` — i.e. for any *paused* `0 9 * * *` / interval job — and the doc comment calls it "the documented escape hatch … re-arms it to fire immediately", true only for one-shots · `CronView.swift:596-605`, `CronViewModel.swift:408-414`, `BotRoutinesView.swift:129-136` · `cron/jobs.py:2045-2066`, `hermes_cli/cron.py:681-700`
- **HIGH · PRE · A recurring job stuck in `state == "error"` has no recovery path in Scarf, and Hermes has one.** `_reject_terminal_activation` explicitly exempts it — `is_terminal_job(job) and not _is_recoverable_error_job(job)` (`cron/jobs.py:1867-1869`), where `_is_recoverable_error_job` is `state == "error" and schedule.kind in {"cron","interval"}` (`:509-522`, docstring: treating it as terminal would "wedge it forever"). So `hermes cron resume <id>` genuinely recovers such a job. Scarf's `isTerminal` is `effectiveState in {completed, error}` with no kind/recoverability arm, so `refusesTerminalJobLocally` blocks Resume client-side and posts "has failed and can't just be resumed — use Resume & Run Now to re-arm it" — which is the HIGH above and also fails. Run Now is correctly disabled (`trigger_job` uses the bare `is_terminal_job`, `cron/jobs.py:2012`), so every affordance is a dead end. This is exactly the "deny an operation the host would have accepted" rule `isV0206OrLater`'s own comment states · `CronViewModel.swift:389-391,401-404,419-422`; `HermesCronJob.swift:509-513`; `BotRoutinesView.swift:129-136` · `cron/jobs.py:1865-1878`, `:504-522`
  - iOS is *more* correct here by accident: `oneShotIsUnresumable` early-returns for `schedule.kind != "once"`, so `IOSCronViewModel.setEnabled` lets the CLI decide (`IOSCronViewModel.swift:142`). The two platforms now disagree on the same job.

### MED

- **MED · NEW (P23/P26 residue) · `withEnabled`'s ungated-write justification still names v0.20.4 after P23 re-floored the gate to v0.20.1.** The comment says "Since v0.20.4 `is_job_runnable()` … refuses to fire" and "Those two Hermes functions are byte-identical at v0.20.0 (v2026.8.3) and v0.20.4 (v2026.8.18)". `_has_pause_marker` first exists at **v2026.8.13** (`pyproject.toml` `version = "0.20.1"`, def at `cron/jobs.py:482`, called from `is_job_runnable` `:489`/`:498`); it is absent at v2026.8.3 (0.20.0). The flag's own doc 800 lines away already says "**Floor v0.20.1, not v0.20.4**" and cross-references the correction — so the tree contradicts itself in two files, the precise rot P28's note warns about ("Grep the old version string repo-wide, not just the flag's own file") · `HermesCronJob.swift:255,270-271` vs `HermesCapabilities.swift:1064-1073,1669` · `cron/jobs.py:477-485` @ v2026.9.7, `:482,:489` @ v2026.8.13
- **MED · NEW (P26 incomplete) · `IOSCronViewModel` still cites a past-target line range for `resume_job`.** `cron/jobs.py: 2212-2233` — at `v2026.9.7` `resume_job` is `:1986-2003`; `2212-2233` lands inside `_advance_after_run`'s repeat-counter block, so the cite resolves to real-but-unrelated code (the most expensive kind). P26 rewrote the `mark_job_run`/`_advance_after_run` cite 70 lines *below* this one in the same file and left this one alone · `IOSCronViewModel.swift:110-111` · `cron/jobs.py:1986-2003`

### LOW

- **LOW · PRE · `SessionsViewModel.runHermes` has zero callers and is a MainActor-isolated spawn wrapper.** Both real CLI sites use `ctx.runHermes` inside `Task.detached` (`:133`, `:522`); this `@discardableResult private func` inherits the target's MainActor isolation and `ServerContext+Mac.runHermes`'s silent 60 s default — "an ungated accessor left in place is the next caller's bug" (P28) · `SessionsViewModel.swift:1047-1050`; `ServerContext+Mac.swift:21`
- **LOW · PRE · `HermesCronIncidentsParser`'s "no `--json`" verification is pinned to the previous target.** It cites v0.21.0 `hermes_cli/subcommands/cron.py:287-305` — correct at `v2026.8.31` (331 lines, incidents at `:287-305`) but at `v2026.9.7` the file is 189 lines and the registration is `:164-172`. The claim itself still holds (no `--json` at target); the anchor does not · `HermesCronIncidentsParser.swift:55-57` · `hermes_cli/subcommands/cron.py:164-172`
- **LOW · NEW (P25) · `exportAllExcludesTrace`'s directory-path cite overshoots the function.** `:425-440` — the multi-session branch is `:425-434` and the `except TraceRedactionError` closes at `:436`; `:439` is `def _export_markdown` · `SessionsViewModel.swift:648-654` · `hermes_cli/sessions_cmd.py:425-436`
- **LOW · PRE · Two emitted task fields are never decoded.** `_TASK_DICT_FIELDS` carries `project_id` and `provider_override` (`hermes_cli/kanban_output.py:20,22`); `HermesKanbanTask`'s `CodingKeys` has neither, so the inspector's override row renders a model with no provider and a provider-only override renders nothing · `HermesKanbanTask.swift:142-159`, consumed `KanbanInspectorPane.swift:220` · `hermes_cli/kanban_output.py:18-24`
- **LOW · PRE · "Resumed — running now" overstates `cron resume --run-now`.** `rearm_oneshot` sets `next_run_at` to now and returns; no dispatch happens until the scheduler's next tick, and unlike `runNow` this path does not follow up with `cron tick` · `CronViewModel.swift:412-414` vs `:540-547` · `cron/jobs.py:2055,2072-2075`; `hermes_cli/cron.py:699-700`
- **LOW · PRE (carried from round 2, not in the P18–P28 plan) · pause-marker truthiness still diverges.** Hermes is `bool(job.get("paused_at"))` (`cron/jobs.py:479`); Scarf treats any non-`null` value as a marker, so `paused_at: ""` / `0` / `false` reads as paused in Scarf and not-paused on the host · `HermesCronJob.swift:484-488`

#### Verified sound

- **P18's cron clear gestures, re-derived end to end.** `--repeat 0` is the clear (`normalize_repeat_value` `cron/jobs.py:591`, guard `tools/cronjob_tools.py:787-792` where `0` passes `is not None`); `--prompt ""` is the clear (`subcommands/cron.py:91`, `type` is plain string); `--repeat` is `type=int` so `""` would be an argparse error (`subcommands/cron.py:97`) — the asymmetry is modelled correctly. Both helpers compare against the **seeded** value (`existingPrompt: job.prompt`, `existingRepeatCount: job.repeatEditValue`, `CronView.swift:200,204`), so an always-blank field writes nothing. The `--name` counter-example is still correctly omitted on empty.
- **P18's cold-launch doctor re-parse.** `load()` re-parses the retained stdout whenever the id roster changes (`CronViewModel.swift:112-113`) with no spawn, and the re-RUN stays gated on `hasLoadedDoctorFindings` (`:122`). Both orderings land on a full roster: doctor-first → `reparseDoctorFindings`; load-first → `adoptDoctorOutput` parses against the current roster. The `isLoadingDoctor` / `isLoadingIncidents` early-returns now coalesce a dropped `force` (`:314-319`, `:208-214`, re-issued at `:232-235`, `:349-352`) and `refreshDiagnosticsAfterMutation`'s gate is `hasLoaded… || isLoading…` (`:789-790`) — no new spawn on a pre-target host (C1).
- **P18's `oneShotIsUnresumable` re-model.** Keyed on terminal state, not `lastRunAt`: `resume_job` calls `compute_next_run(job["schedule"])` with `last_run_at` defaulted `None` (`cron/jobs.py:1991` → `:1096`/`:1103`), so `_recoverable_oneshot_run_at`'s already-run arm (`:846`) never fires there; `rearm_oneshot` keeps `last_run_at` (`:2067-2071`). The ±12h naive window and the `ONESHOT_GRACE_SECONDS = 120` (`:96`) comparison are right in both branches.
- **P18's cron dead-decode deletions are complete and the citations exact.** `_TASK_DICT_FIELDS` / `_SHOW_RUN_FIELDS` / `_RUNS_RUN_FIELDS` at `kanban_output.py:18-32`, single serialisers `_task_to_dict` `:85-88` / `_obj_dict` `:81`, emitters `kanban.py:381` (create), `:429` (list), `:492-498` (show envelope, runs at `:497`), `:1136` (runs) — all verified; none of `task_id`, `claim_lock`, `claim_expires`, `max_runtime_seconds`, `last_heartbeat_at`, `failure_count`, `idempotency_key`, `current_run_id`, `parent_results` is in any of them. `HermesKanbanRun`'s "closed list" enumeration matches both tuples byte for byte.
- **P18's `cronFieldStatus`.** `failed > 0 → .failed` and `created == 0 && (scriptOnly || cancelled) → .skipped`; `cancelledRemaining` is now threaded from the loop (`FleetApplyExecutor.swift:282,380-382`) and the documented `created > 0 && cancelled > 0 → .applied` choice is consistent with the `scriptOnlySkipped` sibling. `--paused`-in-create vs. the second pause write is gated on `hasCronCreatePaused` and matches `cron_create`'s `paused` kwarg (`hermes_cli/cron.py:575-576` → `cron/jobs.py:1780`). Runs under `Task.detached` with `ctx.runHermes`'s bounded 60 s default.
- **P22's kanban diagnostics throttle.** `refreshDiagnosticsIfDue` stamps `lastDiagnosticsFetchAt` **before** the fetch, so a failing host is capped at one attempt per 30 s instead of one per 5 s tick; previous signals survive a failure; the spawn still happens inside the `KanbanService` actor.
- **P25's copy + completeness fixes.** `relativeTimeLabel` is pure, `now`-injectable, and no arm appends a second " ago" (`RelativeDateTimeFormatter.localizedString` already ends in it); the a11y label reuses the same string (`KanbanCardView.swift:159`). The Skills block is now `roster ∪ job.skills` (`CronView.swift:1312-1313,1359`), so an empty roster no longer hides "Clear all skills on save"; the checkbox grid is `.disabled(form.clearSkills)` with a caption, matching `cron_edit`'s `--clear-skills`-wins precedence (`hermes_cli/cron.py:611-618`). iOS card body is plain `Text` with the `javascript:` rationale, and the Mac inspector's comment now names all three sites. The iOS diagnostic sheet's "unblock, complete, or archive" names verbs that exist.
- **P25/P28's trace redaction, walked at the tag.** `--format` choices incl. `trace` first appear at `v2026.7.7` (`hermes_cli/main.py:13546`; absent at `v2026.7.1`) = the `hasSessionsExportFormats` 0.18.1 floor; `--no-redact` is registered for the first time at `v2026.9.7` (`subcommands/sessions.py:83`, absent at `v2026.8.31`) = `hasSessionsExportNoRedact` → `isV0211OrLater`. `_export_trace` redacts by default (`sessions_cmd.py:382-383`, `redact_trace = not getattr(args, "no_redact", False)` at `:395`) and never calls `_cmd_export`'s `_redact` closure (`:307-311`), so `--redact --format trace` is genuinely a no-op. `exportFormatChanged` defaults the toggle ON for trace, restores the non-trace choice on the way back, and `exportArguments` emits `--no-redact` only when OFF **and** above the floor; below it the toggle is ON-and-disabled with an explanation. Export All drops `trace` (`_export_trace`'s no-argument branch resolves one session via `list_sessions_rich(limit=1, …)`, `:385-387`; its multi-session branch writes a directory, `:425-434`), and `confirmExportOptions` recomputes `availableExportFormats.contains` **before** clearing `pendingExportIsAllSessions`.
- **Cron/kanban/sessions CLI verdicts.** `cron create`/`edit`/`pause`/`resume`/`remove` all `return 1` on failure (`hermes_cli/cron.py:578-580,623-625,654-656,693-697`), so `runAndReload`'s exit-code judgement is sound; `cron run` is the one output-judged site and `runOutcome` gets `failureWins: true` + `successAnchored: true` right against `_job_action`'s column-0 print (`:658`) and `_run_outcome` (`:666-678`). `cron incidents ack`'s exit-0 miss path is correctly discriminated by text (`:287` then `return 0` at `:288`). `sessions delete`'s `_not_found` returns 1 (`sessions_cmd.py:46-48`). No `--json` exists on `cron runs` or `cron incidents` at target (`subcommands/cron.py:159-172`), so the text parsers are still the only option.
- **C10 across the surface.** Every cron/sessions/fleet spawn is in a `Task.detached` with a named timeout (30/60/300 s for cron, 20 s for kanban verbs, 60 s for fleet); kanban goes through the `KanbanService` actor; `sessionDeleteRunner` and the rename are detached with the injection seam intact.
- **Kanban gating and citations.** `supportsKanbanCompletionContract` → `isV0211OrLater` correctly gates the `last_failure_error` sub-line — that field enters `_TASK_DICT_FIELDS` for the first time at `v2026.9.7` (absent at all five preceding tags). `hasKanbanDiagnostics` (v0.13) verified both directions: `kanban_parser.py:251-256`, emitter `kanban.py:678-681`, and the same `diagnostics --json` shape at `v2026.5.7:kanban.py:1365-1375`; the global `--board` flag predates it (`v2026.5.7:kanban.py:179`). The rewritten doc no longer mentions the hallucination gate / `auto_blocked_reason` / zombie detection. `KanbanListFilter.tenant`'s corrected claim matches `kanban_db.py:1472-1479`. All four dead verbs (`claim`, `swarm`, `link`, `unlink`) are gone and every remaining `KanbanService` method has at least one caller.

#### Product decisions

1. **What Scarf should offer a wedged *recurring* job.** The two HIGHs share a root: Scarf models "terminal" as `state in {completed, error}` while Hermes splits it — `_is_recoverable_error_job` makes an error-state cron/interval job resumable, and `rearm_oneshot` makes a recurring job un-re-armable. The mechanical fix is to mirror both predicates (allow plain Resume for recoverable-error recurring jobs; show "Resume & Run Now" only for `schedule.kind == "once"`). The product call is what the *other* half of the UI then says: a recurring job that went terminal via `completed` still has no path except edit-the-schedule or duplicate, and "Run Now" stays refused by the host in both cases — so does Scarf (a) surface plain Resume plus a "this job's schedule has no future occurrences" hint, (b) offer a one-click "re-arm as one-shot" that rewrites the schedule, or (c) leave recovery to a manual edit? `CronViewModel.swift:389-391`, `CronView.swift:596`, `BotRoutinesView.swift:129-136`.### YAML parsing/writing, config model, Settings

#### HIGH

**H1 · `ProfileRoutesWriter` carries a fourth, weaker quoting routine — a route *Name* starting with `}`, `]` or a backtick (or carrying a tab/newline) makes PyYAML raise, and Hermes then discards the entire config.yaml layer.** · PRE (P19 missed; file touched by the branch) · `ProfileRoutesWriter.swift:211-235` (`quoted`), emitted at `:106` (`name:`), `:108` (`platform:`), `:116` (`profile:`); `quotedID` at `:205-207` has no line-break guard either. Its own comment claims it "mirrors `GatewayConfigWriter`'s rule" — since P19 that rule is `YAMLScalar.quoteIfNeeded`, and it does not: no `]`/`}`/`` ` ``/`=`-leading case, no `\t` case, no `containsLineBreak` refusal, and `Double(raw) != nil` instead of PyYAML's resolver table (so `.inf`, `.nan`, `2026-09-09`, `0b101`, `12_000`, `<<`, `=` all round-trip retyped). Route Name is free text (`ProfileRoutesSection.swift:330`, hint "Shown in Hermes logs. Optional.") and `normalizedRoute()` (`:390-400`) only `.whitespaces`-trims it, so an interior tab or a leading `}` survives to the file. Verified with PyYAML 6.0.3: `k: }x` → `ParserError`, `k: ]x` → `ParserError`, `` k: `x `` → `ScannerError`, `k: a\tb` → `ScannerError`. Hermes ref: the bare `except Exception` that logs and CONTINUES with the whole config.yaml layer dropped, `gateway/config.py:773-792` @ v2026.9.7.

**H2 · `HermesBotProfileYAML.quoted` states a post-condition it does not hold, and the failure mode is the one its own doc block calls "total metadata loss".** · PRE (P19 missed) · `HermesBotProfileYAML.swift:867-893`. The doc at `:864-866` says "for ANY input string this returns a single line of valid YAML that PyYAML loads back as that exact string." Fuzzed the rule against PyYAML 6.0.3 over ~6000 inputs: 166 failures. It *does* have the control-char arm (`requiresDoubleQuoting:766-771`), so tabs/newlines are safe — but a value beginning `]`, `}` or `` ` `` raises `ParserError`/`ScannerError`, `<<` and `=` raise `ConstructorError`, and `.inf` / `.nan` / `2026-09-09` / `0b101` / `12_000` load as float/date/int. Reached by `display_name` (`:284`), profile `description` (`:290`), and bot `title` / `description` / `color` / `shape` / `group` (`:426-438`) — a description that opens with a markdown backtick or a `}` is ordinary prose. Consequence is spelled out by the file at `:760-765`: `read_profile_meta` swallows the exception, `_is_bot_managed` returns False, and the profile loses `display_name`, `description`, the whole `hermes-bots` block and drops out of the bot roster. `YAMLScalar.quoteIfNeeded` handles every one of these correctly (same fuzz: 0 failures, values *and* keys).

#### MED

**M1 · `HermesFileService.yamlScalar` still has no tab guard, so an MCP `env:` / `headers:` value containing a tab is emitted bare — PyYAML `ScannerError`, whole config.yaml layer discarded, and the post-write verifier passes it.** · NEW (P19 rewrote this exact condition list and added the line-break arm beside it) · `HermesFileService.swift:2212-2216`; compare `YAMLScalar.quoteIfNeeded`'s `|| raw.contains("\t")` at `YAMLScalar.swift:119`. The *keys* in the same emitted row go through `quoteIfNeeded` (`subMapRows:2019`), so one row quotes its key for a tab and not its value. Values are the only unsanitised half: `MCPServerEditorViewModel.swift:195,201` map `($0.key.trimmingCharacters(in: .whitespaces), $0.value)` — the value is passed raw, so leading, trailing and interior tabs all reach the writer. `patchMCPServerField(expecting:)` cannot catch it: the expected rows are built by the same `subMapRows`, so the literal match succeeds on a file PyYAML rejects — exactly the "a structural verifier cannot see damage that leaves the structure intact" case P19 named. `MCPYAMLMapKeyP19Tests.swift:108` parameterises `A\tB` as a *key* and `:275-285` parses a line-break *value* through real PyYAML; no test passes a tab as a value. Hermes ref: `gateway/config.py:773-792` @ v2026.9.7.

**M2 · `max`'s capability floor is two releases too high and `ultra`'s one, so the Reasoning Effort picker withholds a level the host accepts on every 0.18.1–0.19.1 host.** · PRE (P23 did not re-walk it) · `PowerSettingsWriter.swift:10-12,23-25` puts both in `v020Levels` behind `isV020OrLater`, and the type's doc at `:7` asserts "`max` and `ultra` are the v0.20 additions (#62650)". Walked every `v2026.*` tag: `VALID_REASONING_EFFORTS` gains `max` at **v2026.7.7 = v0.18.1** (`hermes_constants.py`, `("minimal","low","medium","high","xhigh","max")`, docstring updated in the same tag) and `ultra` at **v2026.7.20 = v0.19.0**; v2026.7.1 (0.18.0) has neither. At v2026.9.7 it is `hermes_constants.py:873`. Consumers: `AgentTab.swift:40` (`agent.reasoning_effort`) and `:258`/`:277` (reasoning-overrides editor). The sibling picker for the same vocabulary is inconsistent with it — `AuxiliaryReasoningEffort` (`HermesConfig.swift:54`) lists `max`/`ultra` ungated behind a v0.19 flag, which is correct.

**M3 · `HermesApprovalMode.normalize` models only one of PyYAML's falsy bool spellings, so `approvals.mode: no` reads "Manual (ask before every guarded command)" on a host that never asks.** · NEW (P20 added the bool arm and its rationale) · `HermesApprovalMode.swift:55` `if value == "false" { return .off }`. PyYAML loads `no`/`No`/`NO` as Python `False` just as it does `false`/`off`, and Hermes's `_normalize_approval_mode` branches on `isinstance(mode, bool)` → `"off" if mode is False` (`tools/approval_context.py:200,205-206` @ v2026.9.7), so all of them are the `off` mode upstream. Scarf's string reader falls to `.manual` for `no`. The one-helper answer already exists (`HermesYAML.boolishValue`, whose falsy set is `{false,0,no,off}`); the truthy direction is already right by accident (`true`/`yes`/`on` → `.manual`, matching Hermes's `"manual" if mode is True`).

#### LOW

- **L1 ·** `ProfileRoutesSection.swift:164` still cites `gateway/run.py:23923` for the matcher gate. `gateway/run.py` is **5475** lines @ v2026.9.7; the real gate is `gateway/run.py:4211` (`if not getattr(config, "multiplex_profiles", False): return`). P26's own memory note lists this exact citation among the past-EOF cites it triaged — it survived the sweep. C2. · **P26 missed.**
- **L2 ·** `ProfileRoutesSection.swift:102` cites `gateway/profiles.py:987` for "the allowlist is inert without multiplexing". No `gateway/profiles.py` exists at v2026.9.7; the claim is true at `hermes_cli/profiles.py:703-713` (`profiles_to_serve`, `if not multiplex: return [(active, …)]` at `:712`). C2. · **P26 missed.**
- **L3 ·** `PowerSettingsWriter.swift:96` cites `model_switch.py:2007` for "Hermes lowercases at consumption". No `model_switch.py:2007` exists (`hermes_cli/model_switch.py` and `tui_gateway/model_switch.py` are both far shorter); the claim is true at `hermes_cli/model_switch_providers.py:1063` (`excluded={str(p).strip().lower() for p in …}`). P26 corrected the three reasoning-effort cites at `:4-6` of this same file and left the one ten lines down. C2. · **P26 missed.**
- **L4 ·** `HermesBotProfileYAML.swift:23` cites `tui_gateway/methods_profiles.py:780-863` for the CAS revisions and `:72` cites `:789-791` for the 65536-byte guard. That file is **651** lines @ v2026.9.7; the size guard is `:443` (`if len(json.dumps(incoming)) > 65536`) and the revision map is handled at `:450-473`. C2. · **P26 missed.**
- **L5 ·** `HermesBotProfileYAML.swift:281` cites `profiles.py:980-986` for "Hermes pops `display_name` rather than writing an empty string". Real: `hermes_cli/profiles.py:635-640` (`existing.pop("display_name", None)`). C2. · **P26 missed.**
- **L6 ·** `VoiceTab.swift:69` cites `tools/tts_tool.py:2100-2170` for `_generate_xai_tts`. That file is **682** lines @ v2026.9.7 and `_generate_xai_tts` is not defined in it — it is imported at `tools/tts_tool.py:55`. The "no `tts.xai.model` key" conclusion is right; the evidence line is not. C2. · **P26 missed.**
- **L7 ·** `HermesCapabilities.swift:813-818` says gemini/kittentts "appear in `BUILTIN_TTS_PROVIDERS` from its very first tagged form — v2026.4.23 (v0.11.0) `tools/tts_tool.py`". That constant does not exist at v2026.4.23; its first tagged form is **v2026.4.30 (v0.12.0)** `tools/tts_tool.py:316`. The v0.11.0 *floor* is still correct on the other half of the argument (both names are dispatchable providers at `tools/tts_tool.py:1024,1038` @ v2026.4.23 and absent from that file at v2026.4.16) — only the cited artifact is wrong. C2. · **P20.**
- **L8 ·** `HermesYAML.parseNestedYAML` is last-wins for duplicate *list* keys (`:209` `lists.removeValue(forKey: path)`) but not for `values` / `maps`: a config carrying the same section twice keeps first-block siblings that PyYAML's last-wins mapping discards. P19 closed the bullet half of R2's finding and left the scalar/map half. · PRE.
- **L9 ·** Two env fallbacks Hermes reads and Scarf does not model, both in keys Settings renders: `GATEWAY_MULTIPLEX_PROFILES` wins over config.yaml (`gateway/config.py:711-713`, `_env_multiplex_profiles_override:76-95`) while `ProfileRoutesYAML.parseMultiplex:79-108` reads config.yaml only — so the banner and "Enable Multiplexing" can both be wrong on a host with that var set; and `SLACK_REQUIRE_MENTION` is consulted when `extra["require_mention"]` is absent (`plugins/platforms/slack/adapter.py:5923`) while `HermesConfig+YAML.swift:606` uses a flat `default: true`. Both PRE; Scarf already reads `.env` elsewhere, so the capability exists.

#### Verified sound

- **`YAMLScalar.quoteIfNeeded` is correct, for values AND keys.** Re-implemented the rule exactly (incl. the `anywhere`/`leading` sets, the `\t` and trailing-space arms, and the full `implicitResolverPatterns` table) and fuzzed ~4060 inputs through real PyYAML 6.0.3 in both positions (`k: <v>` and `<v>: k`): **0 failures** — no raise, no retype, no key identity change. The two traps P19's memory note names both hold: the float mantissa must carry a `.` (so `1e3`/`1e+3` stay strings, `1.0e+3` is quoted), and bare `y`/`n` are absent from the bool set.
- **The writer/reader quote pair is idempotent.** Same corpus through `quoteIfNeeded` → `normalizedScalar` and → `stripYAMLQuotes`, then re-emitted: **0 drift**, including the `#it's` → `'#it''s'` case R2 found growing a quote per save. `closingQuoteIndex`'s doubled-`''` skip is what makes it work.
- **Every P20 default and sentinel, re-derived from a 32-tag AST-free walk of `DEFAULT_CONFIG` (falling back to `hermes_cli/config.py` pre-v0.19.1), matches Scarf exactly and the cited flip tags are all correct:** `display.show_reasoning` False→True at **v2026.7.7 (0.18.1)**, last `False` at v2026.7.1 — `displayShowReasoning` gates on `isV0181OrLater` (`HermesConfig.swift:1483`); `approvals.mode` manual→smart at **v2026.7.20 (0.19.0)**, `manual` at every tag back to v2026.3.17 (`config_defaults.py:1534`); `approvals.timeout` 60→300 at **v2026.7.30 (0.19.1)** (`config_defaults.py:1535`, reader `tools/approval_context.py:240-249` — both line numbers exact, docstring quote verbatim); `agent.gateway_notify_interval` 600→180 at **v2026.4.23 (0.11.0)**, absent before v2026.4.13 (`config_defaults.py:196`). Also confirmed `memory.{memory_enabled,memory_char_limit,user_char_limit,nudge_interval}` = True/2200/1375/10 at `:1194,1200,1201,1203`; `terminal.container_{cpu,memory,disk,persistent}` = 1/5120/51200/True at `:318-321`; `voice.auto_tts` False at `:1121`; `secrets.bitwarden.override_existing` True at `:2174`; `openrouter.response_cache` True at `:649`. Every one of those values is inside its stepper's range AND reachable by its step (180/30, 300/5, 5120/256, 51200/1024, 2200/100).
- **The absence-sentinel scheme is sound at the source.** `_load_config_impl` starts from `copy.deepcopy(DEFAULT_CONFIG)` and deep-merges the user's file (`hermes_cli/config.py:2197,2211`), while `save_config` runs `_strip_default_values` (`:2341`) — so an absent key on disk genuinely means "the schema default", and the code readers' own fallbacks (`_get_approval_config().get("mode","manual")` at `approval_context.py:236`) are unreachable for a schema-seeded key. Scarf's "schema layer is the authority" rule is right, and `HermesConfig+YAML.swift:784-789` says so correctly.
- **`setMultiplexProfiles`' key choice is right, and both spellings survive `hermes config set`.** `gateway/config.py:708-710` is exact (`data.get` then `if … is None` → `nested_gateway.get`), and the `:733` `_coerce_bool(multiplex_profiles, False)` / `:561` dataclass-default citations in `ProfileRoutesYAML.swift:104-108` are exact. Neither `multiplex_profiles` nor `gateway.multiplex_profiles` is in `_known_top_level_keys()` (`hermes_cli/config.py:3202` excludes `_EXTRA_KNOWN_ROOT_KEYS`; `DEFAULT_CONFIG["gateway"]` has no such sub-key) — but `set_config_value` writes first and only prints a post-write "did you mean" hint (`:3526-3527`), exits 0, and Scarf judges on exit code, so the write lands and is read. `_coerce_config_set_value` turns `"true"` into a real bool for both spellings. `SettingsP20ConfigDefaultsTests.swift:72-84` drives the real VM through an injected runner and covers all four precedence cases including the null-top-level one.
- **`HermesYAML.boolishValue` matches its source exactly** — `_TRUTHY_STRINGS`/`_FALSY_STRINGS` at `gateway/config.py:25-26`, `_bool_token`'s `str(value).strip().lower()` at `:29-32`. `HermesFileService.boolishOptional:2278-2294` additionally runs the `resolvesToNonString && !resolvesToBool` type gate on the RAW scalar before `unquote`, mirroring `_parse_boolish`'s `isinstance(value, str)` halves; `_TRUE_WORDS`/`_FALSE_WORDS` at `tools/mcp_tool_common.py:120-121` and the function at `:124-137` are both exact, as is the un-gated back-reference `v2026.6.19:tools/mcp_tool.py:3754`.
- **P18's "one boolish helper" survived.** Repo-wide grep for `== "true"` / `== "false"` / `lowercased() == "true"` across ScarfCore, the Mac app and iOS returns only comments, `HermesApprovalMode.swift:55` (M3 above), and `SettingEditorSheet.swift:143` — which compares against a value already produced by `HermesYAML.boolishValue` at `:213`, so it is correct by construction.
- **P28's iOS quick-edit fix (xphase H2) is real and complete.** `SettingSpec.Kind.primedScalar:207-222` returns `""` for an empty `currentValue` (never `options.first`); `save():154-160` no-ops when `stringValue == primedValue`; `resolved(capabilities:):238-249` injects the `[""] + HermesApprovalMode.options` sentinel row with the host-default label, and `SettingsView.swift:91` actually passes `spec.resolved(capabilities: caps)` to the sheet. `HermesApprovalMode.options` is `["manual","smart","off"]` = `_VALID_MODES` at `tools/approval_context.py:197` — the phantom `auto`/`yolo` set is gone. `agent.max_turns` primes `Int("Unlimited") ?? 0` → 0, in range on v0.20.5+ (`0...1000`), and `displayMaxTurnsText` never returns "Unlimited" below that floor.
- **No write-back from any Settings control.** `PickerRow:211-219`, `ToggleRow:245-249` and `StepperRow:290-294` all build `Binding(get:set:)` with no `onAppear`/`onChange`; there is no bulk-save path — each interaction is one `hermes config set` through the serialised `writeChain`, which hops off the main actor (`enqueueConfigWrite:249-266`, C10) with an explicit 60 s bound (`configCommandTimeout:22`). `setApprovalMode:537-540` no-ops on the sentinel while `setReasoningEffort:518` deliberately writes the empty scalar — correct, since `parse_reasoning_effort` returns `None` for `""` and for absent alike (`hermes_constants.py:876-889`, all three cited lines `:873`/`:876`/`:885` exact).
- **`WebToolsBackendRoster`'s table is exact, and its widen-for-current hatch is reachable.** Re-derived from `git ls-tree <tag> plugins/web/` at every tag: `brave_free`+`ddgs` first at v2026.5.16 (**0.14.0**), `xai` at v2026.5.28 (**0.15.0**), `keenable` at v2026.8.19 (**0.20.5**), `perplexity` at v2026.9.7 (**0.21.1**), and `tavily` absent at v2026.8.31 (0.21.0) **only** — a window, not a floor, and `finalize:115-117` prunes it in place unless the config names it. `config_defaults.py:350,351,352` (`backend`/`search_backend`/`extract_backend` = `""`) are exact, and the `editorStyle:91-98` hatch takes the stored override keys, so an undetected host with either set gets the split editor rather than the lossy shared row. The footer's keyless-ring copy matches `config_defaults.py:355-356`.
- **TTS/STT rosters match Hermes exactly at target and are correctly floor-gated.** `BUILTIN_TTS_PROVIDERS` is at `tools/tts_command_provider.py:269` (11 names) and `BUILTIN_STT_PROVIDERS` at `tools/transcription_common.py:45` (8) — both citations exact, both rosters now complete for the names Scarf can express. `elevenlabs`/`deepinfra` join `BUILTIN_STT_PROVIDERS` at **v2026.7.20** and are absent at v2026.7.7.2 — `hasElevenLabsDeepInfraSTT`/`hasDeepInfraTTS` at 0.19 is right. `xai` and `local_command` are documented omissions.
- **The rest of the platform read-path and enum work holds.** `sharedPlatformScalar:87-110` implements `platform_section`'s replace-the-bridge-source precedence (top-level block wins outright, then `platforms.<p>.extra`, then `gateway.platforms.<p>.extra`) with PyYAML's "is a dict" test; `_slack_require_mention` is at `plugins/platforms/slack/adapter.py:5917-5926` exactly as cited, defaulting gating ON; `permanent_allowlist` is gone from the whole tree and `command_allowlist` is the only spelling left (`tools/approval.py:327-333`); `human_delay` mode/min/max = off/800/2500 matches `gateway/platforms/base.py:3581-3592`; `agent.service_tier`'s `"normal"` default is semantically identical to Hermes's `""` (both "off" per `config_defaults.py:119-121`) and `HermesServiceTier.normalize` folds both.
- **`ProfileRoutesSection`'s user-facing specificity copy is exact:** `2*guild + 4*chat + 8*thread` at `gateway/profile_routing.py:65`, ties kept by Python's stable `routes.sort(..., reverse=True)` at `:143`; `enabled: bool = True` at `:60`; the always-quote-ids rationale at `:96-101`.
- **`YAMLLineEndings` / `normalizedRoundTrip` do what P19 claims:** BOM stripped once and re-prefixed (`GatewayConfigWriter.swift:355-361`), per-line terminators restored by greedy re-sync with a dominant-ending fallback, pure-LF files taking the byte-identical fast path.
- **P20's sentinel tests are revert-proof**, not output-shaped: `HermesP20ConfigDefaultsTests.swift` pins every default against a bare config AND each resolver at the tag above and below its floor, at the floor's predecessor, and at `.empty` (undetected) — including `displayApprovalMode(.empty) == nil` and all three host-default labels.

#### Product decisions

1. **Lowering `max`/`ultra`'s floor (M2) knowingly changes what a pre-target host renders, which C1 forbids.** Re-flooring `max` to v0.18.1 and `ultra` to v0.19.0 adds a picker row on 0.18.1–0.19.1 hosts that the previous Scarf release did not show. The divergence is in the permissive direction (the host genuinely accepts the value), but it is still not byte-identical. Alan's call: re-floor to the tagged truth, or keep the v0.20 gate and fix only the doc comment to say the floor is deliberately conservative.
2. **Refuse vs. reshape for a control character in a user-typed scalar.** `GatewayConfigWriter` *refuses* a line break and surfaces the reason; `HermesFileService.yamlScalar` silently double-quotes and escapes it; `HermesBotProfileYAML` silently double-quotes it; `ProfileRoutesWriter` does neither. Unifying these on `YAMLScalar.quoteIfNeeded` (H1/H2/M1) picks "reshape silently" for all four. Worth deciding whether a tab or newline in a route Name / bot Title should instead be a visible validation error in the editor, the way `MCPServerEditorViewModel.duplicateKey` already is.
3. **Whether Settings should read `.env` for the two env-overridden keys (L9).** Modelling `GATEWAY_MULTIPLEX_PROFILES` and `SLACK_REQUIRE_MENTION` makes those rows truthful on hosted/provisioned deployments but adds an `.env` read to the Settings load path and a third "in effect" spelling to explain in the UI.Verified enough. Here is the report.

### CLI outcome verdicts / Skills / Plugins / Health / Gateway / MCP

#### HIGH

- **HIGH · PRE · A refused `pairing revoke` still deletes the row.** `_cmd_revoke` is a plain `-> None` that prints `User <id> not found in approved list for <platform>.` and returns; `pairing_command` returns `None` → exit 0. Scarf's arm is `if result.exitCode == 0 { self.approvedUsers.removeAll { $0.id == user.id } }`, so a revoke Hermes refused makes the row vanish with no error until the following `load(force: true)` puts it back — the comment two lines above (“Only drop the row when the CLI agreed — the optimistic removal it replaced hid a failed revoke behind a vanished row until the next load put it back”) describes a fix the exit code cannot deliver (C5). The honest markers exist: `Revoked access for user <id> on <platform>.` (`pairing.py:88`) vs `User … not found in approved list` (`:90`) · `scarf/scarf/Features/Gateway/ViewModels/GatewayViewModel.swift:533-556` (esp. `:544-549`) · `hermes_cli/pairing.py:84-90`, `:3-19` @ v2026.9.7
- **HIGH · PRE · A refused `pairing approve` reports success silently.** Same `-> None` shape: an expired/unknown code prints `Pairing request or code '<code>' not found or expired for platform '<platform>'.` (`pairing.py:80-81`) and a rate-limit lockout prints `Platform '<p>' is locked out after too many failed approval attempts.` (`:76-78`) — both at exit 0. Scarf sets `actionFailed = false` and posts **no message at all**, so the user clicks Approve, sees nothing, and the pending row simply stays. Success marker is `Approved! User <display> on <platform> can now use the bot~` (`:68`) · `GatewayViewModel.swift:510-531` · `hermes_cli/pairing.py:56-81`
- **HIGH · NEW (P21) · `skills update <name> --force` is still judged by its exit code — the twin P21 left behind.** P21 replaced `finishUpdateAll`'s exit-code verdict with `installedCount`/`attemptedCount`, but `finishForceUpdate` is untouched: `if exitCode == 0 { hubMessage = "Updated \(name) (local edits discarded)" }`. `forceUpdateArgs` → `["skills","update",name,"--force"]` → `do_update(name, force=True)`, which is `-> None` (`skills_hub.py:831-832`) and whose nested `do_install` is also `-> None` — a blocked scan verdict prints `Installation blocked: …` and returns at exit 0. So the one action that *destroys the user's local edits* announces that it succeeded when nothing was written. `HermesSkillsUpdateReport` is already the right input and `updateAll()` already parses it · `scarf/Packages/ScarfCore/Sources/ScarfCore/ViewModels/SkillsViewModel.swift:818-827, 1021-1033` · `hermes_cli/skills_hub.py:831-832, 696-701, 714-720`

#### MED

- **MED · NEW (P21) · `parseUpdateReport.failureDetail` is poisoned on every real update, so a failed update quotes a benign warning instead of the reason.** `do_update` calls `do_install(..., force=True)` (`skills_hub.py:868`), and `do_install` prints `Warning: '<name>' is already installed at <path>` **unconditionally** when the lock has an entry — which it always does for an update — and only *then* checks `if not force` (`:680-685`). `is already installed at` is in `skillsInstallFailure`, and `failureDetail` takes the **first** match, so `finishUpdateAll`'s attempted arm renders `Update attempted — Warning: 'reddit' is already installed at research/reddit` rather than the `Installation blocked: …` that actually stopped it. The P21 test fixtures omit that line entirely, so they assert on output the emitter cannot produce · `scarf/Packages/ScarfCore/Sources/ScarfCore/Parsing/HermesSkillsHubParser.swift:295-298`, consumed at `SkillsViewModel.swift:1005-1007`; fixture `scarf/Packages/ScarfCore/Tests/ScarfCoreTests/HermesCLIVerdictP21Tests.swift:156-168` · `hermes_cli/skills_hub.py:680-685, 868`
- **MED · NEW (P21) · `pluginsUpdateFailure`'s bare `"Error:"` + `failureWins: true` lets plugin-controlled text turn a completed update into a reported failure.** Every refusal `cmd_update` can reach goes through `_fail` → `sys.exit(1)` (`plugins_cmd.py:80-83, 809`), so the exit code already catches them and the only exit-0 failure is `capabilities NOT granted`. Meanwhile `cmd_update` prints `format_scan_report(scan_result)` over the freshly pulled tree (`:844`, via `_rescan_after_update` at `:819`) and echoes the raw `git pull` output (`:829`) — both before/around the success lines at `:826`/`:828`. A finding quoting `Error:` out of a plugin's own source, or a commit message containing it, is matched as a bare substring and `failureWins: true` makes it decisive. This is exactly the asymmetry P21 fixed on the success side (anchoring) and left open on the failure side · `scarf/Packages/ScarfCore/Sources/ScarfCore/Services/HermesCLIOutcome.swift:341-344`; call site `scarf/scarf/Features/Plugins/ViewModels/PluginsViewModel.swift:330-337` · `hermes_cli/plugins_cmd.py:80-83, 809, 819, 826-829, 844`
- **MED · NEW (P22) · `ProcessTimeout`'s overrun path re-introduces the unbounded wait it exists to remove, and its doc comment overstates the guarantee (C2).** `waitUntilExit(timeout:)` polls to the deadline, then does `terminate(); waitUntilExit()` — a bare, unbounded wait. A child that ignores SIGTERM, or one wedged in an uninterruptible kernel sleep (the "stuck NFS/FUSE mount" the comment itself names as the motivating case), blocks that call forever, so the overrun arm is only *bounded* for a child that would have been easy anyway. The doc promises "`false` after an overrun, in which case it has been sent SIGTERM and **reaped** — the caller never leaves a runaway child behind", which is not what the code guarantees. The P22 test drives `sh -c "sleep 30"`, which obeys SIGTERM, so the arm is untested; `sh -c 'trap "" TERM; sleep 30'` would hang it. Needs a second bounded poll plus SIGKILL · `scarf/scarf/Core/Models/ProcessTimeout.swift:16-28`; test `scarf/scarfTests/MainActorSpawnDisciplineP22Tests.swift:338-351`; only ad-hoc consumer `scarf/scarf/Features/Health/ViewModels/HealthViewModel.swift:1199`

#### LOW

- **LOW · NEW (P21) · Seven stale `skills_hub.py` citations, in three files, all introduced by P21 and missed by P26's sweep** (P26 corrected only the `do_install` block). At v2026.9.7: `No updates available.` is **:849**, not `:831` (`:831` is `def do_update`); `Updating:` is **:864**, not `:834`; the `Updated N skill(s).` print is **:872** (the `if` is `:871`); `def do_audit` is **:879-880**, not `:878`; `No hub-installed skills to audit.` is **:887**, not `:886`; `Auditing <n> skill(s)...` is **:893**, not `:891`; the unknown-name `_print_error` is **:891**, not `:890`. (All four strings *are* byte-identical across all 32 `v2026.*` tags — I walked them — so only the offsets are wrong.) · `HermesCLIOutcome.swift:353-355, 363, 366, 370`; `SkillsViewModel.swift:650-653, 909, 976-984`; `HermesSkillsHubParser.swift:273, 280, 359, 366, 369`; test comments `HermesCLIVerdictP21Tests.swift:150, 186, 213-215`
- **LOW · NEW (P21) · `pairing.py` hint-line citation is off by one, in three places.** The two hints are at **:39-40**, not `:38-39` (`:38` is the closing paren of the pending-row `print`). The approved-row `print` is **:49**, not `:48` (`:48` is the `for a in approved:`) — though the paired `v2026.6.19:57` cite *is* correct · `GatewayViewModel.swift:377, 395, 407, 426` · `hermes_cli/pairing.py:39-40, 49`
- **LOW · NEW (P24) · The transport discriminator tests `transport: sse` before `url`, but Hermes gates on `url` first.** `_is_http()` is `"url" in self._config` (`tools/mcp_tool_health.py:27`), and `:412`'s `config.get("transport") == "sse"` is only reached on the HTTP path; Hermes's own status payload reports `"stdio"` for any url-less entry regardless of `transport:` (`tools/mcp_tool_discovery.py:484`). Scarf's closure returns `.sse` for a url-less `transport: sse` entry, so it renders a transport the host does not run and the editor then offers SSE-only fields — the same class of divergence P24's comment says it closed. One-line reorder · `scarf/scarf/Core/Services/HermesFileService.swift:1135-1139` · `tools/mcp_tool_health.py:27`, `tools/mcp_tool_discovery.py:484`, `tools/mcp_tool_transport.py:412`
- **LOW · NEW (P24) · `loadMCPServers` now costs up to 2 remote `test` round trips per MCP server.** `basenames(for:)` returns two spellings whenever the name needs sanitizing, and each is probed with its own `transport.fileExists`. On a remote context with a dozen servers that is ~24 serialized SSH round trips inside one load (detached, so not a C10 violation, but the spinner). One directory listing of `mcp-tokens/` would answer every server at once · `scarf/scarf/Core/Services/HermesFileService.swift:321-323`
- **LOW · NEW (P24) · Dead residue from the `sse_read_timeout` removal.** `HermesMCPServer.sseReadTimeout` is still parsed (`HermesFileService.swift:1150`) and threaded through two initializers, but nothing renders or writes it, and the writers are line-level patchers so the key round-trips regardless of the model field. The four localization entries (`SSE read timeout`, its placeholder and two footers) are also orphaned · `scarf/Packages/ScarfCore/Sources/ScarfCore/Models/HermesMCPServer.swift:67-80`, `scarf/scarf/Localizable.xcstrings:27767, 92886-92929`
- **LOW · PRE · `HermesPluginList`'s doc cites v2026.8.31 and quotes a `_plugin_status` that no longer exists.** At v2026.9.7 it is a one-line ternary over a `names = {name, key}` set, not the three-branch `if` chain the comment presents as a verbatim Python block — so the block reads as a quote and is a paraphrase of a two-release-old tag (C2). The behaviour and the `cmd_list` JSON shape are both still correct · `scarf/Packages/ScarfCore/Sources/ScarfCore/Parsing/HermesPluginList.swift:5-16, 44-53` · `hermes_cli/plugins_cmd.py:1290-1293, 1312-1331`
- **LOW · PRE · `dashboardListenerPID` waits for the child before draining its pipe.** `lsof.run()` → `waitUntilExit(timeout:)` → `readDataToEndOfFile()`. Safe for `lsof -t` (a few bytes), but it is the pipe-deadlock shape, and on an overrun the data is read from a pipe whose writer was SIGTERMed · `scarf/scarf/Features/Health/ViewModels/HealthViewModel.swift:1193-1206`
- **LOW · PRE · Missing `--` on five argv sites that have one on their siblings.** `skills install`/`skills uninstall`/`mcp login` all end options with `--`; `plugins update|remove|enable|disable <name>`, `skills update <name> --force`, and `pairing approve|revoke <platform> <id>` do not, so a name/id beginning with `-` exits 2 at argparse. Positionals verified present and unambiguous at `hermes_cli/subcommands/plugins.py:52-85`, `subcommands/skills.py:79-83`, `subcommands/pairing.py:17-26` · `PluginsViewModel.swift:334, 340, 366, 390`, `SkillsViewModel.swift:793-795`, `GatewayViewModel.swift:517, 540`

#### Verified sound

- **Every marker string in my surface re-read byte-for-byte at v2026.9.7**, and the four `skills_hub` strings (`No updates available.`, `Updating:`, `No hub-installed skills to audit.`, `Auditing `) walked across **all 32 `v2026.*` tags** — identical at every one, so the C1 "judged the same on a pre-target host" claims hold even though the offsets do not. `Installed:` (`:720`), `Uninstalled '<n>' from <p>` (`skills_hub_install.py:220` via `_report_pair:144-150, 917`), every `skillsInstallFailure` entry (`:134-135, 498, 506, 520, 525, 537, 582, 592, 642, 682, 684, 699`), both `mcpLoginSuccess` lines (`mcp_config.py:695, 697` under `_success:34`) and all six `mcpLoginFailure` refusals (`:104, 631, 634, 641, 677, 705`), all four plugins enable/disable/update success+failure markers (`:1005, 1012, 1023, 1033, 1092-1098, 1187, 1191, 1198, 826, 828`), and both `pairing list` hint strings.
- **P21's `was removed.` floor walk independently re-derived**: the string lives only inside `_refuse_legacy_relay`, which is *defined inside* `cmd_enable` and called only from it (`plugins_cmd.py:996-1002`, called `:1002`, `:1007`); `cmd_disable` is `:1182`. Removing it from `pluginsDisableFailure` is right, keeping it on `pluginsEnableFailure` is right.
- **Anchoring is applied at every emitter that prints at column 0** (install, uninstall, audit, mcp login) and correctly *not* applied to the `plugins` markers, whose text is a mid-sentence clause. `unglyphed` covers `✓ ✗ ⚠ ⊘ •` — the exact glyph set `mcp_config.py:34-36` and `plugins_cmd.py:1198` emit. `stripANSI`'s pattern is now lexer-interpolated (`"\u{1B}\\[…"`); `SkillsViewModel.firstSignificantLine`'s surviving `#"\x1B\[…"#` raw string is *not* the same bug — `\x1B` is a real ICU escape.
- **`security audit` is correct end to end.** Every cite resolves exactly: `SEVERITY_ORDER:29`, `_render_human:253-269` (heads `:255`/`:257`, row `:264`), `cmd_security_audit:286-312` (`:293`, `:299-301`, `:307`, `:311-312`), `main.py:2074-2075`, `subcommands/security.py:26`'s `default="critical"`. The three-way verdict, the `.clean`-with-findings label, and the `<tier> … ==` row discriminator all match the emitter; `MEDIUM` is genuinely OSV's alias for `MODERATE` at the same rank. The round-2 LOW about the stale `hermes audit` header is fixed (`HealthViewModel.swift:88, 90`).
- **`mcp login` drain choreography is sound, including P28's second pass.** Handlers installed before `proc.run()`; `readabilityHandler` deliberately left live on termination; verdict gated on EOF **and** exit; per-run `OutputInbox` under an `NSLock` so unordered `Task { @MainActor }` hops cannot interleave text; 2 s grace deadline for a pipe held by a grandchild; generation-mismatch branch in the spawn continuation unhooks the retired reader, terminates the child and reaps the remote half; the post-spawn handle publish is skipped when `didFinish`. `stop()`'s `wasRunning == false` hole in the spawn window is covered by that same branch. `loginOutcome`'s `fallbackDetail: false` is right — the refusal arms print the config.yaml sample (`:685-692`) and a `Then re-run …` hint (`:692`) *after* the reason.
- **P24's OAuth path port is exact.** `_safe_filename` (`mcp_oauth.py:104-106`), the four suffixes and their `remove()` order (`:284-290, 391-394`), `remove_oauth_tokens` (`:690-693`). The Swift port iterates unicode *scalars*, tests `generalCategory` in `L* ∪ N* ∪ _`, strips before slicing to 128 scalars, and falls back to `default` only on an empty result — all four match CPython. The legacy raw-name fallback is correctly dropped for any name carrying `/`, `\`, `.` or `..`, and detection is broad while deletion is narrow.
- **Device-prompt parsing**: `\r\n` normalised *before* the split (the grapheme-cluster trap), unterminated-final-line tolerated only for the sentinel, sentinel verbatim against the single `print` at `tools/mcp_oauth_device.py:126-127`; the `:124-125` deadline and `:132-144` poll-loop cites both resolve.
- **`_parse_boolish` type gate ported correctly**, including the inversion: `resolvesToNonString && !resolvesToBool → nil` runs on the raw scalar before `unquote`, so bare `0`/`1` fall to the host default while `"0"` matches. `tools/mcp_tool_common.py:120-121, 124-137` verified, `ssl_verify` correctly excluded.
- **`pkill` reap**: `-u` probed via `id -u` over the same transport and validated as ASCII digits, reap abandoned if the probe fails, ERE escaping complete for POSIX, and the `mcp login .*-- <name>$` pattern verified to match both the flow and no-flow argv shapes.
- **C10/C5 elsewhere in the surface**: `GatewayViewModel.load()` is one `Task.detached` over the whole body with live `Task.isCancelled` checks, `loadGeneration` and `inFlightLoadGeneration` are genuinely two tokens, `cancelLoad()` both cancels and retires the result, and `GatewayView.attachCapabilitiesIfNeeded` calls it before replacing the VM. All five gateway mutations and `HermesGatewayListService.fetch` now take a named timeout and a runner seam (with the documented reason production keeps the stdout-only transport call). `SkillsViewModel` is `@MainActor` on the whole type with the off-main work in `nonisolated static` helpers. `launchDashboard`/`stopDashboard` are detached and `lsof` is bounded. `MCPServersViewModel.load` is detached.
- **argv re-verified against the tagged argparse (C5)**: `skills install|update|audit|uninstall` (`subcommands/skills.py:54-92`, incl. `--force` on update and the absence of `--yes` there), `plugins install|update|remove|enable|disable|list` (`subcommands/plugins.py:18-85`), `mcp login` with `name` + `--flow {browser,device}` (`subcommands/mcp.py:55-60`), `gateway list` with no arguments (`subcommands/gateway.py:108`), `pairing list|approve|revoke` (`subcommands/pairing.py:15-26`), `security audit --fail-on` (`subcommands/security.py:21-33`). **`--flow`'s v0.21.1 floor re-walked across all 32 tags** — absent at v2026.8.31 and every tag below, present only at v2026.9.7.
- **P25/decision 2 landed**: iOS `UpdatesView:32-33, 89-97` renders `updateFaults` in parity with `SkillsView:845-846, 894-902`, and the Skills button, tooltip, accessibility label, banner copy (`Skills re-scanned` / `Re-scan failed`) and doc comment all say "re-scan"; the `gateway/slash_commands.py:1038-1048` `/reload-skills` cite resolves.

#### Product decisions

1. **What a partly-refused `skills update` should quote.** Fixing the `failureDetail` poisoning (MED above) needs a rule: suppress `is already installed at` from the failure-marker set *for the update path only* (it is load-bearing for a plain `skills install`), or quote the **last** refusal rather than the first, or list one reason per attempted skill. I'd suggest the first, but it means `skillsInstallFailure` stops being one shared list.
2. **Whether a refused `pairing approve`/`revoke` should be a sticky error or a quiet no-op.** Both now need an output verdict; the question is presentation. `revoke` has a clean binary (`Revoked access for user` vs `not found in approved list`), but `approve` has *three* outcomes — approved, not-found/expired, and rate-limit lockout with a "clears in ~N minute(s)" hint and a manual-reset path. Surfacing the lockout text as-is is long; summarising it loses the remediation. Same trap as the plugins consent sentence.### Capabilities, platform roster, model catalog, drift script

Tag→release map re-derived independently across all 32 `v2026.*` tags (`^version` in `pyproject.toml` at each tag) and agrees with the branch's map exactly.

#### HIGH

- **HIGH · NEW (P23) · P23 replaced one wrong `/compress` spelling with the other wrong one, on the half of the window Scarf actually has users in.** P23 deleted `hasCompressCommand` and hardcoded `compress` on the strength of `hermes_cli/commands.py` — the **CLI/TUI** command table. Scarf's chat composer does not talk to that table; it talks to the **ACP adapter**, whose slash set is a separate, smaller dict. Walked across all 32 tags: `acp_adapter/server.py` `_SLASH_COMMANDS` carries `"compact": "Compress conversation context"` from v2026.3.17 (0.3.0) through **v2026.7.20 (0.19.0)** (`acp_adapter/server.py:459` @ v2026.7.20) and flips to `"compress"` at **v2026.7.30 (0.19.1)** (`acp_adapter/server.py:574,603,2051` @ v2026.7.30), staying there to v2026.9.7 (`acp_adapter/commands.py:50`). There is no alias either way — `_handle_slash_command` is `if cmd not in self._COMMANDS: return None` and its own docstring says unknown commands "fall through to the LLM" (`acp_adapter/commands.py:88-95` @ v2026.9.7; same shape at `acp_adapter/server.py:1743-1748` @ v2026.7.20). So on every 0.6.0–0.19.0 host — thirteen of the sixteen supported releases — Scarf's compress gesture now sends `/compress`, nothing compresses, and a turn is burned prompting the LLM with the literal text. Before P23 those hosts got `/compact`, which is correct there. **The original flag was off by one release (0.19.1, not 0.20.0); P23's removal is a regression on the larger half of the window.** · `RichChatViewModel.swift:737-755` (and the `hasCompressCommand` obituary at `HermesCapabilities.swift:845-858`, whose whole argument cites the wrong file) · `acp_adapter/server.py:459` @ v2026.7.20 vs `:574` @ v2026.7.30. The test that should have caught it asserts the bug: `compressSlashCommandIsCapabilityFree` (`HermesCapabilitiesTests.swift:1389-1404`) pins `/compress` for a v0.12 host.

- **HIGH · NEW (P25) · `hasSessionsExportNoRedact` is floored three minor releases too high, and its doc comment asserts a 32-tag walk that did not find what it says.** The flag reads `isV0211OrLater` and the comment states "argparse registered no such option there… A walk of all 32 v2026.* tags puts the first registration at v2026.9.7." False. `sessions_export.add_argument("--no-redact", action="store_true", …)` is `hermes_cli/main.py:13567` at **v2026.7.7 (0.18.1)** — absent at v2026.7.1 (0.18.0) — and is present at every tag since, including the one the comment singles out (`hermes_cli/main.py:14245` @ v2026.8.31). It is functional there too: `redact_trace = not getattr(args, "no_redact", False)` at `hermes_cli/main.py:13944` @ v2026.7.7. Consequence: on 0.18.1–0.21.0 hosts the "Redact secrets" checkbox is force-disabled and the sheet tells the user the flag is an argparse error, so ten releases that support opting out cannot. · `HermesCapabilities.swift:1475-1492`; consumer `SessionsView.swift:36-41` · `hermes_cli/main.py:13567` @ v2026.7.7. **The tests encode the false claim rather than catching it** — `HermesCapabilitiesTests.swift:1104-1107` comments "registered for the first time at v2026.9.7… a 0.21.0 host exits 2 on the option" and asserts `!caps.hasSessionsExportNoRedact` at 0.21.0; correcting the floor makes that test fail.

- **HIGH · PRE · Seven of Scarf's slash-menu rows are names the ACP adapter has never dispatched — the exact class P23 existed to close, left standing in the function P23 edited.** The ACP slash surface at v2026.9.7 is exactly nine names: `help, model, tools, context, reset, compress, steer, queue, version` (`acp_adapter/commands.py:44-66`, handlers `:116-291`; `_available_commands` at `:71-74` advertises only those). `alwaysAvailableCommands` offers `clear`, `cost`, `reload-skills`, `exit` unconditionally plus gated `yolo`, `sessions`, `codex-runtime`. None is in `_COMMANDS`, so all seven fall through to the LLM as prose. `/cost` is the starkest: walked across all 32 tags, **no `CommandDef("cost", …)` has ever existed** — the real command is `CommandDef("usage", …)` (`hermes_cli/commands.py:277` @ v2026.9.7) — so "Show cost breakdown for this session" is a dead row on every host Scarf has ever shipped against. `clear` and `exit` exist only as `cli_only=True` terminal commands (`commands.py:58-59`, and `exit` is merely an alias of `quit` at `:302-303`), and `reload-skills` (`:259-260`) is a CommandDef the ACP adapter does not wire. Meanwhile three real ACP commands — `reset`, `context`, `version` — are absent from Scarf's fallback list entirely, so a resumed session (the case the fallback exists for) offers four dead rows and hides three live ones. `hasYOLOSlashCommand`'s doc comment flatly says "Available in ACP" (`HermesCapabilities.swift:330-334`) — `yolo` appears nowhere under `acp_adapter/` at any of the 32 tags (C2). · `RichChatViewModel.swift:730-819`, `HermesCapabilities.swift:330-347`.

#### MED

- **MED · NEW (P23/P28) · `selected` / `selectedPlatform` is never reconciled against the narrowed roster, so P28's "show every row until the read lands" opens a window in which a sub-floor form stays open and writable.** Both surfaces now render every row until `hasLoadedConfiguredPlatforms` / `hasLoadedPlatforms` flips. A user who clicks `ntfy` on a 0.14 host in that window keeps it: `PlatformsView`'s detail pane switches on `viewModel.selected.name` with no visibility check and renders `NtfySetupView`, whose Save writes `platforms.ntfy.*` for an adapter the host lacks; the List selection binding (`:78-85`) can only *set* from `visiblePlatforms`, so nothing ever clears the stale selection. Same shape in Tools, where it is worse because `toggleTool` shells `hermes tools enable … --platform <name>` (C5) — the precise failure P28's note says the one-seam refactor closed. · `PlatformsView.swift:130,174-201` and `:30-41`; `ToolsView.swift:19-23,90-101`, `ToolsViewModel.swift:43-46,55`.
- **MED · NEW (P23) · `hasGeminiKittenTTS`'s floor is right but its grounding is false, in the specific way the charter's C2 exists to stop.** The doc says gemini/kittentts "appear in `BUILTIN_TTS_PROVIDERS` from its very first tagged form — v2026.4.23 (v0.11.0) `tools/tts_tool.py`", and the MARK block repeats it as a "first set". Walked all 32 tags: `BUILTIN_TTS_PROVIDERS` does not exist at v2026.4.23 — its first tagged form is `tools/tts_tool.py:316` @ **v2026.4.30 (0.12.0)**. The 0.11.0 floor is nonetheless correct, on different evidence: the provider *dispatch* arms `elif provider == "gemini"` / `== "kittentts"` are `tools/tts_tool.py:1024,1038` @ v2026.4.23, and `tools/tts_tool.py` does not exist at all at v2026.4.16 (0.10.0). A floor whose cited constant postdates it by a release cannot be re-verified from the comment. · `HermesCapabilities.swift:788-819`.
- **MED · NEW (P23) · Three new flags ship with no boundary tests, against the charter guardrail that every parity cluster adds parse / all-on / degradation / patch-still-on coverage.** `hasGeminiKittenTTS`, `hasElevenLabsDeepInfraSTT` and `isV011OrLater` have zero test references repo-wide; their only consumers are `SettingsViewModel.swift:91,131` and `HermesConfig.swift:1523`. (Both floors are independently correct: `_BUILTIN_NAMES` gains `elevenlabs`/`deepinfra` at `agent/transcription_registry.py:40-49` @ **v2026.7.20**, has neither at v2026.7.7.2; `gateway_notify_interval` 600→180 at v2026.4.23.) · `HermesCapabilitiesTests.swift`.
- **MED · NEW (P23) · `sessionsRenameIsUngated` would pass unchanged if the removal were reverted.** Its body asserts `!v012.hasSessionsOptimize` and `v012.detected` — nothing about rename, and nothing about the consumer. The doc comment claims it "pins the absence of a gate"; re-adding `hasSessionsRename` at a v0.16 floor and re-wrapping the `ChatSessionListPane` menu item would leave the suite green. The compile-time argument the comment offers covers only the symbol's existence, not the consumer's unconditionality. · `HermesCapabilitiesTests.swift:1366-1382`.
- **MED · NEW (P23) · Four of the twelve gated roster rows break the rule the phase's own decision records, and none of the four is tested.** The P23 note states "Each floor is now a shared `static let …PlatformFloor` so the roster row and its `has…Platform` flag cannot drift". `irc` (0.12), `weixin` (0.9), `qqbot` (0.10) and `msgraph_webhook` (0.14) carry inline `.init(major:…)` literals instead, and `HermesP23RosterAndGateTests.parityCycleRowsCarryTheirVerifiedFloors` omits all four (only `irc` appears, incidentally, in the v0.12 visibility test). All four floors do check out — `plugins/platforms/irc/` @ v2026.4.30 (absent v2026.4.23), `gateway/platforms/weixin.py` @ v2026.4.13 (absent v2026.4.8), `gateway/platforms/qqbot.py` @ v2026.4.16 (absent v2026.4.13), `gateway/platforms/msgraph_webhook.py` @ v2026.5.16 (absent v2026.5.7) — so this is drift risk plus an overstated record, not a wrong gate. · `HermesTool.swift:196,198,199,200`; `HermesP23RosterAndGateTests.swift:29-49`.

#### LOW

- LOW · NEW (P23) · `photonPlatformFloor`'s doc says it is shared with "`HermesToolPlatform.known`'s `photon` row" — there is no `known` symbol; the accessor is `KnownPlatforms.all` (the nine sibling floor docs all say so correctly). Same stale name at `HermesTool.swift:202-203`. · `HermesCapabilities.swift:709`
- LOW · NEW (P23) · `/compact`'s TUI meaning is cited one line high in both copies of the argument: `_TUI_EXTRA`'s declaration is `tui_gateway/server.py:3845` @ v2026.4.30 but the `("/compact", "Toggle compact display mode", "TUI")` tuple is at **:3846** (which is what round 2 cited). · `HermesCapabilities.swift:855`, `RichChatViewModel.swift:746`
- LOW · NEW (P25) · `hasSessionsExportNoRedact` cites `hermes_cli/sessions_cmd.py:394` for `redact_trace = not args.no_redact`; the statement is at **:395** (:394 is the `from agent.trace_upload import …` line). Round 2 raised this same off-by-one for `SessionsViewModel`; the capability file acquired a second copy of it. · `HermesCapabilities.swift:1479`
- LOW · NEW (P27) · `parse_models_dev_map`'s docstring calls `PROVIDER_TO_MODELS_DEV` "a plain annotated dict literal at v2026.9.7:107" — :107 is the comment line, the assignment is **:108**. · `scripts/check-hermes-tables.py:221-222`
- LOW · NEW (P23) · `availableCommands`' fallback comment still enumerates "`/clear`, `/compact`/`/compress`, `/cost`, …" — the `/compact` branch it describes was deleted 200 lines above, in the same commit. · `RichChatViewModel.swift:961`
- LOW · NEW (P26) · `overlayMetadata`'s new doc offers `ai-gateway` as an alias the canonical-fallback now resolves; it does not — `ai-gateway` canonicalises to `vercel`, which is not an `overlayOnlyProviders` key, so that spelling still returns nil. `grok-oauth` → `xai-oauth` is a genuine example. · `ModelCatalogService.swift:181-188`
- LOW · PRE · `parse_hermes` keeps one silent-substitute path the P27 hardening closed everywhere else: if `ALIASES` is present but is neither an `ast.Dict` nor the `_ALIAS_GROUPS` comprehension, the `elif name == "ALIASES"` arm falls through and `aliases = alias_groups` quietly supplies a different table's contents. Lane 5's equivalent hole is unit-tested five ways (`test_dict_call_…`, `test_dict_comprehension_…`, `test_renamed_table_…`); lane 1's has no test at all. · `scripts/check-hermes-tables.py:194-210`; `scripts/tests/test_check_hermes_tables.py:59-106`
- LOW · NEW (P27) · The suite's own "a skip is not a pass" rule is not applied to itself: ten of thirteen tests sit behind `@unittest.skipUnless(_target_tag_available())` and a machine without the hermes checkout prints `OK`. · `scripts/tests/test_check_hermes_tables.py:198-199,107-110`

**README's Hermes-target lines — confirmed stale, every one:**
- `README.md:24` — badge `Hermes-v0.20` (target is v0.21.1).
- `README.md:46` — "Hermes v0.6 through v0.20 all work".
- `README.md:132` — "Current target: **v0.20.4 "Herald"** (v2026.8.18)" — four releases behind `HERMES_TARGET_TAG = "v2026.9.7"` (`scripts/check-hermes-tables.py:92`).
- `README.md:139` — credits `/compress` to v0.20.0. On the CLI table `compress` is canonical from v2026.3.17 (0.3.0); on the ACP path it arrives at v2026.7.30 (0.19.1). Neither is v0.20.0 (C2).
- `README.md:140` — "v0.20.4 … **Verified — current target**".
- `README.md:134-140` — the compatibility table stops at v0.20.4; no rows for v0.20.5, v0.20.6, v0.21.0 or v0.21.1.

#### Verified sound

- **All twelve platform-roster floors re-walked with `git ls-tree` at the floor tag *and* one release below, all confirmed both directions:** teams + yuanbao v2026.4.30 (absent v2026.4.23), google_chat v2026.5.7 (absent v2026.4.30), line + simplex v2026.5.16 (absent v2026.5.7), ntfy v2026.5.28 (absent v2026.5.16), whatsapp_cloud v2026.6.19 (absent v2026.5.29.2 — and note `photon` ships as `plugins/platforms/photon/`, not `gateway/platforms/photon.py`, while `whatsapp_cloud` is the reverse; both doc comments name the right path), buzz v2026.7.30 = 0.19.1 (absent v2026.7.20), plus the four B4 literals above. Every ungated row's justification holds: dingtalk/sms/api_server v2026.3.23 (0.4.0) and wecom v2026.3.30 (0.6.0) are at or below the supported minimum, and the B4 rows are unreleased (`70c564c5` carries no Scarf tag; latest is v3.1.0), so gating them does not remove a row users already see — decision 6 satisfied.
- **The P23 re-floors re-verified independently at the floor and one below**, matching round 2: `hasCronRuns` 0.19.0, `hasCuratorAdopt`/`hasApprovalsSuggest` 0.19.1, `hasSessionsExportFormats` 0.18.1, `hasCronPauseMarkerGate`/`hasBuiltinPersonalitiesInCode` 0.20.1, the five 0.20.3 curator/skills verbs. The v0.20.4 MARK block's claim that "only `hasMCPIdentityHeader` is still a genuine v0.20.4 floor" is accurate for every member of the group.
- **Fresh walk of three clusters rounds 1–2 never touched — v0.17, v0.18, v0.19 — every floor correct both directions:** `hasMaxConcurrentSessions` 0.17 (absent v2026.6.5), `hasTelegramRichMessages` 0.17 (`gateway/platforms/telegram.py` gains `status_indicator` at v2026.6.19), `hasCuratorConsolidate` 0.17, `hasCronAttachToSession` 0.18.0 (absent v2026.6.19), `hasMCPReauth` 0.18.0 (`hermes_cli/mcp_config.py:952` @ v2026.7.1, absent v2026.6.19), `hasTitleGenerationLanguage` 0.18.0 (`agent/title_generator.py::_title_language` @ v2026.7.1, file absent v2026.6.19), `hasConfigUnset` 0.19.0 (`unset_config_value` in no file at v2026.7.7.2), `hasAuxiliaryReasoningEffort` 0.19.0 (`agent/auxiliary_client.py:6467` @ v2026.7.20, absent v2026.7.7.2). Also confirmed: `hasWebToolsBackendSplit` 0.13 (`search_backend` in no `.py` at v2026.4.30, six files at v2026.5.7).
- **Both new convenience predicates' doc claims are exact at the tag:** `isV0181OrLater` — `"show_reasoning": False` @ v2026.7.1 → `True` @ v2026.7.7; `isV011OrLater` — `"gateway_notify_interval": 600` @ v2026.4.16 → `180` @ v2026.4.23.
- **`parseLine`'s fail-closed bound is correct and its tests are bidirectional and non-vacuous**: `(0...9).contains(major)` rejects the date shape and two-digit majors while `v9.300.4000` and a suffix-less `v0.20.3` still parse; the three tests would all fail with the guard removed. `.empty` needing no new caller case checks out — it is already the failed-probe value.
- **`HermesP23RosterAndGateTests` is genuinely reverting-proof** on the eight parity-cycle floors, the shared-constant identity, the `bluebubbles` + core-roster ungated set, the v0.12 visibility snapshot, the all-visible-at-target sweep, and `isVisible`'s hatch at `.empty` *and* at a real sub-floor.
- **P28's roster-seam remediation holds end to end.** `KnownPlatforms.visible(on:isConfigured:)` is the single seam; both `PlatformsView:30-41` and `ToolsView:19-23` route through it with the has-been-read companion flag; `ToolsViewModel.loadPlatforms` now calls `PlatformsViewModel.computeConfiguredPlatforms` rather than its own `hasSuffix(":")` scan; the nested-block detector accepts `platforms.<n>`, `gateway.platforms.<n>`, a flow map at the path itself and anything beneath, with `ntfy`/`simplex` env arms added and `whatsapp_cloud`'s deliberate absence documented; BOM stripping and `plainKeySeparatorIndex` close the two top-level-scan holes round 2 found. The two ungated accessors (xphase L4) are gone — `KnownPlatforms.all`'s only production callers are the two detectors that must see every row.
- **`WebToolsBackendRoster.editorStyle` is sound and C1-safe.** `.empty` + either override → `.split`; a detected v0.13+ host is always split; a detected v0.12 host with no override keeps the combined row. The "unreachable for any config Scarf could have written" argument holds because the combined branch writes only `web.backend` (`WebToolsTab.swift:107-112`). Three tests cover all four quadrants.
- **Model catalog**: every citation P26 rewrote is exact at v2026.9.7 — `agent/models_dev.py:110` (`"openai-api": "openai"`, so the "Scarf extension" claim was indeed false and the correction is right), `:120` (`"opencode-free": "opencode"`), `:129` (`"meta-ai": "meta"`), `hermes_cli/providers.py:117` (`_ALIAS_GROUPS`) and `:139` (the inverting comprehension). `overlayMetadata`'s raw-then-canonical order matches `providerByID`/`validateModel`. The drift script passes all five lanes against tag v2026.9.7 (`aliases=87 aggregators=8 overlays=25 lanes=5/5`).
- **P27's hardening is real, verified empirically**: `--tag` default reads `git show v2026.9.7:…` (the working tree here is `v2026.9.7-385-g9e6c4100cb` and is not consulted); an absent cache makes lanes 3/4 print `SKIPPED lane N`, `NOT-OK … lanes=3/5` and **exit 2**; `--allow-skip` returns 0 with `partial` in the verdict; an unknown tag exits before reading a blank; `--tag v2026.7.1` correctly produces 12 FAILs and exit 1. `scripts/tests/` runs clean (13 tests) via `python3 -m unittest discover -s scripts/tests -t .`, and the tag-vs-worktree fixture does diverge the two as the note claims.
- **P26's `hasKanbanDiagnostics` rewrite is citation-exact at v2026.9.7**: `kanban_parser.py:251-256` (`diagnostics` subcommand with `--json`), `kanban.py:627-628` (emitter) and `:678-681` (JSON branch), `kanban_parser.py:176-181` (`--max-retries`) inside `create` at `:148`, `kanban_db_dispatch.py:190` (`reap_worker_zombies`). The deleted claims (`hallucination_gate`, `auto_blocked_reason`, "read through `kanban show`") are genuinely absent from the tag.
- **C7 clean** for my surface: no managed-tier path in any of the 26 commits' file lists.

#### Product decisions

1. **`/compress` vs `/compact` on the ACP path.** The spelling genuinely flips at v0.19.1, so a correct gate is a `hasACPCompressSpelling`-style flag floored there — i.e. restoring the flag P23 deleted, at 0.19.1 rather than 0.20.0. The alternative is to declare 0.19.0-and-below compression unsupported and let it fall through. Restoring a just-deleted flag one release lower is a reversal of P23's "a floor below the supported minimum is no floor" reasoning (the floor here is *inside* the window), so it needs your call.
2. **Scope of the ACP slash-command audit.** Fixing `/cost` alone is a one-line deletion; reconciling Scarf's fallback list with the ACP adapter's nine-name surface means dropping `/clear`, `/cost`, `/reload-skills`, `/exit`, `/yolo`, `/sessions`, `/codex-runtime` and adding `/reset`, `/context`, `/version` — a visible change to a menu users know. Do we reconcile the whole list, or only delete the provably-never-existed `/cost` this cycle?
3. **`hasSessionsExportNoRedact` at 0.18.1.** Correcting the floor re-enables the "Redact secrets" opt-out on ten releases, which means Scarf can emit `--no-redact` on hosts it previously never did. P28's reasoning (redaction default ON per format) is unaffected, but the blast radius of un-redacted trace exports widens by ten releases — confirm that is wanted rather than keeping the conservative 0.21.1 gate with an honest comment.
4. **Post-load selection reconciliation.** When the roster narrows after the detached read, should Scarf snap `selected`/`selectedPlatform` back to `cli`, leave the stale form visible but read-only, or keep today's behaviour on the grounds that the window is short? Snapping yanks the pane out from under a user mid-typing; leaving it writable is the C5 hole.iOS target builds clean (`BUILD SUCCEEDED`, 1 warning). Findings:

### Concurrency and spawn discipline (C10), platform-setup forms, iOS twins

#### HIGH

**H1 · The iOS sentinel row has no sentinel-WRITE guard, so selecting "Host default" pins `manual` on a host that runs `smart` — and Scarf then displays `smart` over it.**
P28 gave `SettingEditorSheet` the absent-key sentinel row and the untouched-save guard, but not the Mac's *no-op on the sentinel* rule. `hasValidValue` deliberately returns `true` when `options.contains("")` (`scarf/Scarf iOS/Settings/SettingEditorSheet.swift:112`), and `save()`'s only guard is `stringValue != primedValue` (`:154`). So a user whose config carries `approvals.mode: manual` who selects the "Host default (smart)" row gets `stringValue == ""`, `primedValue == "manual"` → the guard passes → `vm.saveValue(key: "approvals.mode", value: "")` → `hermes config set approvals.mode ''`. The Mac path refuses exactly this: `SettingsViewModel.swift:537-539` — `func setApprovalMode(_ value: String) { guard !value.isEmpty else { return } … }`, with a comment naming the consequence.

The consequence is real at the tag: `_coerce_config_set_value` returns the string verbatim for a str-typed key (`hermes_cli/config.py:3306-3312` @ v2026.9.7), so `approvals.mode: ''` lands on disk; `_normalize_approval_mode("")` falls through `if normalized:` and returns `"manual"` (`tools/approval_context.py:197-214` @ v2026.9.7). Meanwhile Scarf's own reader is `raw.isEmpty ? nil : normalize(raw)` (`HermesConfig.swift:1538-1540`), so the row goes back to rendering **"Host default (smart)"** while the host enforces `manual` — a worse state than the H2 bug P28 set out to fix, because it is now silent. `Scarf iOSTests/SettingsEditorSentinelP28Tests.swift` tests only `primedScalar` and the option/label sets; nothing exercises the write, so this passes. **NEW (P28).** Fix shape: refuse the empty scalar in `save()` (or in `IOSSettingsViewModel.saveValue`) the way `setApprovalMode` does, and pin it with a test that drives `save()` with a stored mode and the sentinel selected.

**H2 · The form choreography proves the `.env` read and not the config.yaml read, so one blipped config.yaml read plus a Save wipes the WhatsApp Cloud credentials out of config.yaml — with no warning.**
`PlatformSetupHelpers.loadForm` takes the *guarded* env read (`loadEnv` → `loadProven`, `PlatformSetupHelpers.swift:37-43`) but the tolerant config read: `snapshot.config = HermesFileService(context: context).loadConfig()` (`:198`) and `snapshot.rawConfigText = context.readText(…) ?? ""` (`:201`). `loadConfig()` returns `.empty` on an unreadable file (`HermesFileService.swift:39`), and `loadConfigResult()` — the proven variant — exists at `:50` and is unused here. `WhatsAppCloudSetupViewModel` is config-ONLY: `loadSnapshot(includeEnv: false)` with `guard let self, let cfg = snapshot.config?.whatsappCloud` (`WhatsAppCloudSetupViewModel.swift:50-51`), and `whatsappCloud` is a non-optional struct (`HermesConfig.swift:1835`), so a blipped read passes the guard with `.empty` and blanks every field. `save()` then writes ten `hermes config set` pairs including `extra.access_token`, `extra.app_secret`, `extra.verify_token` as empty strings plus `platforms.whatsapp_cloud.enabled: false` (`:86-98`). `envFailure` is nil, so no message bar appears and `isBusy` is clear — Save is enabled. This is GW-F6 exactly, reached through the half the P22 choreography did not prove. The `.env` half *is* safe (the guarded writer refuses: `HermesEnvService.swift:133`/`:174`) — which is what makes the asymmetry the bug. Same shape, smaller blast radius, in `SignalSetupViewModel.swift:60` and `EmailSetupViewModel`'s `rawConfigText ?? ""` read. **PRE (newly visible — P22 single-sourced the choreography and documented the proof on only one of its two reads).** Fix shape: give `FormSnapshot` a `configFailure` from `loadConfigResult()` / a guarded `readText`, surface it like `envFailure`, and make `commitSave` refuse when the snapshot was not proven.

#### MED

**M1 · `detectSignalCLI` runs a PATH walk — and can block on the lazy 5–8 s `zsh -l -i` probe — inside the main-actor commit hop of the load P22 just detached.**
`SignalSetupViewModel.swift:61` calls `Self.detectSignalCLI()` from inside `loadSnapshot`'s `@MainActor` apply closure. `detectSignalCLI` (`:66-75`) reads `HermesFileService.enrichedEnvironment()` then stats `dir + "/signal-cli"` for every PATH entry. `enrichedShellEnv` is a `static let` whose initializer is two `zsh` probes at 5 s + 3 s (`HermesFileService.swift:2468-2484`); `scarfApp.swift:89-91` warms it on a detached task at launch, but a `static let` initializer is a `swift_once` — a main-actor reader that arrives while the warm-up is still running *blocks on it*. The method is a `private static func` on a `@MainActor` class, so it is main-actor-isolated and cannot be hoisted into the detached work without an annotation. `MCPLoginController.defaultProcess` has the milder version of the same (`MCPLoginController.swift:237`, inside the `@MainActor ProcessFactory` called from `start()`). **NEW (P22 detached the load and left this in the commit).** Fix shape: make `detectSignalCLI` `nonisolated` and compute it inside `loadForm`'s detached body (it needs nothing from `self`).

**M2 · `saveDirectYAML` does not join `writeChain`, so the one race the chain exists to prevent is still open between the two writer families.**
`SettingsViewModel.enqueueConfigWrite` serialises every `hermes config set` with its re-read (`SettingsViewModel.swift:249-269`), and `runConfigMigrate` joins it explicitly because "a migrate interleaved with a toggle's write/re-read pair is exactly the race `writeChain` exists to prevent" (`:1143-1165`). `saveDirectYAML` (`:1012-1085`) — the path behind `agent.reasoning_overrides` (`:940`), `model_catalog.excluded_providers` (`:948`) and `profile_routes` (`:966`) — rewrites the *same file* and never touches `writeChain`. Its `GuardedTextFile.withLock` serialises it against other *Scarf* writers only; the `hermes config set` subprocess does its own read-modify-write outside that lock, so a toggle flipped while a direct-YAML save is in flight can be lost, and the chain's post-write re-read can publish pre-splice bytes. **PRE (`writeChain` predates the branch; `main` has it at `:192`).** Fix shape: wrap the `saveDirectYAML` await in the same `previous = writeChain` / `writeChain = Task { … }` pattern `runConfigMigrate` uses.

#### LOW

- **L1 ·** `KanbanBoardViewModel.swift:199-200` says the diagnostics fetch "Runs inside the actor, off the main actor (C10)". `refreshDiagnosticsIfDue` is a method on a `@MainActor` class (`:20, :494`); only `KanbanService`'s body is actor-isolated and detached (`KanbanService.swift:25, 613`). The throttle itself — the thing the comment annotates — runs on the main actor. Stamp-before-fetch is correct (`:497`). **NEW (P22), C2-class.**
- **L2 ·** P22 named a timeout on every `MessagingGatewayViewModel` mutation and pinned it (`MainActorSpawnDisciplineP22Tests.swift:170, 178`), but `HealthViewModel.swift:508, 533, 666` still call `ctx.runHermes([…])` with no timeout argument, inheriting the silent 60 s default (`ServerContext+Mac.swift:21`) — the same "contract says every site must name it" rule, applied to one view model and not its neighbour. Bounded, off-main, so not a freeze. **P22 missed.**
- **L3 ·** `ProcessTimeout.swift:6-18` and the P22 memory note call `waitUntilExit(timeout:)` "the one place the C10 timeout poll lives for ad-hoc spawns outside the transport", but three ad-hoc spawns still use the unbounded form and each reads its stderr pipe only *after* the wait — so a >64 KB diagnostic from the child is an unrecoverable deadlock, not merely a long wait: `ProjectTemplateService.swift:414`, `ProjectTemplateExporter.swift:364`, `AppRelauncher.swift:82`. Outside the P18–P28 diff, but it makes the helper's own doc comment overstated.
- **L4 ·** `GatewayBehaviorViewModel.save()` calls `PlatformSetupHelpers.saveForm(context:envPairs:configKV:)` with no `runner:` (`GatewayBehaviorViewModel.swift:190-192`), so the one platform-setup surface that is *not* a `PlatformSetupForm` has no `HermesCLIRunner` seam and no `Thread.isMainThread` test — `MainActorSpawnDisciplineP22Tests` covers the 15 conformers and skips this one. `load()` also lacks the `guard !isBusy` its 15 siblings get from `loadSnapshot` (`:68-72`), so two `.onAppear`s commit in completion order.
- **L5 ·** The "Reload" button on all 15 setup views is not disabled while busy (e.g. `TelegramSetupView.swift:85`, `NtfySetupView.swift:58`), while `loadSnapshot`'s `guard !isBusy` (`PlatformSetupHelpers.swift:267`) swallows the press silently — a press during a save does nothing and says nothing.
- **L6 ·** Stale-tag citations P26's sweep left: `EmailSetupViewModel.swift:70-72` cites `adapter.py:565` @ v2026.8.31 for a line the file below it correctly cites as `:354` @ v2026.9.7 (both are accurate at their own tags, but the audit target is one of them); `WhatsAppCloudSetupViewModel.swift:73` says "Verified against Hermes v2026.8.31" for the no-env-fallback claim. I re-verified the latter at the target: `gateway/platforms/whatsapp_cloud.py` @ v2026.9.7 contains no `os.environ`/`getenv`, so the claim holds — only the tag is stale.
- **L7 ·** `MCPLoginController.finish()` clears the reader via `stdoutPipe?…readabilityHandler = nil` (`:451`), but a login that finishes *before* its spawn continuation resumes never published `stdoutPipe` (the `guard !self.didFinish` at `:214` is what P28 added), so that run's handler is left installed until the pipe itself reports EOF. Benign — `pump()` is gated on `didFinish` and the inbox is per-run — but it is the one edge the M1/M2 fix leaves asymmetric.

#### Verified sound

- **The P28 MCP-login remediation holds under re-derivation.** Fresh `OutputInbox` per run captured by the reader closure (`MCPLoginController.swift:102-103, 133-146`); the spawn's generation-mismatch branch unhooks the reader, nils the termination handler, terminates, and reaps the remote half using the *captured* `server` — which is what covers the `stop()`-during-spawn-window case where `stop()` found `runningServer` already nil (`:187-200`). `stop()` bumps generation before terminating (`:252`). `HermesP28CrossPhaseRemediationTests.swift:259-299` asserts the invariant (`readabilityHandler == nil` on run A) rather than a text leak, and is driven through two `start()`s with no `await` between them, which is the actual window.
- **Blank-form-save, `.env` half.** `saveForm` maps an empty value to `unset` (`PlatformSetupHelpers.swift:77-95`), but `unset`/`setMany` go through `guardedMutate`, which returns `false` when the read was refused (`HermesEnvService.swift:133-134, 169-173`), and `saveForm` propagates that as a failure rather than "Saved" (`:94, :114`). So the unreadable-`.env` case cannot comment out a live key — only the config.yaml half (H2) is exposed.
- **All 15 forms route through the one choreography.** Every `*SetupView` calls `load(…)` on `.onAppear` and carries `.disabled(viewModel.isBusy)` on Save (checked all 15); `commitSave`/`loadSnapshot` both guard `!isBusy`, and `isLoading` is cleared *before* the `!isSaving` commit check so a superseded load cannot leak the spinner (`PlatformSetupHelpers.swift:267-283`). No VM calls `PlatformSetupHelpers.saveForm` directly except `GatewayBehaviorViewModel` (L4). Grep over the whole `PlatformSetup/` directory for `readText`/`loadConfig`/`HermesEnvService`/`Process(`/`enrichedEnvironment`/`FileManager` finds only the detached bodies plus the Signal site (M1).
- **C10 test honesty.** Every assertion in `MainActorSpawnDisciplineP22Tests` is `Thread.isMainThread` recorded inside the injected runner (`:381`), an ORDER check that cannot be satisfied by an inline spawn (`:310`), or an attempt *count* (`:258, :280`) — never a duration. The one wall-clock expectation (`:351`) is annotated as a boundedness ceiling and is not the test's subject. No vacuous pass: each off-main assertion is preceded by a "the runner was reached at all" expectation. `bff03f0f`'s 10 s → 120 s change is a poll deadline, not an assertion.
- **Kanban throttle.** `lastDiagnosticsFetchAt` is stamped before `await fetch()` (`KanbanBoardViewModel.swift:497-498`), so a failing host is capped at one attempt per 30 s; `supportsDiagnostics` clears the stamp on re-enable, and both halves are pinned through the production method.
- **iOS twins swept.** No iOS twin exists for the P25 `relativeTimeLabel` "ago ago" fix (no `ago`/`RelativeDateTimeFormatter` anywhere under `Scarf iOS/Kanban/`) or for the Mac skills "Reload"→"Re-scan" rename (no `reload`/`audit` under `Scarf iOS/Skills/`). `updateFaults` lives on the *shared* `SkillsViewModel:104, 969`, so the new iOS faults section renders the same three states as the Mac pane. iOS cron writes `cron/jobs.json` whole, so the Mac's `--clear-skills` precedence concern has no iOS analogue; `CronListView.swift:323` emitting `nil` for an emptied roster is correct for a whole-file rewrite.
- **`displayTelegramRichMessages` is not written back in the forbidden direction.** `TelegramSetupViewModel.swift:85` primes from the resolved default and `save()` writes it, which `HermesConfig.swift:1387` forbids — but the key is only written under `capabilities.hasTelegramRichMessages` (`:111`), so an undetected host writes nothing, a v0.17 host writes the `True` it ships (`config.py:2144` @ v2026.6.19) and v0.18+ writes the `False` it ships (`config.py:2367` @ v2026.7.1). No divergence reachable. It is the only `display*(capabilities:)` read in the whole `PlatformSetup/` directory.
- **Hermes citations re-verified at v2026.9.7 (exact):** `_VALID_MODES` at `tools/approval_context.py:197`; `clear_skills` wins at `hermes_cli/cron.py:612`; `mark_job_run` at `cron/jobs.py:2239`, `_advance_after_run` call at `:2266`, `repeat["completed"]` bump at `:2212` (inside the cited `2203-2217`); `PlatformConfig.gateway_restart_notification: bool = True` at `gateway/config.py:393` with the `toplevel_or_extra` read at `:438`, matching Scarf's top-level-of-platform write path; `_DISPLAY_ENV_BRIDGE["busy_ack_enabled"]` at `gateway/run.py:1813`; `extra.get("skip_attachments", False)` at `plugins/platforms/email/adapter.py:354`.
- **P28's H1/H3/L4/L6 remediation is complete.** `computeConfiguredPlatforms` now unions top-level sections, `platforms.<name>`/`gateway.platforms.<name>` nested *and* flow-style keys, and `.env` arms for `ntfy`/`simplex` (`PlatformsViewModel.swift:165-300`); both `PlatformsView.swift:30-41` and `ToolsView.swift:19-23` go through the single `KnownPlatforms.visible(on:isConfigured:)` seam with the same `!hasLoaded… || contains` widen-while-unread rule; `ToolsViewModel.loadPlatforms` calls the shared detector rather than its own scan (`:105-107`); no ungated `KnownPlatforms.all` roster accessor survives outside the seam itself (`HermesTool.swift:220`).

#### Product decisions

1. **Two incompatible postures for "the host's resolved default".** Settings (Mac tabs, iOS sheet) treats an absent key as a sentinel and writes *nothing*; the platform-setup forms prime the resolved default and write it on every Save (`GatewayBehaviorViewModel.swift:39-45` documents this as the deliberate answer to B5 — change Scarf's default to match Hermes rather than avoid writing). Both are defensible, but the forms' posture silently pins whatever Hermes's default is *today*, so the next default flip is a behaviour change for everyone who ever opened a form. Alan should pick one rule, or state why forms differ.
2. **Should H1's fix unset or refuse?** The Mac's "Host default" row is inert — selecting it does nothing at all, and getting back out of an explicit mode needs `hermes config unset` behind `hasConfigUnset` (documented at `SettingsViewModel.swift:531-538`). Matching that on iOS means a row the user can select that does nothing, which reads as a bug. The alternative — wiring the sentinel row to `config unset` on both platforms — is a new gated write path, not a parity fix.