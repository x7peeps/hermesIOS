# P38 — round-4 NEW findings (P30–P37 residue): per-item results

Branch `fix/whole-surface-audit-r3`. Six commits: `ea65ff90`, `fa4d1414`, `87f0ca40`, `43b594bd`, `e9055e55`, `6db90fe2`.
All 25 findings addressed. Nothing marked a pending product decision was implemented.

| # | Sev | Finding | Result | Commit |
|---|---|---|---|---|
| 1 | HIGH | `noFutureOccurrencesHint` names an edit Hermes refuses | FIXED (copy only). Verified `_apply_schedule_update` (`cron/jobs.py:1899-1910`) + the second `_reject_terminal_activation` (`:1965`, predicate `:1865-1879`) and `_advance_after_run` (`:2192-2215`) @ v2026.9.7. New copy: "This job has no runs left — duplicate it to schedule a new one."; a finite `repeat` gets `noFutureOccurrencesHint(repeatTimes:)`, which names the limit. **No Duplicate button** — pending product decision. 3 catalogue keys added in 6 locales, old key removed. P30 tests updated. | `ea65ff90` |
| 2 | MED | Mac row context menu is a fourth offer site | FIXED. `CronView.swift` contextMenu now drives off `viewModel.recoveryOffer(for: job)` (`canResume`/`canRearm`) and disables Run Now via `refusesTerminalJobLocally`, matching `BotRoutinesView`. The menu is not reachable from the a11y tree unopened, so the alarm is a source pin in `CronRecoveryP38Tests.rowContextMenuUsesTheSharedOffer` plus the VM-level offer tests. | `ea65ff90` |
| 3 | MED | iOS checks `oneShotIsUnresumable` ahead of the offer; `canRearm` branch dead | FIXED. Past-deadline rule folded into `HermesCronJob.recoveryOffer` as a THIRD door, gated on the new `hasCronPastOneShotResumeRefusal` (`isV0181OrLater`; `resume_job:1991-1996` @ v2026.9.7, sentence absent at v2026.7.1 and earlier — walked across all 32 tags). Mac, Bots and iOS inherit it, closing C10 reviewer M6. iOS keeps "re-arm it from the Mac app" for `canRearm`. New `CronRecoveryOffer.refusesResume` is the gate both platforms key on (excludes `.none`, preserving `setEnabled`'s idempotence). Parity test extended to the third floor, a past-deadline `run_at`, and both gates' `refusesResume`; new gate-level tests in `M5FeatureVMTests.p38*`. | `ea65ff90` |
| 4 | MED | `friendlyCronFailure` names re-arm for every terminal refusal | FIXED. Now `friendlyCronFailure(_:offer:)`; `runAndReload` threads `job:` at pause/resume/re-arm/remove/edit. With no job in hand the wording is unchanged (the caller cannot rule re-arm out). | `ea65ff90`, `6db90fe2` (edit path) |
| 5 | LOW | `isTruthyPauseMarker` doc misquotes `_has_pause_marker` | FIXED. Doc now states the real predicate (`state == "paused" or bool(paused_at)`, `:477-479`) and that this helper ports the second arm only, `effectiveState` carrying the first. | `ea65ff90` |
| 6 | LOW | `SessionsViewModel` cites `sessions_cmd.py:383-388` | FIXED → `:385-389` (blob opened; `_export_trace` at `:381`, the branch at `:385-389`). Same fix in `AuditP25SurfaceCopyTests`. | `ea65ff90` |
| 7 | LOW | `CronScheduleArgument` "verified at v2026.8.31" | FIXED. Re-verified at v2026.9.7, `cron/jobs.py:733-755`. | `ea65ff90` |
| 8 | MED | `parseNestedYAML` purge misses `values[path]`/`maps[path]` | FIXED, both. Also exempts a flat dotted sibling from the descendant sweep on a RE-open — probed with python3+PyYAML: `{"gateway": {"port": 2}, "gateway.enabled": true}` and `{"slack": {"bot_name": "x"}}`. New `HermesP38YAMLPurgeTests` (6 tests) covers the two-block fixture through `sharedPlatformScalar`, the flow-form first block, both dotted-sibling cases, and that a genuinely nested descendant is still purged. | `fa4d1414` |
| 9 | LOW | nothing-stored no-op only in `setApprovalMode` | FIXED. Moved into `unsetSetting` next to the floor gate; `isStored` is a REQUIRED argument. `setAuxiliaryMaxConcurrency` takes `stored:` from the view. `HermesP37ConfigUnsetFloorTests`'s row table now seeds each key as stored (it would otherwise have passed vacuously). New `HermesP38ClearRowNoOpTests`. Edge documented: `isStored` derives from `config`, `.empty` until first load — same posture as P35's `setApprovalMode`. | `fa4d1414`, `6db90fe2` (doc) |
| 10 | LOW | `controlCharacterFieldLabel` omits `groups`/`legacyGroup` | FIXED. `BotDraft` carries both (never edits them) and the refusal checks them — the writer emits both through `quoteIfNeeded` (`HermesBotProfileYAML.swift:437`, `:441`), so they reach `profile.yaml` on every save. New `HermesP38BotGroupControlCharacterTests`. | `fa4d1414` |
| 11 | HIGH (doc) | two docs claim every `config set` refusal exits 1 | FIXED, both (`unsetSetting` doc and `WriteVerdict` doc). Verified `set_config_value`'s managed arm (`hermes_cli/config.py:3450-3452`, `managed_error` `:453-455` @ v2026.9.7). Both point at the board task "Audit P39: managed-install refusals — config set / save_config exit-0 verdicts" (`t-ba727c07`). **Verdict NOT implemented** — pending product decision. | `fa4d1414` |
| 12 | MED | `dashboardListenerPID` hand-rolls the hoisted drain | FIXED. Body is now `lsof.waitDraining(timeout:pipes:)`; private `drainGrace` deleted; the never-closed read handle is now owned by the primitive. Marked `nonisolated` (its only caller is inside `Task.detached`). P22 allowance removed; `t-cd9fd829` moved to done with a note. | `87f0ca40` |
| 13 | LOW | pairing-approve prefix tag wrong | FIXED. Walked: `v2026.7.20:95` bare, `v2026.7.30:100` prefixed, `v2026.9.7:80` prefixed — so **v2026.7.30**, not v2026.8.3. Doc + memory note `architecture/hermes-pairing-approve-revoke-refuse-at-exit-0-judge-by-the` corrected, and both now record that the LOCKOUT marker is a split f-string below v2026.9.7 (`v2026.8.31:91-93`) so grep gives a false negative. | `87f0ca40` |
| 14 | LOW | `HermesCLIVerdictP21Tests` skills_hub.py numbers | FIXED, all seven. `do_audit` `:879-880`, `_print_error` `:891`, `Auditing…` `:893`, `No hub-installed skills…` `:887`, `do_update` `:831-832`, nested `do_install` `:868`; `Installation blocked:` is printed by `_install_blocked` (`:498`) from its call at `:699-700`. | `87f0ca40` |
| 15 | MED | `nonInterruptiveCommands` doc stale | FIXED. v0.13/`v2026.5.7` floor (`acp_adapter/server.py:170-171`, absent v2026.4.30), pointers to `hasACPSteer`/`hasACPQueue`, the no-op claim replaced with the fall-through-to-the-LLM fact (`acp_adapter/commands.py:88-95`), and the stale `/goal` TODO removed. | `43b594bd` |
| 16 | LOW | `hasCronRecoverableErrorResume` names a symbol absent at its tag | FIXED. Doc now says v2026.8.31 carries the guard as two INLINE `update_job` blocks (`:2583-2595`, `:2684-2696`) and only v2026.9.7 extracts `_reject_terminal_activation` (`:1865-1879`) — verified by grepping both blobs. | `ea65ff90` |
| 17 | LOW | `hasGoals` doc wrong; `hasACPQueue` uncited | FIXED. `hasGoals` → "CLI/gateway only", consumers named (`ChatViewModel.swift:1269`, iOS `ChatView.swift:56`). `hasACPQueue` given the same two-tag citation as `hasACPSteer`. | `43b594bd` |
| 18 | LOW | `alwaysAvailableCommands`'s `hasActiveSession` unread | FIXED. Parameter removed, six call sites updated, doc reworded to the real rule (surfaced always, greyed pre-session via `sessionRequiredCommandNames`). | `43b594bd` |
| 19 | LOW | `AgentTab` credits v0.20 for `max`/`ultra` | FIXED. `max` `hermes_constants.py:794` @ v2026.7.7 (0.18.1); `ultra` `:835-837` @ v2026.7.20 (0.19.0). | `43b594bd` |
| 20 | LOW | `everyFallbackNameIsDispatchedOrClientSide` uses the union | FIXED. Split into `acpDispatchedNamesUnion` and `acpDispatchedNames(at:)`; the test also asserts the compress row EQUALS `compressSlashName(capabilities:)`. "nine" → "nine at any one version, ten in total". Proven: hardcoding the roster's compress row makes it fail on four hosts — invisible to the union. | `43b594bd` |
| 21 | HIGH | subscript after a non-guarding count `#expect` | FIXED → `try #require(servers.first)` with the test `throws`. Alarm: `HermesP38SourceSweepTests.noSubscriptFollowsACountExpectation`, scoped to the 17 phase suites. **"Failure" shape:** the rule's violation is a TRAP that kills the host, so there is nothing to observe at runtime — the sweep was watched reporting `HermesP35MCPTokenProbeTests.swift:111` with the fix reverted. | `e9055e55` |
| 22 | MED | `try!` in P30–P37 test files | FIXED beyond scope: **19 `try! #require` sites across 7 files** (`HermesP35SelectionAndFloorsTests`, `BotsViewModelTests`, `FleetServiceTests`, `PreReleaseFixupTests`, `AuditF2ArgvAndSecretSurfacingTests`, `BotModePhaseAB0Tests`, `HermesV0211CronParityTests`) — each enclosing `@Test func` made `throws`. Alarm: `noTestForceTriesARequire`, repo-wide. Same "failure is the shape" reasoning as 21; watched reporting `HermesP35SelectionAndFloorsTests.swift:44` with the fix reverted. Force-unwraps and post-count subscripts in the same files were swept too. | `e9055e55` |
| 23 | MED | P22 sweep: dead allowance, weak "still REAL" check, fragile mapping, missing shapes | FIXED, all four. `HealthViewModel.swift` allowance removed (dead after 12) and `t-cd9fd829` closed; "still REAL" is now isolation-based (the sweep must reach the site) rather than substring-based; `allowed` is `[String: (path:, task:)]`; the matcher gained an `isRunning` spin (body must be `Thread.sleep`/`usleep`, so an `await Task.sleep` loop is correctly not a finding) and a `.wait(` on a name bound to a `DispatchSemaphore`/`DispatchGroup`, with `ProcessTimeout.swift` excluded as the primitives' own file. Triaging the new shapes also fixed a REAL blind spot: a multi-line signature made the opt-out walk miss a `nonisolated` on the opening line. | `87f0ca40` |
| 24 | LOW | `ProvenConfig.exists` read by nobody | DELETED, with the reasoning kept in the doc: absence is already folded into `config` (`.empty`) and `rawText` (`""`), and the refusal path it could gate never sees an absent file (an ABSENT config.yaml is deliberately not a refusal). | `e9055e55` |
| 25 | LOW | no sweep over `*SetupViewModel` env reads | ADDED. `everyPlatformFormReadsEnvUnderTheSharedGuard`: no `*SetupViewModel` may call `PlatformSetupHelpers.loadForm` directly, and every `snapshot.env` read must sit inside the `loadSnapshot` closure (where P37's `guard snapshot.envFailure == nil` applies). Watched failing with Discord's `loadSnapshot` swapped for `loadForm`. | `e9055e55` |

## Deliberately not done

- **A Duplicate button** for the cron dead end (item 1) — pending product decision; copy only.
- **An output verdict for `config set`** (item 11) — pending product decision, `t-ba727c07`. The exit-0 managed arm is documented at both doc sites.
- **A repo-wide subscript-after-count sweep** — ~100 pre-existing sites; filed as `t-f43f0af5` and the sweep is scoped to the phase suites with an assertion that the scope list still resolves.

## Tests

- ScarfCore package: **2685 tests, 179 suites, all passing.**
- Mac `scarfTests`: the full parallel run shows 25 tests red, all inside five load-sensitive suites (`ChatViewModelStartLifecycleTests`, `SessionDeletedSignalTests`, `ScarfMiniAppBridgeTests`, `ChatViewModelMismatchChooseModelTests`, `PermissionApprovalEditShapeTests`). **All 47 of those tests pass when the five suites are run in isolation**, and none touches a surface P38 changed.
- **Proven pre-existing on a `main` worktree**, per the round-3 rule: `git worktree add --detach <scratch>/main-check main` + the same full run with a scratch `-derivedDataPath` fails **34** tests under the same concurrent load — a SUPERSET, including `HermesP28CrossPhaseRemediationTests`, `SettingsP20ConfigDefaultsTests` and several ACP `session/prompt` suites that were green on this branch. Worktree and DerivedData removed afterwards.
- New suites: `HermesCronRecoveryP38Tests` (8), `HermesP38YAMLPurgeTests` (6), `CronRecoveryP38Tests` (6), `HermesP38SettingsResidueTests` (7), `HermesP38SourceSweepTests` (3), plus 4 new `M5FeatureVMTests.p38*`.
