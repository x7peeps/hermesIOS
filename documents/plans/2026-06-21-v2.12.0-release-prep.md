# v2.12.0 release prep — runbook

**Date:** 2026-06-21 · **Branch:** `feat/hermes-v017-parity` · **Theme:** Hermes v0.17.0 catch-up + the #119 remote-chat perf fix.

Prep is **done**; the cut is the maintainer's (`./scripts/release.sh` — needs the Developer ID cert, `scarf-notary` profile, and per-machine Sparkle EdDSA key). Nothing is pushed.

## Scope (diff since v2.11.0)

Nine non-noise commits. Three are the remote-sync perf fix already on `main`; six are this branch's v0.17 work:

- `bd2f55b` / `b0eae76` / `2dbee49` — off-main file-watcher I/O (gh **#119** typing-lag) + `import ScarfCore` fix. (Already on main; v2.12.0 inherits them.)
- `0d9e026` — Tier 1: 4 broken Health/Settings CLI invocations + WhatsApp allowlist no-op + config-set error surfacing.
- `0ee1b0a` — Curator prune → reversible "Archive idle skills" redesign.
- `ea8566d` — v0.17 capability gates + `curator.consolidate` + `max_concurrent_sessions` (+ backfilled v0.16 test cluster).
- `44b0ee7` — Telegram `rich_messages` + `status_indicator`.
- `f7a819f` — WhatsApp Cloud + SimpleX setup forms.
- `f62ba0a` — adversarial-audit fixes (WhatsApp Cloud `enabled` write + polish).

## Deliverables (done)

- [x] `releases/v2.12.0/RELEASE_NOTES.md` (committed with README).
- [x] `README.md` → "What's New in 2.12.0".
- [x] `wiki/Release-Notes-Index.md` → v2.12.0 table row + highlights paragraph.
- [x] `wiki/Hermes-Version-Compatibility.md` → currency update (was 2 cycles stale at v0.15): v0.17 + v0.16 "currently targeted" prose, v0.16.0 + v0.17.0 Verified-table rows, footer.
- [x] `.memory/decisions/Hermes v0.17 Compatibility Decisions` (recorded earlier).

## Verification on this branch

- Mac Debug build: **SUCCEEDED**.
- **642 ScarfCore tests pass** (the 1 intermittent failure is the known flaky `RemoteSQLiteBackend` cross-suite subprocess race, [[t-aud32]] — not these changes).
- Adversarial fresh-eyes audit of all 6 commits done; findings fixed in `f62ba0a` (HIGH: WhatsApp Cloud was configured-but-disabled).

## What's left for the maintainer (the cut)

1. Integrate `feat/hermes-v017-parity` → `main`, then **`git push origin main`** — only on Alan's explicit say (standing rule: never push until told).
2. **`./scripts/release.sh 2.12.0`** (or `--draft` first, then promote per `wiki/Release-Process.md`). The script bumps `MARKETING_VERSION` (idempotent), archives + signs + notarizes, EdDSA-signs the appcast, pushes appcast to `gh-pages`, tags, and creates the GitHub release using `releases/v2.12.0/RELEASE_NOTES.md` as the body.
3. The script preflights the Sparkle key against `Info.plist:SUPublicEDKey` and aborts on mismatch.

## After the cut

1. Verify: `gh release view v2.12.0` (published, both zips attached), `MARKETING_VERSION` = 2.12.0, local main in sync, appcast at `https://awizemann.github.io/scarf/appcast.xml` carries it.
2. **#119 (the wired-in retest):** comment that v2.12.0 ships the remote-chat typing-lag fix; ask @aseelye to update via Sparkle and retest with a fresh **ScarfMon** capture so we can confirm it's fully closed on their setup. Close #119 when confirmed (or close with a reopen-if-recurs note, per the team's pattern).
3. Bump the **Latest release** line on the wiki **Home** page; the Release-Notes-Index row is already added.
4. Mark the v2.12.0 release task done on `TASKS.md`.
5. **iOS:** the shared-core changes (curator/config/platform models + the off-main read fixes) ride in ScarfCore; a ScarfGo TestFlight build carrying them is a **separate track**. Hold iOS-only issue retests — **#112** Failure-2 ([[t-ios-cfg-get]]) — until that build is out.

## Not in this release (tracked follow-ups)

- iMessage via Photon — held; protocol still changing upstream ([[t-5ab57e6f]]).
- MCP per-server `keepalive_interval` editor ([[t-07a9baa4]]).
- Google Chat allowlist no-op + `google_chat` id rename (pre-existing, same class as the WhatsApp fix) ([[t-2d6888ea]]).
- #113 images-ignored is a Hermes model-vision-routing / user-config matter, not a Scarf bug (verified unchanged in v0.17); composer heads-up enhancement is [[t-31img]].
