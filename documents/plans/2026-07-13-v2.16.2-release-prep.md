# v2.16.2 release prep — 2026-07-13

Point release for the gh#126 remote viewing-profile work (PR #127 — first merged external
contribution, @JonLaliberte — plus our HERMES_HOME process-layer follow-up `ef169d0`).

## Scope (v2.16.1..main, noise excluded)

- `de35611` fix(macos): scope remote Sessions to the selected Hermes profile (gh#126) — JonLaliberte
- `ef169d0` fix(macos): scope remote hermes invocations to the viewing profile (gh#126) — process layer
- `b808632` fix(ios): synthesize prompt completion from send result (gh#124) — iOS track, NOT shipped by this Mac release
- `7e20662` test(ios): controller-level regression for gh#124 + enable iOSTests target

## iOS impact — none (verified)

- `SSHTransport` change compiles for iOS but is not ScarfGo's runtime transport (Citadel is, and it
  has injected `HERMES_HOME` since v2.13 / #120).
- `UserDefaultsProfileSelectionStore` moved ScarfIOS→ScarfCore behavior- and key-identical
  (`com.scarf.ios.profile-selections.v1` preserved).
- ScarfIOS package built for iOS Simulator during the PR review — green.
- gh#124's iOS fix remains queued for the **next TestFlight build** (last iOS build: v2.13.0);
  #124 stays open as its tracker.

## Checklist

- [x] Release notes — `releases/v2.16.2/RELEASE_NOTES.md`
- [x] README "What's New in 2.16.2" section
- [x] Wiki `Release-Notes-Index` row (index was current through v2.16.1; no backfill needed)
- [x] This runbook
- [ ] Maintainer: `git push origin main` (local main is ahead: 2 Memophant commits + this prep)
- [ ] Maintainer: `./scripts/release.sh 2.16.2` (preflights Sparkle key vs `Info.plist:SUPublicEDKey`)
- [ ] After cut: verify `gh release view v2.16.2` (both zips), appcast carries 2.16.2,
      `MARKETING_VERSION` bumped to 2.16.2
- [ ] After cut: comment on #126 / PR #127 that v2.16.2 is out (reporter + contributor can retest);
      hold #124 until the next TestFlight build
