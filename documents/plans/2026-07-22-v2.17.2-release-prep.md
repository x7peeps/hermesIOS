# v2.17.2 release prep — runbook

Point release. Prep completed 2026-07-22 by Claude; cut is the maintainer's.

## Scope (v2.17.1..main, noise excluded)

- `99fad45` fix(markdown): GFM tables render via the Marker engine — **gh#134** (@steveisakson). Headline.
- `b03d031` chore(deps): Marker pinned remote `from: 0.8.1` (contentText + nonisolated fixes upstream).
- `afcc39b` fix(ios): per-server-entry SSH key resolution + cellular login-timeout retries — **gh#133** (@turnkeysitedesign). Ships via next ScarfGo TestFlight build, not the Mac cut.

## Checklist state

- [x] Release notes — `releases/v2.17.2/RELEASE_NOTES.md` (release.sh auto-includes in bump commit + GitHub release body)
- [x] README "What's New in 2.17.2" section inserted above 2.17.1
- [x] Wiki `Release-Notes-Index.md` — v2.17.2 row added (index was current, no backfill needed)
- [x] Wiki `Home.md` — Latest/Previous/Earlier rolled (2.13.0 dropped to index), Targets-Hermes clause added ("no Hermes surface"), footer date bumped
- [x] Verification: full scarfTests suite green against pinned Marker 0.8.1; Release build + dev-copy launch OK
- [ ] Commit `README.md` + `releases/v2.17.2/RELEASE_NOTES.md` (regular repo files — ask/confirm)
- [ ] Memophant tiers (`wiki/`, `documents/`, `.memory/`, `TASKS.md`) — Alan commits via Memophant modal
- [ ] Draft gh#134 reply approved & posted (text in session; post after Alan's go-ahead — close on ship)

## Cut steps (maintainer)

1. `git push origin main` — local main is ahead of origin (table fix + Marker pin commits).
2. `./scripts/release.sh 2.17.2` (or `--draft` first; script preflights Sparkle key vs `Info.plist:SUPublicEDKey`).

## After the cut

- `gh release view v2.17.2` — published, both zips attached; appcast at awizemann.github.io/scarf/appcast.xml carries it.
- Post "v2.17.2 is out, please retest" on **gh#134** (Mac — post now). **Hold gh#133** until the ScarfGo TestFlight build is out (iOS-only fix).
- Mark release task done on TASKS.md.
