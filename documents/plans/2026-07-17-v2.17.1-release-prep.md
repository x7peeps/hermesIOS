# v2.17.1 release prep — 2026-07-17

Patch release; theme: **exports land on the user's Mac** (sessions gh#129/PR#130, profiles gh#132/gh#131) plus the shared-ScarfCore gh#112 diagnosis for the follow-up ScarfGo build.

## Scope (v2.17.0..main, noise excluded)

- `bbd4b76`/`2b4def8`/`123d922` + merge `5119819` — PR #130 (JonLaliberte): session export piped to the Mac via `sessions export -`; silent-failure surfacing; detail-sheet export; `.jsonl` UTType; off-main-actor.
- `5d9df1d` — gh#131 parent-dir Verify fix + traceback last-line banners (surface later removed by gh#132).
- `ecdf995` — gh#132: profile export streams to the Mac (`RemoteProfileExport`: /tmp scratch → `streamRawBytes` → atomic move → cleanup); remote write-path sheet deleted.
- `7a3b208` — gh#112 (iOS-only behavior, ScarfCore): CLI-probe diagnosis + in-app Docker wrapper guidance. Mac behavior unchanged; ships to users via next ScarfGo TestFlight.
- `e5944eb` — retire legacy `scripts/wiki.sh` (dev-only).

## Verification state

- Merged tree: 986/986 ScarfCore, 277/277 scarfTests (macOS 27.0 / Xcode 26.6 — the PR #130 suite's first-ever run).
- `hermes sessions export -` verified live on v0.17.0 (this Mac, 6 MB JSONL) and v0.18.2 (PR author, over SSH).
- Outstanding manual smoke: one remote **profile** export click-test against a real SSH host (streaming primitive is production-proven via backups; this exact flow not yet driven live).

## Checklist

- [x] Release notes — `releases/v2.17.1/RELEASE_NOTES.md`
- [x] README "What's New in 2.17.1"
- [x] Wiki `Release-Notes-Index` row (2026-07-17)
- [x] Wiki `Home.md` — Latest/Previous/Earlier rolled, footer + Targets-Hermes updated
- [x] `main` pushed and in sync with origin (nothing pending)
- [ ] Commit `README.md` + `releases/v2.17.1/` (or let `release.sh` pick up the notes in the bump commit)
- [ ] Maintainer: `./scripts/release.sh 2.17.1` (or `--draft` first) — script preflights Sparkle key vs `Info.plist:SUPublicEDKey`
- [ ] Post-cut verify: `gh release view v2.17.1` (both zips), MARKETING_VERSION bumped, appcast carries it
- [ ] Post-cut comments: "v2.17.1 is out, please retest" on gh#129/#131/#132 (JonLaliberte); **hold gh#112** until the ScarfGo TestFlight build is out
- [ ] Wiki tier commit in Memophant (auto-publishes via GitHubWikiPublisher)

## iOS track (separate)

ScarfGo TestFlight build carrying gh#112 — after the Mac cut; then the gh#112 "please retest" comment.
