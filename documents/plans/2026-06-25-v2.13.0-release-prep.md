# v2.13.0 release prep — 2026-06-25

Dual-track release (Mac via `release.sh` + ScarfGo TestFlight), version kept in lockstep at **2.13.0**. First release to carry the #120 profile-switching work and the gh#112 iOS transport fix — neither is on `main`/v2.12.0.

## Scope (since v2.12.0)

Headline (iOS / ScarfGo):
- **#120 ScarfGo profile switching** (Design B — per-connection `HERMES_HOME`/`remoteHome` scoping; never mutates the host `active_profile`). Profiles tab is now a switcher.
- **gh#112 iOS transport pooling + open-coalescing** — fixes "Transport refused" save failures + empty Settings; pool one SSH connection per server, coalesce concurrent opens.

Shared / Mac:
- **Skills "What's New" snapshot keyed per (server, profile)** — fixes cross-profile pill bleed (iOS-visible; Mac no-op but rides in shared ScarfCore).
- ScarfIOS package made unit-testable on macOS; build/i18n/AGENTS release-prep.

## Verification

- gh#112: unit suite green; live integration test vs ephemeral sshd — 24-way burst 20/24 connect failures (un-pooled) → 0/24 (pooled+coalesced). Reproducible via `./scripts/verify-ios-transport-pool.sh`.
- #120 check #2 (active_profile unchanged): verified deterministically (HERMES_HOME-scoped write leaves `active_profile` untouched; only `profile use` mutates it).
- #120 checks #1 (ACP dies/respawns scoped) + #3 (zero-profile footer): driven on the simulator (see session result; re-eyeball on a real device if needed).
- Pill: ScarfCore `swift test` green (677). NOTE a rare pre-existing parallel flake in `RemoteSQLiteBackendTests` (~1/7, passes alone) — spun off as a separate task, not a release blocker.

## Prep checklist

- [x] RELEASE_NOTES — `releases/v2.13.0/RELEASE_NOTES.md`
- [x] README "What's New in 2.13.0"
- [x] wiki `Release-Notes-Index.md` row
- [x] iOS `TESTFLIGHT_CHECKLIST.md` + `APP_STORE_METADATA.md`
- [x] Memory updated (transport pooling, profile-snapshot, F2 correction)
- [ ] Merge `feat/120` → `main` (3-way; expect `xcstrings`/`pbxproj` conflicts), rebuild Mac + iOS, full tests, push `origin/main`
- [ ] Maintainer: `./scripts/release.sh 2.13.0` (Mac cut)
- [ ] Maintainer: iOS archive + TestFlight upload per `TESTFLIGHT_CHECKLIST.md`
- [ ] After cut: post "v2.13.0 / TestFlight out, please retest" on gh#112; mark release task done

## Cut steps (maintainer-owned — do NOT run from prep)

1. Land on main: the merge + `git push origin main` (this session handles the merge+push per Alan's call).
2. **Mac:** `./scripts/release.sh 2.13.0` — preflights the Sparkle EdDSA key against `Info.plist:SUPublicEDKey`, bumps the version, attaches `RELEASE_NOTES.md` to the GitHub release, updates the appcast. Needs the Developer ID cert + `scarf-notary` profile + Sparkle key on the machine.
3. **iOS:** Xcode Archive (`scarf mobile`, Any iOS Device) → App Store Connect upload → Public Beta → Submit for Review. Build number must exceed 45.

## Notes / risks

- The merge is the one non-trivial mechanical step: `feat/120` predates v2.12.0, so merging brings v2.12.0's released work together with #120/gh#112. Resolve conflicts (likely `Localizable.xcstrings`, `project.pbxproj` build/marketing numbers), then re-verify both targets build + tests pass before pushing.
- The Mac "What's New" is thin (the user-facing features are iOS); the dual-track 2.13.0 is for version-sync, with shared-core changes riding into the Mac binary.
