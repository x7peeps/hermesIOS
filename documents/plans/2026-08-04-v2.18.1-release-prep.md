# v2.18.1 Release Prep — 2026-08-04

Fast follow to v2.18.0: the v0.20 power-settings pass. Scope: v2.18.0..main — merge db8c070
(compression tuning, agent.reasoning_overrides table via new GatewayConfigWriter.setMap,
model_catalog.excluded_providers editor; all gated isV020OrLater) + a417d8c (fresh-eyes audit
fixes: inline flow-dict replacement not duplication, CRLF/commented section headers in the shared
locator — also hardens pre-existing gateway allowlist setList — disable-alias acceptance +
error surfacing on writer refusal). Verified: 1,090 ScarfCore tests green, macOS Debug build
succeeds, writer output cross-validated with PyYAML (Hermes's parser). Audit verdict trail:
fix-first → all findings fixed → re-verified.

## Prep checklist (done by Claude)
- [x] releases/v2.18.1/RELEASE_NOTES.md
- [x] README.md — "What's New in 2.18.1" inserted
- [x] wiki/Release-Notes-Index.md — v2.18.1 row
- [x] wiki/Home.md — Latest/Previous/Earlier rolled (v2.16.2 dropped), footer bumped
- [x] Memory: decisions/hermes-v0-20-compatibility-decisions carries the power-settings entry
- Known limitation documented in notes: comments inside an existing reasoning_overrides block
  are not preserved on table edits (F4, accepted).

## Maintainer cut steps (Alan, from terminal)
1. Memophant commit modals for managed tiers (wiki commit auto-publishes; no scripts/wiki.sh).
2. Commit regular files: releases/v2.18.1/RELEASE_NOTES.md + README.md.
3. git push origin main (local main ahead: db8c070 merge + a417d8c fix + release-prep files).
4. ./scripts/release.sh 2.18.1

## After the cut
- Verify gh release v2.18.1 (both zips), MARKETING_VERSION 2.18.1, appcast serves 2.18.1.
- No gh# issue follow-ups owed (no community-reported issues shipped in this delta).
- Remaining backlog: t-1cc0a505 (deferred v0.20 settings surfaces), ScarfGo TestFlight build.
