# Session resume state — 2026-09-02 (Scarf orchestration, post-3.0.0)

## Where we are
Scarf 3.0.0 IS SHIPPED (published GH release, both zips, appcast 3.0.0, MARKETING_VERSION bumped). This session ran, in order: Hermes v0.21 audit+parity (v2.23.0), Bot Mode Phase A (v2.24.0), Phase B + parallel backfill session (merged), the full per-section five-lens audit + F1–F9 remediation (~47 commits), the 3.0.0 cut, then a post-release warnings sweep. Local main is AHEAD of origin with UNPUSHED commits: 8b4ae88 (46 Swift-6-mode diagnostics → 0) + whatever the two in-flight agents land. Alan wants those pushed once green.

## IN FLIGHT right now (task notifications will arrive; act on them)
1. **Bot-create fix + ratchet agent** (Opus): fixing (a) two Release-build errors exposed by the new SWIFT_TREAT_WARNINGS_AS_ERRORS=YES in all 7 Release pbxproj blocks (FleetApplyViewModel.swift:215 captured-var self — fix shape: `let owner = self` binding; Scarf iOS/Chat/ChatView.swift:2161 weak-vs-strong capture), plus any further errors; commits the pbxproj ratchet as its own commit; (b) THE LIVE BUG Alan hit: creating a bot with the optional profile field empty → profile created but identity write fails unsafeToWrite; retry ("Make a Bot") refuses with "duplicate ui_meta, an inline mapping, or an oversized block". Root cause direction: `hermes profile create` emits a profile.yaml shape (likely flow-style/empty inline mapping) HermesBotProfileYAML refuses; the fix teaches the writer Hermes's own default shapes while keeping refusal for genuine ambiguity. Alan's real file /Users/awizemann/.hermes/profiles/seo-research-bot/profile.yaml is the fixture (READ-ONLY — do not modify; after the fix, Make a Bot must work on it untouched).
2. **Pattern-hunt auditor** (Opus, read-only): sweeping EVERY Scarf reader/writer of Hermes-authored files against fixtures generated by Hermes's real serializers (yaml.safe_dump traits: 80-col folding, flow-style {}, etc.). Class has shipped 3 bugs (bot_peers folding, cron jobs.json shapes, profile.yaml refusal). Convention recorded: conventions/hermes-authored-file-fixtures-must-come-from-hermes-s-own. Its AT-RISK findings become a fix package.

## When both report
- Verify fix agent: Release builds (Mac + "scarf mobile" iOS) zero errors, Debug + full suites green (~1818 core / 607 app baseline), bot-create + Make-a-Bot fixed with generated fixtures. Then `git push origin main` (Alan explicitly approved pushing this batch).
- Then launch the TWO QUEUED ITEMS Alan approved: (a) stale-verification sweep — close/refresh WS-2/4/5/6/7/8 + t-todo-* misc verify tasks (most implicitly re-verified across v0.17–v0.21 cycles); fold pattern-hunt AT-RISK findings into it or a sibling package; (b) perf-branch port — port LiamVan6868's perf/batch-hydrate-toolcalls idea (batch tool-call hydration into queryBatch; single-pass JSON key scan) onto current main as fresh work with credit in the commit message, then delete the stale branch (2 conflicts vs main; branch untouched otherwise).

## Process conventions (the machine that works)
- Sequential Opus/Sonnet fix packages on main; each: verify findings first (refute plainly — ~10 refutations so far were real), build, full ScarfCore + foreground -only-testing app suites, INLINE adversarial self-audit (no background audit subagents — they stall), scoped conventional commits with footer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Never push without Alan. Never stage managed tiers (.memory/ wiki/ documents/ TASKS.md tasks/). Memophant task per package; orchestrator (me) moves task state. Durable facts appended to decisions/section-audit-remediation-2026-09 (the current cycle log). Fresh-eyes re-audit for security/contract work. Memophant MCP reports stale binary all session — works fine, reconnect when convenient.
- Known traps: agents that spawn sub-clusters stall waiting on cross-session reports (forbid subagents or relay manually); one auditor fabricated findings then retracted (verify citations); zsh `:h` in "$tag:path" silently truncates (quote or avoid); Debug hides Swift-6-mode diagnostics ScarfCore's swift test can't see (hence the Release ratchet).

## Board (open, beyond the in-flight)
- Alan's hands: charter indent fix t-00f9972e (restores read_charter), Memophant tier commits, MCP reconnect.
- Queued lanes he ordered: after the two items → quality backlog (VM-banner l10n policy decision, remote symlink containment, flaky t-6e1e9c86/t-aud32, t-aud21 reasoning lazy-load completion, t-aud30, t-9634ae74, t-8f4f5d68, t-a21accf0, t-1cc0a505) → iOS cycle (R2/R3/R4/R6, t-52f4564b, notif trio) → gh#105 edit-remote-servers → roadmap (charter-for-users GO brief awaiting Alan; Bot Mode Phase C: group rooms owned-orchestrator, @mention autocomplete, per-bot presence extras; t-93517390 blocked upstream).
- 3.0.0 post-cut: all reporter follow-ups were already closed; nothing owed.

## Key refs
documents/reports/2026-09-02-section-audit-findings.md (consolidated audit), documents/plans/2026-09-02-v3.0.0-release-prep.md, decisions/section-audit-remediation-2026-09 + bot-mode-phase-{a,b}-decisions + hermes-v0-21-compatibility-decisions (memory), .memory/charter.md (installed; YAML indent bug pending Alan). Hermes reference: worktree pattern `git -C ~/.hermes/hermes-agent worktree add --detach ~/.hermes/hermes-agent-v0.21.0-audit v2026.8.31` (currently removed; re-add when needed). Release cut = Alan's ./scripts/release.sh only.
