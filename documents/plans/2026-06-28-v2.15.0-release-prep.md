# v2.15.0 release prep — runbook

**Date:** 2026-06-28
**Version:** 2.15.0 (skips 2.14 — feature release, not a Hermes-compat one)
**Task:** t-af0690ff
**Prepared on branch:** main (integrated, clean tree)

## What this release is

"Projects grow up" — the largest Projects update since v2.3. Everything since `v2.13.0` (~33 substantive commits, the whole `feat/projects` arc):

- **First-class `ScarfProject` object** — portable `.scarf/project.json` + per-host `~/.hermes/scarf/projects.json` registry (`f770fe4`).
- **Mini-apps** — sandboxed `WKWebView` panels driving the agent via `window.scarf`; manifest/permission model, default-deny grant sheet, `scarf.prompt`/`onEvent` dedicated rate-limited ACP session, data channel (`b152e41`, `8d6a543`, `727237c`, `1521871`, `bd0a91c`, `8d59e52`, `0851561`, + hardening `350c3bd`/`81627a0`/`40480a9`/`1a3cf83`/`4523b9c`).
- **Fleet/Portfolio** — same repo across hosts grouped by stable id, drift, Apply to Fleet (`2a32643`), capability-gated cron copy (`40f8d1c`, `b9f98ae`).
- **One-click Upgrade Project** + mini-app authoring skill + slide-in overlay (`9024156`).
- **Cockpit single-pane** (`8e7b2c4`) — 12 panels: dashboard/sessions/board/site/context/cron/memory/secrets/templates/slash/miniapps/fleet.
- **Project chats load AGENTS.md** — spawn `hermes acp` with cwd=project (`b421280`, `5538e30`); carries the trust-awareness line (t-cea43144).
- **Hardening:** SSH path command-injection escape (`5b68699`), window-frame persistence (`71a1239`), FIFO mini-app events (`4523b9c`), MiniAppAgentSession docstring/decision (`47c2101`).

No GitHub issues in the `v2.13.0..main` range need a "please retest" follow-up (open issues #113/#112/#61/#45 are unrelated).

## Deliverables (done this session)

- `releases/v2.15.0/RELEASE_NOTES.md` — house-format notes.
- `README.md` — "What's New in 2.15.0" section.
- Wiki: **new** `Mini-Apps.md` + `Fleet-and-Portfolio.md` (deep how-to pages); updated `Projects-and-Profiles.md` (cockpit/first-class/Upgrade + cross-links), `Chat.md` (AGENTS.md context + trust note), `Release-Notes-Index.md` (row + highlights + backfilled 2.13.0), `Home.md` (Latest release), `_Sidebar.md` (two new links).
- This runbook.

## Verification (real)

- **Release-config build:** `xcodebuild -project scarf/scarf.xcodeproj -scheme scarf -configuration Release build` → **BUILD SUCCEEDED**.
- **ScarfCore:** `swift test --package-path scarf/Packages/ScarfCore` → **765/765**.
- **App target:** `xcodebuild test -only-testing:scarfTests -destination 'platform=macOS'` → **228/228**.
- Adversarial doc audit: cockpit 12-panel enum, Apply-to-Fleet fields (model preset/board/cron), mini-app rate limit (8/60), permission model + CSP all verified against source.

`MARKETING_VERSION` left at **2.13.0** on purpose — `release.sh` bumps it to 2.15.0 (+ build 46→47) in its own commit. Bumping it by hand would flip the script into "resume mode" and skip the bump.

## The cut — maintainer (Alan) runs

Preconditions met: on `main`, working tree will be clean after the prep commit, `releases/v2.15.0/RELEASE_NOTES.md` present, `gh-pages` worktree intact at `.gh-pages-worktree/`.

1. `git push origin main` (local main is ahead of origin).
2. `./scripts/release.sh 2.15.0`  — bumps version + build, archives Universal + ARM64, signs (Developer ID), notarizes (`scarf-notary`), staples, EdDSA-signs the appcast, pushes appcast to gh-pages, pushes main + tag `v2.15.0`, creates the GitHub release with both zips. (Or `--draft` first, then promote per `wiki/Release-Process.md`.)
   - Preflight aborts on a Sparkle key mismatch (`Info.plist:SUPublicEDKey` vs Keychain) — see `.memory/ops/Sparkle Key Recovery.md`.

## After the cut

- `gh release view v2.15.0` (published, both zips), `MARKETING_VERSION` = 2.15.0, appcast at https://awizemann.github.io/scarf/appcast.xml carries it.
- Move t-af0690ff and t-cea43144 to done.
- iOS/ScarfGo is a separate TestFlight track — not part of this Mac cut.
