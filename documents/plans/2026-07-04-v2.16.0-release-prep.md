# v2.16.0 release prep — 2026-07-04, expanded 2026-07-10 · READY TO CUT

Minor release: Hermes v0.18-line parity (audited at v0.18.0, re-audited at v0.18.2) — the v0.17→v0.18 catch-up, mirroring the v2.12.0 precedent. Prepped same-day as the v2.15.1 patch; expanded 2026-07-10 with the v0.18.1/v0.18.2 delta audit results before the cut.

## Scope (v2.15.1..main, noise excluded)

- `9338c59` feat(hermes): Hermes v0.18.0 parity + audit-surfaced bug fixes
  - compacted-search schema gate (`messages.compacted`, schema v17 — first DDL change since v0.16); search widens to `(active=1 OR compacted=1)`, transcripts stay active-only
  - provider tables: +`moa` overlay (AuthType `.virtual`), −`google-gemini-cli` (+2 aliases) → models.dev-backed `vertex`; check-hermes-tables.py exits 0 vs v2026.7.1
  - cron `attach_to_session` round-trip (nil → absent key)
  - v0.18 capability flags + `isV018OrLater` + test cluster
  - PRE-EXISTING FIX: Web Tools tab wrote/read dead `web_tools.*` keys → real `web.*` keys (silent no-op since ≤v0.9; deliberate no-migrate of stale values)
  - PRE-EXISTING FIX: cron withEnabled()/buildJob() dropped workdir/context_from/no_agent on save; withEnabled moved onto HermesCronJob
- `011aad2` docs(release): v2.16.0 release notes + README What's New
- `0a33b15` fix(cron): lossless jobs.json round-trip + custom-provider preflight skip (v0.18.2 audit)
  - generic unknown-key preservation in HermesCronJob/CronSchedule (kills the strip-on-toggle class; covers v0.18.2's run_claim + ~15 pre-existing unmodeled fields incl. enabled_toolsets/repeat/provider routing)
  - key fixes: pre_run_script→script, expression→expr + interval minutes (never matched Hermes)
  - ModelPreflight skips custom/custom:* (t-ed3700b2, same class as gh#121)
- `0e4eebf` docs(release): notes + README cover the v0.18.2 re-audit

Full source-verified audits: [[Hermes v0.18.0 Audit Findings]] + [[Hermes v0.18.2 Audit Findings]] · decisions: [[Hermes v0.18 Compatibility Decisions]].

## Checklist

- [x] Version picked: **2.16.0** (minor — new user-facing surfaces; Hermes-catch-up precedent v2.12.0)
- [x] feat/hermes-v018-parity fast-forwarded into main (9338c59)
- [x] `releases/v2.16.0/RELEASE_NOTES.md` written (house format) + committed
- [x] README "What's New in 2.16.0" inserted above 2.15.1 + committed
- [x] `wiki/Release-Notes-Index.md` row added (Memophant tier — user commits)
- [x] `wiki/Hermes-Version-Compatibility.md` updated: v0.18 row = current target, new lead paragraph
- [x] Verified pre-cut: Debug build green, 796/796 ScarfCore tests, CredentialPoolsGating + ToolGateway app suites green, check-hermes-tables.py exit 0 vs v2026.7.1
- [x] v0.18.2 delta re-verified (2026-07-10): 799/799 ScarfCore tests, macOS + iOS Debug builds green, check-hermes-tables.py exit 0 vs v2026.7.7.2, real jobs.json byte-equivalence round-trip, adversarial review run (1 edge case → deliberate skip, recorded in decisions note)
- [x] Adversarial 8-angle review run; 2 findings fixed pre-commit, 2 deliberate skips recorded
- [x] Secret scan of v2.15.1..main + working tree: clean (pattern scan — only doc-text/SQL-column false hits)
- [x] No open gh# issues addressed by this range (#121 shipped in 2.15.1; #61 large-DB perf is unrelated)
- [ ] Maintainer: `git push origin main`
- [ ] Maintainer: `./scripts/release.sh 2.16.0` (script bumps MARKETING_VERSION 2.15.1→2.16.0, preflights Sparkle key vs Info.plist SUPublicEDKey)
- [ ] Post-cut: verify `gh release view v2.16.0` (both zips), appcast serves 2.16.0, main in sync
- [ ] Board task → done after cut

## Notes for the cut

- MARKETING_VERSION is still 2.15.1 — release.sh owns the bump; do not hand-edit.
- iOS/ScarfGo (separate track): next TestFlight build carries the cron field-loss fix + the v2.15.0 project-context fix; hold iOS-issue follow-ups until it ships.
- Stale `web_tools:` blocks in user config.yaml are inert by design — release notes tell users to re-pick backends once.
