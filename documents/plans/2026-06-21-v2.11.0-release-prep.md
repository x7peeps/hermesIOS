# v2.11.0 release prep — 2026-06-21

Coordinated catch-up to **Hermes v0.16.0 (2026.6.5)**. The compatibility code is already
merged to `main` (PRs #114, #115, #116) and `main` is in sync with `origin`. This is the
release cut for that work. Prior version: **v2.10.3** (build 42).

## Scope (10 commits, `v2.10.3..main`)

All v0.16 compatibility work — no `gh#` issue fixes in this range (those shipped in v2.10.3),
so **no "please retest" follow-ups are owed** for this cut.

- `82ae046` feat: v0.16.0 compatibility — `messages.active` soft-delete filter (6 read paths,
  schema-gated), capability flags + `isV016OrLater`, ACP `session_info_update`, gated session
  rename, Spotify→Plugins relocation, Bedrock overlay provider, 8 SQL-shape tests.
- `c30def9` feat: quick-win surfaces — `sessions optimize` Health action, Kanban goal pill,
  `sessions.rewind_count` badge + `withTitle` apiCallCount fix.
- `5a733c9` chore: drop 14 WS-* "verify-against-live-host" TODOs (confirmed against v0.16 source).
- `b7c6bf3` docs(memory): land v0.16 compatibility facts on main.
- `9bc39b7` fix: gateway allowlist top-level path + DingTalk `allowed_chats` + `gateway list`
  text parsing (no `--json`).
- `6786e66` fix: dead-verb/config-shape corrections — kanban `verify`, curator `--json`,
  OpenRouter `response_cache` scalar, MCP SSE add path, ACP `/goal`/`/subgoal` de-advertised.
- `729e742` docs(memory): mark v0.16 wire mismatches resolved.
- (+ merge commits #114/#115/#116)

## Deliverables (this prep)

- [x] `releases/v2.11.0/RELEASE_NOTES.md` — full house-format notes (GitHub release body).
- [x] `README.md` — new "What's New in 2.11.0" section above v2.10.3.
- [x] `wiki/Release-Notes-Index.md` — new table row (2026-06-21) + highlights-by-major entry.
- [x] This runbook.

## Hand-off — the maintainer's cut (NOT run by the prep skill)

1. `main` is already in sync with `origin` — no `git push origin main` needed unless the
   doc commits below are pushed first.
2. `./scripts/release.sh 2.11.0` — bumps `MARKETING_VERSION` 2.10.3 → 2.11.0, archives
   Universal + ARM64, signs (Developer ID, team `3Q6X2L86C4`), notarizes (`scarf-notary`
   keychain profile), staples, EdDSA-signs the Sparkle appcast, pushes appcast to `gh-pages`,
   tags `v2.11.0`, and creates the GitHub release with both zips. Builds under `$TMPDIR`
   (iCloud-Drive xattr gotcha) automatically.
   - The script preflights the Sparkle key against `Info.plist:SUPublicEDKey` and **aborts on
     mismatch** — run from the machine holding the production EdDSA key (see
     `.memory/ops/Sparkle Key Recovery.md`).
   - Optional: `./scripts/release.sh 2.11.0 --draft` first, then promote per
     `wiki/Release-Process.md`. (Alan chose a full release.)

## After the cut

- `gh release view v2.11.0` — published, both zips attached.
- `MARKETING_VERSION` = 2.11.0, local `main` in sync, appcast at
  `https://awizemann.github.io/scarf/appcast.xml` carries 2.11.0.
- Mark the release task done on `TASKS.md` (the "Update our app to support the latest Hermes
  v2026.6.5" item).
- iOS/ScarfGo is a separate TestFlight track (shared core changes ride along; queue separately).

## Notes

- Doc commits: `README.md` + `releases/v2.11.0/RELEASE_NOTES.md` are regular repo files
  (`release.sh` auto-includes the notes in the version-bump commit). The `wiki/` + `documents/`
  edits are Memophant-owned — leave dirty for the Memophant commit modal.
- Deferred (additive, P2, not blockers): sessions-optimize maintenance action, `insights`/
  `dashboard` affordances, Kanban run-terminate (REST `/runs/{id}/terminate` only).
