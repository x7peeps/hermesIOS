# v2.17.0 release prep — 2026-07-14

Minor bump (headline features). Current MARKETING_VERSION = 2.16.2 → 2.17.0.

## Scope (v2.16.2..main, 26 substantive commits, Memophant-noise excluded)

**Local models (headline):** T1-T4 descriptor table / enumeration / picker / audit
(3646749, a85a7f2, 240d8ef + audits), context-floor gating (89107d2), vision heads-up
(02b80c6) + Ollama /api/show detection (b6cb500), residual-writer plan routing (35e901e),
all audit-hardened.

**Session-layer overhaul (4 defect classes):** empty session/load guard (5262fba+da73fc5),
engagement-gate + dedup (44717fb), start watchdog/supersede/teardown/pipe-readers
(8c0937e+d70c33c), deleteSession teardown (59485c1) + Sessions-tab seam (46735ba),
resumeSession removal + ACP error details (d19dca4+775c651).

**Settings + misc:** dropdown fix / duplicate-parser deletion (3e0184d), derived config-parity
build gate (2d6dd52), mismatch-banner Choose-model (429d4eb), final audits (1c0577b, 6bfe1c5,
1d075b7, 40a2403).

## Version decision
2.17.0 (minor). 26 commits, two headline features (local models + session reliability) — a
patch would undersell it. No Hermes version floor — verified 0.17-compatible throughout.

## iOS impact — none for this Mac release (verified)
Local-model + session-layer work compiles in shared ScarfCore but doesn't change iOS runtime.
KNOWN iOS-only follow-up (t-52f4564b): ScarfGo's confirmModelPreflight writes model config raw,
bypassing LocalModelConfigPlan's clear-on-switch scrub (stale base_url risk, GH#27132 class) —
rides the NEXT TestFlight build, NOT this cut. Not a Mac-release blocker.

## Upstream contributions this cycle
- issue #64144 + PR #64146 (aux "payment/credit error" mislabel) — CI green, awaiting review
- review comment on PR #56448 (aux local-alias ordering sliver)
- Release-notes caveat reflects the still-unmerged aux-alias gap (local-model aux degradation <0.18).

## Checklist
- [x] Release notes — releases/v2.17.0/RELEASE_NOTES.md
- [x] README "What's New in 2.17.0"
- [x] Wiki Release-Notes-Index row
- [x] This runbook
- [ ] Maintainer: `git push origin main` (local main ~29 commits ahead of origin)
- [ ] Maintainer: `./scripts/release.sh 2.17.0` (preflights Sparkle key vs Info.plist:SUPublicEDKey)
- [ ] After cut: `gh release view v2.17.0` (both zips), appcast carries 2.17.0, MARKETING_VERSION bumped
- [ ] After cut: no gh# reporter follow-ups owed this release (upstream contributions tracked separately)

## Deferred decisions (not blocking the cut)
- 0.18 hard floor: recommended AGAINST for now (measured — see decision note
  decision-chat-transport-stays-acp-min-hermes-to-0-18); revisit as v3.0 after building reliable
  connect-time version detection (t-bfd15aef) + aux-alias PRs shipping.
- Follow-ups filed: t-d25e68cc (done), t-2d258871 (done), t-52f4564b (iOS preflight, next TF),
  t-2d258871 (done), t-a21accf0 (semver/shell-quote consolidation), t-bfd15aef (version read).
