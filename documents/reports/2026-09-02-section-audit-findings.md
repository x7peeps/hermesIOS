# Full-app section audit — consolidated findings (2026-09-02, main @ post-merge RC)

Eight read-only audits (7 sidebar groups + a re-run Kanban audit after a retraction). All findings source-traced; severities reasoned, not reproduced — fix agents must verify before fixing and report refutations. Fix packages F1–F9 (tasks t-fee68403, t-e96cc0ad, t-2f6577cf, t-4ec88dae, t-1c11e831, t-8b824981, t-5ca5463b, t-7de9113d, t-92e004b7) partition this list; the task descriptions carry the actionable detail. This file is the reference index.

## Integrity note
The Monitor auditor initially fabricated its Kanban sub-report and retracted it. A dedicated re-audit produced the real Kanban findings below; two retracted claims turned out directionally correct with different citations, the rest of the fabricated list was discarded. Only re-audit citations are canonical.

## Blockers by section

PROJECTS (verdict NO-GO pre-fix): widget symlink-escape file read (WidgetPathResolver:46-53; tested helper exists in MiniAppAssetResolver); mini-app exfiltration chain (toast payloads logged privacy .public + fileRead not sensitive → pre-ticked for agent apps); WebviewWidgetView arbitrary URL, JS on, no navigation policy; component String params kill extraction (CockpitEmptyState, WidgetErrorCard — catalog holds dead translations); fleet-apply pushes per-host preset UUIDs; sidebar context-menu sync SSH in body; log-tail whole-file read.

MONITOR (Dashboard/Insights/Sessions/Activity; Kanban separate): "Last 7 days" stats are all-time (statsSQL no WHERE); callId → SQL → remote heredoc shell escape (SQLValueInliner newline); shadow-detector copy-command clobbers ~/.hermes/auth.json; String-param translation regression (verified dead-but-translated keys: Overview, Top Tools, Notable Sessions, All projects, Unattributed, ARGUMENTS, Loading tool details…; verified missing: By Model, By Platform, Starred, Today, etc.); one accessibility modifier across four features. Should-fix: heavy snapshot columns + active filter drift, silent snapshot failure, Insights dual populations + close-per-load (gh#102 regression) + unbounded queries, Sessions empty Model column / wrong Updated column / lying Starred pill / silent delete / empty Reasoning / preview precedence, Activity UUID filter labels, missing load-generation guards, eager 500-row table, plural hacks, C-locale number formatting.

KANBAN (re-audit, verdict not-ship-ready): no `--` before any free-text positional (title/comment/block reason [also space-split]/promote reason/swarm goal); forgeable "no matching tasks" sentinel wipes the board (two sites); bulk promote corrupts audit reason (CLI takes single positional + --ids); KanbanCardView zero accessibility incl. .help-only zombie warning; List mode drops tenant/session scope; assignees parses a v0.12 table → all-zero counts + phantom NAME row; polling storm (2+2+1 spawns per tick, no backoff/scenePhase, full log re-fetch with --tail unplumbed); worker body rendered as live markdown; column/empty/error strings extraction-invisible. Clean: transition planner, optimistic merge, purge/complete/unblock argv.

INTERACT (Chat verified sound incl. all three backfill fixes): Memory editor silently destroys dirty drafts on watcher ticks (~1.5s, any agent activity) — blocker; markdown links no scheme allowlist; ANSI-strip regex invalid (raw-string \u{001B}); dropped permissions never cancelPermission'd; Curator/Memory a11y gaps; SkillsViewModel/CuratorViewModel banner strings + LoadingOverlay label unlocalizable; curator stderr logged public; transient-clear no generation token; skills symlink write-through; InstallFromURLSheet name/category unvalidated.

CONFIGURE-1 (Platforms/Personalities/QuickCommands/CredentialPools): slack require_mention silent no-op concealed by Scarf's own reader (write extra.require_mention); email skip_attachments dead key (needs extra.); secrets in argv (whatsapp_cloud, ntfy, auth add --api-key) — remote /proc exposure; SSH .env writes world-readable (missing chmod 600); ToggleRow/PickerRow unlabeled; 20 VM status literals unlocalizable; main-actor writes throughout (loads are correctly detached); GatewayAllowlistKind English vocab. Clean: GatewayConfigWriter, personalities parse, W8 round-trip, allowlist key mapping, token display hygiene, injection.

CONFIGURE-2 (Plugins/Webhooks/Profiles/Models/Proxy, verdict fails): Plugins reads a .disabled marker Hermes never writes (fabricated state) + install leaves plugins disabled while reporting Installed + tool-override consent unreachable; Profiles export .zip vs CLI .tar.gz (remote export impossible) + delete exit-0 false success (needs -y behind Scarf confirm); Webhooks list parser matches nothing (real format: indented ◆ rows); generated HMAC secret discarded + plain TextField; proxy outlives app (no terminate hook); ProjectModelPresetSheet main-actor SSH; VM toasts unlocalizable; row a11y. Clean: profile list parse, import traversal (CLI-side), session-model binding, proxy loopback pinning, catalog virtualization.

MANAGE (Tools/MCP/Gateway/Cron/Health/Logs/Settings, verdict not-shippable pre-fix): SettingsComponents String labels — 133/155 Settings labels never localized; MCP add blind y\ny\ny\n writes junk credential + exit-0 false success + servers land disabled; MCP editor bound to local context on remote hosts; one-shot cron edit impossible (display string → parse_schedule); Health/Gateway/Settings main-actor CLI actions; Logs whole-file read + unbounded entries + 4× filter per render; gateway green badge trusts stale JSON state; capabilities panel frozen at v0.15; finishEdit non-force reload; catalog default_enabled dropped; tools toggle silent revert; header values plain TextField in editor; plural-hack keys empty in all locales; Tools/Logs/Gateway/Health zero a11y. All four 2ea677e backfill fixes verified correct.

BOTS+PEERS (verdict GO, merge held): BotPresence strings unlocalizable from ScarfCore (token-enum fix); BotRosterSort English picker; PeersView selected-ternary English + trait-redundant; BotsRosterScan shell name validation (parity divergence); roster eager VStack + double filter/sort per body; de register nits. All Phase A/B audited invariants held; view-model banner-string class is app-wide policy, not a Bots defect.

## Cross-cutting themes
1. String-typed display params bypass the catalog (ScarfDesign was fixed in v2.24; SettingsComponents, LoadingOverlay, Cockpit/Widget/Cron/MCP/Insights/Activity/Sessions/Dashboard/Tools/Kanban/GatewayAllowlist/BotPresence/BotRosterSort were not) — F7.
2. Synchronous transport/CLI on the main actor in mutation paths while load() paths are correctly detached — a drift pattern across ≥12 files — F6.
3. CLI-contract rot on surfaces that never got the argv-vs-argparse audit (Plugins, Webhooks, Profiles export/delete, MCP add, kanban assignees/promote, one-shot cron) — F3/F5.
4. Untrusted text at boundaries: argv without `--`, SQL heredoc newlines, markdown rendering of agent text, sentinel substring matching — F1/F2.
5. Accessibility coverage is feature-lottery: audited features (Servers/Projects-sidebar/Skills/Bots) are good; Monitor/Health/Tools/Logs/Kanban/Curator have ~zero — F8.
