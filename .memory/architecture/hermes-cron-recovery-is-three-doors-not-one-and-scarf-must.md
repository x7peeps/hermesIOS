---
title: Hermes cron recovery is three doors, not one — and Scarf must model all three
type: note
permalink: scarf/architecture/hermes-cron-recovery-is-three-doors-not-one-and-scarf-must
tags: [hermes, cron, capability-gating, v0.21]
source_paths: [scarf/Packages/ScarfCore/Sources/ScarfCore/Models/HermesCronJob.swift, scarf/Packages/ScarfCore/Sources/ScarfCore/Models/CronRecoveryOffer.swift, scarf/Packages/ScarfCore/Sources/ScarfCore/ViewModels/IOSCronViewModel.swift, scarf/scarf/Features/Cron/ViewModels/CronViewModel.swift, scarf/Packages/ScarfCore/Sources/ScarfCore/Services/HermesCapabilities.swift]
source_paths_inferred: false
source_sha: f934cdf6d79ce87411296880ecdff3d0f9b02853
created: 2026-09-10
updated: 2026-09-13
reviewed: 2026-09-19
reviewed_by: audit:claude-code (background)
---

## Observations
Hermes has three separate activation doors for a cron job, each with its OWN terminal predicate. Scarf modelled them as one (`isTerminal`) and produced two dead ends. `CronRecoveryOffer` + `HermesCronJob.recoveryOffer(hostRefusesTerminalJobs:hostRecoversErrorRecurring:hostRefusesPastOneShotResume:now:)` is the single port; both platforms' view models delegate to it, and `scarfTests/CronRecoveryOfferP30Tests` is the drift alarm.

| door | Hermes predicate @ v2026.9.7 | recurring `error` | recurring `completed` | terminal `once` |
|---|---|---|---|---|
| `resume_job` -> `update_job` | `is_terminal_job and not _is_recoverable_error_job` (`cron/jobs.py:1865-1878`) | OK from v0.21.0 | refused | refused |
| `rearm_oneshot` (`resume --run-now` / `--at`) | the job's OWN `schedule.kind != "once"` raises `_REARM_RECURRING_ERROR` (`:2040-2042`, `:2065-2066`) | refused | refused | OK |
| `trigger_job` (`cron run`) | bare `is_terminal_job` (`:2012`) | refused | refused | refused |

The `rearm_oneshot` guard is checked twice: once on the PARSED `run_at` (always `once` for an injected `now`, so it never fires on the `--run-now` path) and once on the job's own schedule inside `apply` — only the second one matters. `cron_resume` turns either into exit 1 (`hermes_cli/cron.py:691-695`).

## Observations
- [constraint] `_is_recoverable_error_job` is `state == "error" and schedule.kind in {"cron","interval"}`; first tag with it is v2026.8.31 (0.21.0), last without it v2026.8.27 (0.20.6) — hence `hasCronRecoverableErrorResume = isV021OrLater` #capability-gating
- [constraint] Re-arm (`cron resume --run-now`) is one-shot-only and always has been: the own-schedule guard is inside `rearm_oneshot` at the function's first tag, v2026.8.27 (0.20.6) — the same floor as `hasCronResumeRunNow`, so it needs no flag of its own #cron
- [gotcha] The resume gate and the run gate are DIFFERENT predicates: `update_job` exempts recoverable-error jobs, `trigger_job` does not — so a recurring job in `error` is resumable and not runnable, and sharing one `refusesTerminalJobLocally` for both is a bug #cron
- [decision] A recurring job that reached `completed` gets no resume/re-arm button, a hint that says "duplicate it to schedule a new one", and (round-4 decision 5, P42) a **Duplicate button** behind that hint on all three surfaces — an ordinary `cron create` pre-filled from the record, which is the one door no terminal guard sits on. P30's original "edit the schedule to run it again" copy named a gesture Hermes REFUSES (`_apply_schedule_update` writes `next_run_at`, `update_job` then raises at `:1965`) #cron
- [decision] **Every hint the offer can produce names Duplicate, on every arm** (P50, round-5 decision 11). The pre-v0.21 recurring-`error` arm (`errorNeedsNewerHermesHint`) was the last holdout: it said "edit the schedule to re-arm it", and that gesture is refused on exactly the hosts that see it — at `v2026.8.27` `update_job` writes `next_run_at` for any record whose `state != "paused"` (`cron/jobs.py:2310`, `:2322`, `:2345`), an `error` job qualifies, and the guard below raises because `is_terminal_job` is the bare `state in {completed, error}` (`:638-640`) with no `_is_recoverable_error_job` exemption at that tag (`:2367-2375`). `create_job` (`:1915`) carries no terminal guard, and Duplicate is rendered on every surface that renders this hint (Mac detail pane and Bots list on `offer.isDeadEnd`; both row context menus unconditionally). `HermesCronRecoveryP50Tests` drives `recoveryOffer` over every hint-producing arm instead of listing one family — which is how the P38 test let this arm through for two rounds #cron
- [gotcha] Hermes's `_has_pause_marker` is TWO arms — `state == "paused" or bool(job.get("paused_at"))` (`cron/jobs.py:477-479`). Scarf ports only the second (`isTruthyPauseMarker`) because `effectiveState` already carries the first. Python truthiness: `""`, `0`, `false`, `[]`, `{}` are NOT markers; reading any non-null as one made Scarf render "paused" for a job the host kept firing #cron
- [decision] Round-4 decision 6 (P42): iOS gained the re-arm itself (`IOSCronViewModel.resumeAndRunNow(id:)` → `cron resume <id> --run-now`) instead of pointing at the Mac, gated on the same `offer.canRearm`. It has **no JSON fallback** by design — a re-arm rewrites a terminal record's schedule, claims and repeat counter (`:2036-2075`), exactly the state `_reject_terminal_activation` exists to stop a client inventing #cron
- [refinement] P53 refactored refusal messages via `ResumeRefusal` struct (reason/remedy/joiner fields) to prevent punctuation bugs: P50b's `editorEnabledLockNote` footer trimmed the banner sentence at its first `" — "` seam, which split sentences like "… — the one-shot time (…) is in the past …" — the reason itself carried the dash. The seam is now an explicit field so different surfaces (list banner, modal editor, bots) can reframe the same reason for their context without guessing where to split #cron
- [decision] P53 decision: the modal editor's locked `Enabled` toggle footer (`editorEnabledLockNote`) keeps the REASON from `resumeRefusalMessage`, points to where the remedy lives ("Close this editor, then press and hold to Resume & Run Now" or "… swipe to Duplicate"), rather than duplicating the row's full actions into a sheet toolbar that covers the list they reference #cron

## Relations
- relates_to [[Hermes v0.21.1 Compatibility Decisions]]
- relates_to [[Hermes Capability Gating Pattern]]

- [fact] **`trigger_job` uses the BARE `is_terminal_job`, so Run Now on a terminal job is a guaranteed exit 1** (`cron/jobs.py:2012` @ `v2026.9.7`) — no `_is_recoverable_error_job` exemption, unlike the resume door, which is why the two gestures need SEPARATE predicates and `refusesTerminalJobLocally` is not the same question as `recoveryOffer.canResume`. P50 (round 5, `55e464f8`) disabled the Mac detail pane's PRIMARY "Run now" button, the last live offer of it; the row context menu (`:494`) and `BotRoutinesView` (`:201-203`) already refused #cron


- [gotcha] **`next_run_at` is the one runtime field a schedule change invalidates, and "forward every field the editor doesn't own" is what hid it** (P56, `d3db068c`). iOS `CronEditorView.buildJob` carried `existing?.nextRunAt` across a re-time, alongside `last_run_at` and the failure counters where the rule is right. The due scan fires on the STORED instant — `_evaluate_due_job` reads `job.get("next_run_at")` (`cron/jobs.py:2925` @ `v2026.9.7`) and recomputes only when it is absent — and only ONE kind self-repairs: `_reanchor_stale_cron` (`:2801-2819`) re-anchors a `cron` instant off its expression's lattice, an `interval` fires once early, and a `once` is RETIRED unrun by `_retire_expired_oneshot` (`:2853-2865`). A one-shot moved to next Tuesday was deleted for missing last Tuesday. Fix is `nextRunAt: scheduleMoved ? nil : existing?.nextRunAt`; clearing hands the recomputation to `_recover_missing_next_run` (`:2690-2710`), the same contract `clearingNextRunAt()` documents for the `setEnabled` fallback. **The Mac has no twin** — `CronView` shells `cron edit`, where `_apply_schedule_update` (`:1899-1910`) rewrites the field itself. When a form forwards a record's machine fields, ask which of them the edited fields COMPUTE #cron #ios
- [constraint] **Every validation `cron edit` performs, and where each one lives on iOS** (P56, addendum lesson 14). Enumerated from `cron_edit` (`hermes_cli/cron.py:594-632`) through `update_job` (`cron/jobs.py:1929-1971`) at `v2026.9.7`: `_IMMUTABLE_JOB_FIELDS` (`:369`) and `_UPDATE_FIELD_NORMALIZERS` (`:1591-1596`) cover fields the iOS sheet has no widget for; `_reject_terminal_activation` (`:1865-1878`) is `CronEditorView.enabledIsLocked` (P50b); `job_payload_is_empty` (`:428-435`) is the form's non-blank-prompt rule, strictly stronger; `_validate_job_mode_invariants` cannot fire because the sheet cannot change `script`/`no_agent`/monitor; `_next_run_or_reject_past_oneshot` / `_fill_missing_next_run` (`:1899-1927`) are `oneShotTimeIsUnusable` (P50 decision 13). THREE had no iOS home and now live in `HermesCronJob.scheduleFormRefusal`: a blank or malformed cron `expr`, an `interval` with no `minutes`, and an unreadable one-shot `run_at`. All three land the SAME silent way — `compute_next_run` answers nil (`:1096-1123`) and the due scan's recovery cannot arm a record it cannot compute, so the job reads "scheduled" forever and never fires — except a malformed-but-non-empty `expr`, which reaches an untried `croniter(expr, base_time)` (`:1122`) and raises inside the tick. The port is SHAPE-ONLY, faithful to `parse_schedule`'s own pre-filter — **`cron/jobs.py:757-758` @ `v2026.9.7`; `:755-756` is the COMMENT above it, corrected in P56b `aa708b9c` after re-opening the blob (the wrong range had been pasted at five sites)** — never a croniter reimplementation: Scarf refuses what Hermes's pre-filter refuses and leaves the host the final word #cron #ios
- [fact] The iOS form has no `minutes` field at all, so `.intervalMinutesMissing` refuses every NEW or kind-switched `interval` job rather than writing one that can never fire; editing an existing interval still saves because `buildJob` carries the stored `minutes` while the kind is unchanged. Growing the form that field is `t-b74c65a4` #cron #ios
